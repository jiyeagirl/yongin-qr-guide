/* 본 화면(공공시설·상점가·둘러보기 탭) 공통 설정.
 *
 * KAKAO_APP_KEY — Kakao Developers 의 **JavaScript 키**.
 *   키만으로는 지도가 뜨지 않는다. 같은 콘솔에서
 *   [앱 설정 > 앱 > 플랫폼 키 > JavaScript 키 > SDK 도메인] 에 이 화면을 여는 주소를 등록해야 한다.
 *   로컬 확인용은 http://localhost:8000 (tools/serve.mjs 의 기본 포트).
 *     - localhost 와 127.0.0.1 은 서로 다른 도메인으로 취급된다. 등록한 쪽으로 접속할 것
 *     - 포트까지 정확히 일치해야 한다
 *     - 입력값 앞뒤 공백 주의 ("유효하지 않은 도메인" 오류의 흔한 원인)
 *   키가 비었거나 도메인이 등록되지 않으면 KakaoMap 이 목업(MapCanvas)으로 떨어지고
 *   지도 자리에 사유가 표시된다. 레이어 구조와 시트 동작은 그대로 검증할 수 있다.
 */
export const KAKAO_APP_KEY = "d635e21f2375b9e1ff4ac5935b967314";

/* 지도 초기 확대 레벨. 4 = 약 반경 500m 가 화면에 들어온다 (상점가 한 구역 크기). */
export const MAP_LEVEL = 4;

/* 탭별 기본 줌 레벨 (기능명세서 5-3 #6).
   탭을 바꾸면 카메라를 QR 지점으로 되돌리고 이 레벨로 맞춘다 —
   지도를 멀리 끌어둔 채 탭을 바꾸면 새 레이어의 마커가 화면 밖이라 "빈 화면"으로 보인다.
   같은 탭 안에서는 유지한다 (필터를 바꿔도 카메라는 그대로).

   공공시설·상점가는 QR 지점 주변이 대상이라 4,
   둘러보기의 축제는 32개소 전체가 대상이라 더 넓게 잡는다. */
export const TAB_MAP_LEVEL = { facility: 4, district: 4, discover: 7 };

/* 무한 스크롤 한 번에 붙이는 점포 수 (U-ST-04). 335곳을 한 번에 그리지 않는다. */
export const PAGE_SIZE = 20;

/* 둘러보기 탭 축제 목록에서 접지 않고 보여주는 수 (U-DC-01).
   32개소 전체가 대상이라 6건이 한 번에 깔리는데, 그러면 이 탭의 나머지 세 섹션
   (신규 매장 · 인기 매장 · 골목 한바퀴)이 통째로 첫 화면 밖으로 밀린다.
   목록은 상태(진행중 → 예정 → 종료) 다음 거리 순이므로 앞의 두 건이 가장 급한 것이다.
   나머지는 [더 보기]로 그 자리에서 펼친다 — 자료를 줄이는 것이 아니라 접는 것이다. */
export const FESTIVAL_PREVIEW = 2;

/* 둘러보기 탭 "다른 상점가"에서 깔아두는 수 (U-DC-04).
   31곳을 한 번에 펼치면 탭 최하단이라는 배치 자체가 무의미해진다 — 그 위 섹션들이 화면 밖으로 밀린다.

   **[더 보기]로 8곳씩 붙이던 것을 5곳 고정으로 바꿨다** (2026-08-18). 누를 때마다 목록이
   길어져 탭 최하단이 화면 서너 개 분량이 됐고, 그렇게 다 펼쳐도 거리순 31줄이라 아는 이름
   하나를 찾을 방법이 없었다. 접었다 폈다 하는 손잡이가 하는 일은 결국 **같은 화면에 두 가지
   길이를 만드는 것**이다. 나머지는 [전체보기]가 여는 S13 이 구(區) 칩과 함께 맡는다.

   5인 이유: 축제 카드 2장 + 코스·매장 레일이 위에 있어서, 여섯 줄부터는 이 섹션 혼자
   한 화면을 넘긴다. 다섯 줄이면 "가까운 데가 이 정도 있구나"가 한눈에 읽히고 끝이 보인다. */
export const DISTRICT_PREVIEW = 5;

/* 도보 속도 — 거리 표기를 "약 320m, 도보 5분"으로 환산할 때 쓴다 (U-FC-06).
   직선거리 기준이며 목록 단계에서 경로 API 를 호출하지 않는다. */
export const WALK_M_PER_MIN = 67;

/* ── 도보 길찾기 (S07 · U-NV-01 / 04 / 05) ─────────────────────────────────
   경로를 받아올 **서버 프록시** 주소. 비워두면 예시 경로가 그 자리를 채운다
   (더미 데이터 3종과 같은 취급이다). 자세한 내용은 data/walkRoute.js 머리말 참조.

   왜 제공자 주소를 바로 적지 않는가:
     1. 카카오가 공개한 길찾기 REST API 는 **자동차** 경로다. 도보는 공개 API 가 없어
        어느 제공자를 쓰든(카카오 협의 · TMAP 보행자 API) 서버 한 단계가 필요하다.
     2. REST 키는 브라우저에 들어갈 수 없다. 이 파일은 배포본에서 누구나 읽는다 —
        위의 KAKAO_APP_KEY 가 여기 있어도 되는 것은 JavaScript 키가 공개 전제이기 때문이다.
     3. 캐시와 1일 1,000건 한도(U-NV-05)는 기기별이 아니라 앱 단위라 서버에서 걸어야 맞다. */
export const WALK_ROUTE_ENDPOINT = "";

/* 기기별 2차 방어선. 진짜 한도 관리는 서버가 한다 (위 3번). */
export const WALK_ROUTE_QUOTA = 1000;

/* 경로 응답을 기다리는 한계(ms). 길 위에서 8초를 기다리느니 직선 안내(U-NV-04)가 낫다. */
export const WALK_ROUTE_TIMEOUT = 6000;

/* 예시 경로를 일부러 늦추는 시간(ms). **검수용 상수다 — 실 연동 시 0 으로 둔다.**
   예시 경로는 계산이 즉시 끝나 로딩 상태가 한 프레임도 보이지 않는데,
   실 API 는 수백 ms 가 걸린다. 그 사이 화면이 어떤지 확인할 수 없으면 만들 수도 없다. */
export const SAMPLE_ROUTE_DELAY = 320;

/* 정보 기준일자 (U-CM-07). 점포는 상가정보 분기 갱신이라 dunjeon.js 가 따로 갖고 있고,
   공공시설은 표준데이터 갱신 주기가 달라 여기서 관리한다. */
export const FACILITY_AS_OF = "2026.05 기준";

/* 축제 정보 기준일자 (U-CM-07). 시설·점포와 갱신 주체가 다르다 —
   축제는 공공데이터가 아니라 산업진흥원과 상인회가 전달하는 자료라 주기가 따로 논다. */
export const FESTIVAL_AS_OF = "2026.08 기준";

/* 골목형 상점가 지정 현황 기준일자 (U-CM-07). 시 고시로 갱신되는 자료라 위 셋과 또 다르다.
   둘러보기 탭 최하단 고지와 S13 전체보기가 같은 값을 봐야 한다 — 한쪽에만 적어두면
   지정이 늘었을 때 한 화면만 고쳐진다. */
export const DISTRICT_AS_OF = "2026.07 기준";

/* 축제 상태(진행중·예정·종료) 판정 기준일 — **검수용 상수다. 실서비스에서는 제거하고 서버 시각을 쓴다.**
   1차 제공 축제 6건이 전부 2026.10~11 이라 실제 오늘(8월)로 계산하면 6건 모두 "예정"이 된다.
   그러면 U-FT-01 이 요구하는 세 가지 상태 구분과 U-CM-18 탭 뱃지를 화면에서 확인할 방법이 없다.
   둔전 축제일(10.17)로 맞춰 두면 한 화면에서 셋이 모두 나온다 —
   에버랜드마을 종료 / 구성언남·둔전 진행중 / 남사한숲·머내마을·보정중심 예정. */
export const TODAY = "2026-10-17";
