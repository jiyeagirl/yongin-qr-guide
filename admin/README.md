# 관리자 웹 (`/admin/`)

기준 문서는 **`admin/admin_specifications.md` (관리자 웹 기능명세서 v1.6)** 이다.
화면은 **열다섯이고 번호가 M01 ~ M15 로 이어진다.** 환경 설정 화면이 빠지면서
(2026-08-24) 한동안 빈 M15 를 남겨 두었는데, v1.5 에서 뒤 번호를 당겼다 —
**계정 관리가 M16 에서 M15 가 되었다.** 옛 문서의 「M16」은 지금의 M15 다.

> **범위가 좁다.** 명세서 머리말이 그것을 못 박았다 —
> "개별 건의 조회·수정·신규 등록과 오류신고 기반 보정. **데이터 일괄 적재, 매칭, 갱신,
> 검수는 개발 쪽에서 처리하며 별도 문서(데이터 매칭 및 검수 기획)로 분리한다.**"
>
> 그래서 이 화면들이 하는 일은 한 가지다: **한 건씩 고치고, 한 건씩 새로 넣는다.**
> 무엇이 빠졌고 왜 빠졌는지는 아래 「범위 밖으로 나간 것」에 있다.

## 시민용과 왜 완전히 나누는가

**시민용 앱에서 들어갈 수 있는 입구를 두지 않는다.** QR 을 찍고 들어온 시민이 관리자
화면을 볼 이유가 없고, 로그인이 붙는 순간 그 링크는 실패하는 문으로만 남는다. 그래서
주소도 폴더도 나눈다 — `/screens/main/` 과 `/admin/` 은 서로를 가리키지 않는다.
(검수 입구인 저장소 루트 `index.html` 에만 링크가 있다.)

**디자인 시스템 진입점도 나눈다.** `design-systems/index.js` 는 시민용, `admin.js` 는
관리자용이다. 관리자 컴포넌트를 `index.js` 에 합치면 재수출을 타고 시민용 모바일 번들에
데이터 표와 다이얼로그가 들어간다 — 길에서 QR 을 찍는 사람이 내려받을 이유가 없는 것들이다.
합치지 않은 결과, 관리자 화면을 열 개 만든 뒤에도 시민용 번들은 **95모듈 751KB 그대로**다.

## 화면

**대상을 부르는 이름은 「골목형 상점가」다** (2026-08-24) — 메뉴 · 제목 · 단추 · 다이얼로그 · 토스트 ·
항목명까지 줄이지 않고 그대로 적는다. 지정 제도의 이름이고 시민 화면도 그렇게 부른다.
예외 하나: 오류신고의 「대상 유형」 값(`상점가`)은 **시민 신고 폼과 함께 쓰는 저장값**이라
바꾸지 않았다 — 바꾸면 이미 접수된 건의 값과 갈린다.

| 화면 | 명세서 | 파일 | 경로 |
| --- | --- | --- | --- |
| 로그인 · 비밀번호 초기화 요청 | M01 | `pages/Login.jsx` | (로그인 전 전체화면) |
| 대시보드 | M02 | `pages/Dashboard.jsx` | `#/dashboard` |
| 골목형 상점가 목록 · 등록 · 수정 | M03 · M04 | `pages/Districts.jsx` | `#/districts` |
| 점포 목록 · 등록 · 수정 | M05 · M06 | `pages/Stores.jsx` | `#/stores` |
| 축제 목록 · 등록 · 수정 | M07 · M08 | `pages/Festivals.jsx` | `#/festivals` |
| 공공시설 목록 · 등록 · 수정 | M09 · M10 | `pages/Facilities.jsx` | `#/facilities` |
| QR 지점 목록 · 현황 수정 | M11 · M12 | `pages/QrPoints.jsx` | `#/qr` |
| 오류신고 관리 | M13 | `pages/Reports.jsx` | `#/reports` |
| 데이터 갱신 현황 | M14 | `pages/DataAsOf.jsx` | `#/asof` |
| 계정 관리 · 비밀번호 초기화 요청 | M15 | `pages/Accounts.jsx` | `#/accounts` |

**환경 설정 화면이 있었다** — `pages/Settings.jsx` · `#/settings` (2026-08-24 삭제.
그때의 번호가 M15 였고, 지금 그 번호는 계정 관리의 것이다).
두 구획이었고 둘 다 이 화면의 일이 아니었다.

- **서비스 운영 설정** (안내 범위 · 배너 기준 · 상점가 임계 거리 · 신규 매장 판정 기간 ·
  코스 반경 · 탭별 지도 확대 단계) → **개발 쪽.** 개별 건이 아니라 화면 전체가 도는
  방식을 정하는 값이다. 게다가 화면에 서 있는 내내 아무 데도 연결되지 않았다 —
  `config.js` 는 모듈 상수라 관리자가 밀어 넣을 수 없어서, 담당자가 반경을 고쳐도
  시민 화면은 그대로였다. 값 자체는 그 코드에 그대로 있다.
- **변경 이력** → 기록은 계속 쌓인다 (`data/store.js` 의 `readHistory`). 없어진 것은
  **보는 자리**이고, 다시 둔다면 대시보드 아래가 자연스럽다.

**목록과 등록·수정을 한 파일에 둔다.** 명세서는 M03/M04 처럼 둘로 나눠 세지만, 폼은
목록 위에 뜨는 다이얼로그다. 페이지를 나누면 335곳에서 조건을 좁혀놓고 한 곳 눌러본 뒤
돌아왔을 때 필터·쪽 번호·스크롤이 통째로 초기화된다 — 명세서 1장의 "상세 복귀 시
필터·스크롤 유지"를 지키는 가장 짧은 방법이 애초에 목록을 언마운트하지 않는 것이다.
(시민용 셸이 상세를 오버레이로 여는 것과 같은 판단이다.)

**공공시설 관리가 기준 화면이다.** 골격(머리 → 유형 탭 → 필터 줄 → 표 → 다이얼로그 폼 →
삭제 확인)을 거기서 정하고 나머지가 복제한다. 새 관리 화면을 만들 때 배치를 다시 정하지 않는다.

## 범위 밖으로 나간 것 (2026-08-20 명세서 개정)

명세서가 관리자의 일을 **한 건 단위**로 좁히면서 다섯 가지가 빠졌다. 하나씩 이유가 다르고,
그 이유가 전부 같은 곳을 가리킨다 — **화면에서 결과를 되돌릴 수 없는 일은 화면의 일이 아니다.**

| 빠진 것 | 어디 있었나 | 왜 빠졌나 |
| --- | --- | --- |
| **매칭 검수 큐** | 화면 하나 (구 M07) | 실패를 손보는 일이 곧 매칭을 다시 돌리는 일이다 |
| **공공데이터 동기화** | 화면 하나 (구 M18) | 원본 조회·비교·반영 전부 서버 일이다 |
| **점포 일괄 업로드** | M05 의 다이얼로그 | 5,000행을 한 번에 넣고 화면에서 되돌릴 방법이 없다 |
| **구역 주소 목록 · 재매칭** | M04 의 1:N 편집기 | 주소를 손보는 일이 곧 매칭을 다시 돌리는 일이다 |
| **게시값 대비 산출값 비교** | 상점가 목록의 경고 | 명세서 2-1: "340 대 335처럼 차이를 알아도 **어느 5곳인지 모르면 고칠 수 없기 때문**" |

함께 사라진 작은 것들:

- **`sort_order` 노출 순서** (▲▼ 로 옮기던 것) — 항목표에서 빠졌다. 32곳의 차례는
  시민 화면이 거리로 정한다
- **공공시설 `source`** (원본 · 원본수정 · 직접등록) — 동기화가 어느 필드를 덮어쓸지
  정하는 값이었다. 읽는 곳이 없어졌다
- **「매칭 검수 대기」 처리 대기 배지** — 누를 곳이 없는 배지는 숫자만 있고 할 일이 없다
- **`ref_store_count` · `ref_onnuri_count`** 입력칸 — 점포수는 이제 ⚙ 읽기 전용 둘뿐이다
- **권한 구분 자체** (`CITY` · `DEVELOPER`) — 이 화면을 쓰는 사람이 용인시 담당자뿐이라
  업무에 나눌 등급이 없다 (2026-08-20). 계정 항목표에서 `role` 이 빠졌다.
  2026-08-24 에 계정 관리 하나가 최종 관리자(`admin`)에게만 열렸지만 `role` 은 돌아오지
  않았다 — 판정을 아이디로 한다 (아래 「계정」)
- **QR 지점 등록·삭제** (+식별자 자동생성) — 지점이 생기는 일은 화면에서 값을 만드는 일이
  아니라 안내판을 제작해 붙이는 일이다 (2026-08-20). 관리자는 설치 현황만 갱신한다
- **API 쿼터 설정 구획** — 한도·임계치는 카카오 계약과 서버가 정하는 값이라 화면에서 손댈
  자리가 아니다. 오늘 사용량은 대시보드가 그대로 보여준다
- **「조회수 상위 점포 10 · 조회수 상위 공공시설 10」 순위표** — 시민 화면에 그런 목록이
  없다 (2026-08-20). 그 자리는 상점가를 골라 **둘러보기 탭의 두 레일을 그대로 보는**
  구획이 대신한다. 담당자가 보는 여섯 곳이 곧 시민이 보는 여섯 곳이다

지운 파일: `pages/MatchQueue.jsx` · `pages/Sync.jsx` · `pages/BulkUpload.jsx` ·
`data/queue.js` · `data/bulkUpload.js` · `data/zones.js` · `data/districtRef.js`.
관리자 번들이 **120모듈 1,022KB → 113모듈 935KB** 로 줄었다.

## 입력 항목은 명세서가 정한다

`data/fields.js` 가 **명세서의 항목표를 그대로 들고 있고**, 폼 열이 전부 거기서 나온다.
화면이 항목을 하나 더 넣고 싶어도 넣을 자리가 없다 — 그게 이 구조의 요점이다.

폼이 지키는 것 여섯:

1. **명세서에 있는 항목만.** 없는 칸을 만들어 두면 담당자는 채워야 하는 것으로 읽고,
   채운 값의 출처는 아무도 모른다
2. **필수 열 네 값이 화면에서 서로 다르게 보인다.** ● 필수 · ○ 선택 · ◐ 조건부 · ⚙ 자동을
   각각 다른 배지로 적는다. 하나로 뭉개면 담당자가 ⚙ 칸을 비어 있는 필수 칸으로 읽고
   아무 값이나 채운다 — 그 순간 집계값이 수기값이 된다
3. **범위를 칸 아래에 그대로 적는다.** `2~40자` · `0~9999`. maxlength 로 막기만 하면
   담당자는 글자가 더 안 써지는 이유를 모른 채 지운다
4. **선택 항목을 비우면 시민 화면에 `-` 가 뜬다**고 폼 아래 한 줄로 적는다
5. **가부 항목은 Switch, 저장값은 불리언.** 표시(○ · ×)는 읽는 쪽이 정한다
6. **데이터 기준일 칸이 없다.** 카테고리 단위로 M14 에서 정한다 (명세서 7장)

### 이름이 둘인 이유

명세서는 `address_road` · `store_count` 처럼 **DB 컬럼명**으로 항목을 부른다. 그런데
시민용 화면 열둘이 이미 `addr` · `stores` 를 읽고 있어, 이름을 바꾸면 잘 돌고 있는 화면
열둘을 뜯게 된다. 그래서 **키는 우리 것을 쓰고 명세서 이름을 `spec` 에 적어 둔다.**
서버 연동 때 이 한 표가 곧 매핑표가 된다.

## 명세서의 까다로운 대목을 어떻게 풀었나

| 명세서 | 어디서 | 어떻게 |
| --- | --- | --- |
| V-02 도로명주소 직접 타이핑 불가 | `AddressField.jsx` | 카카오 지도 SDK 의 `services`(Places · Geocoder)로 검색한다. **JavaScript 키로 도는 기능이라 서버 없이 실제로 동작한다** — 이 화면에서 유일하게 더미가 아닌 부분이다. 고른 주소가 좌표를 함께 들고 온다 |
| 입력 원칙 3 좌표 입력란 없음 | `CoordField.jsx` | 위경도 칸 대신 지도만 준다. 자동 변환된 자리가 틀렸으면 눌러서 옮기거나 핀을 끈다. V-01 범위 밖이면 막지 않고 경고한다 |
| 2-1 점포수 ⚙ | `pages/Districts.jsx` | 노출 상태인 점포를 **실제로 세어** 보여준다. 점포 관리에서 한 곳을 숨기면 이 숫자가 줄어드는 것으로 정의가 확인된다. 점포 자료가 없는 31곳은 세지 못하므로 상점가 레코드의 값을 그대로 보여준다 (「점포 미등록」 배지는 뺐다 — 31곳 전부에 붙어 아무것도 가려내지 못했다) |
| 2-1 `onnuri_market_name` | (화면에 없음) | 온누리 원본의 `소속 시장명` 표기를 그대로 들고 있는 **매칭 키**다. 값은 남아 있지만 관리자 화면에서 고치지 않는다 (2026-08-20) — 고쳐도 그 결과(가맹 몇 건이 걸리는지)가 이 화면에서는 보이지 않고, 0건이 나왔을 때 실제로 손볼 곳은 매칭 설정이다 |
| 2-3 조아용 이미지 선택 | `AssetPicker.jsx` · `data/assets.js` | 업로드가 아니라 사전 등록 에셋 8종의 썸네일 그리드. **다른 축제가 쓰는 그림에 「N건 사용 중」이 붙는다** — 등록하는 사람은 자기 축제만 보고 있어 겹침을 알 방법이 없다 |
| 2-4 · 2-5 프로그램 · 부스 | `Repeater.jsx` | 1:N 편집기. 화면에서는 **그냥 선택 항목**이다 (2026-08-20) — 우선순위 `C` 는 우리 쪽 자료 사정이고 담당자가 하는 행동은 선택 항목과 같다. **부스는 글로 안내한다** — 좌표 · 배치도 이미지(V-06) · 번호 · 유형을 뺐다. 시민 화면이 그대로 인쇄하는 칸만 남는다 (2026-08-24): 두 목록 모두 「시작 일시 · 종료 일시 · 이름 · 위치」로 차례가 같고, **프로그램에만 「설명」이 아랫줄로 붙는다**(`row2`). 시각은 글자가 아니라 날짜·시각 고르개로 받고 달력이 축제 기간 밖을 막는다. 부스의 「규모」는 없앴다 — 무엇을 하는 곳인지는 부스명이 이미 말한다 |
| 5장 동일 대상 3건 강조 | `pages/Reports.jsx` | `fc-004` 에 세 건을 넣어 두었다. 규칙이 실제로 걸리는 데이터가 없으면 만들어놓고 확인할 방법이 없다 |
| 8-1 두 거리 값 | `data/settings.js` | 안내 범위 2km · 배너 기준 1km 를 `facilities.js` 의 `NEAR_LIMIT` · `NEAR_ENOUGH` 에서 가져온다. **시민 화면이 이미 그렇게 돌고 있어** 숫자를 지어내지 않았다 |
| 10장 물리 삭제 없음 | `data/store.js` · `pages/RemovedItems.jsx` | 덮개의 `removed` 가 곧 `is_deleted` 다 — 원본 배열에서는 아무 것도 지워지지 않는다. **그 플래그를 화면에서 되돌린다** (2026-08-24): 목록 다섯의 「전체 \| 삭제된 항목 n」 탭 |
| 10장 삭제 차단 → 연쇄 삭제 | `pages/Districts.jsx` | 막지 않고 **함께 지운다** (2026-08-24). 확인 창이 함께 갈 축제·QR 지점을 이름으로, 소속 점포를 곳수로 적고, 되돌리면 **그 삭제로 지워진 것만** 함께 돌아온다 |
| 10장 변경 이력 | `data/store.js` | 등록·수정·삭제·복구마다 대상 · 변경 필드 · 주체 · 일시를 쌓는다. **열람 화면은 없다** (환경 설정 화면과 함께 나갔다) |

## 데이터

읽기 원본은 **시민용과 같은 파일**이다. 복사본을 만들면 둘이 갈라져, 관리자에서 고친 값이
시민 화면에 없는 상태가 곧바로 만들어진다.

```
읽기  screens/main/data/{facilities,dunjeon,districts,qr}.js
쓰기  admin/data/store.js                    ← 원본 위에 덮개(overlay)
관리자 전용  admin/data/{fields,account,stats,reports,settings,assets}.js
```

`store.js` 의 덮개가 갖는 것은 다섯이다 — `edits`(바뀐 필드만) · `added`(통째 행) ·
`removed`(id) · `links`(연쇄 삭제로 함께 지운 id) · `history`(변경 이력). 읽을 때 원본에 겹쳐 목록을 만든다. 실연동 때는 이
파일이 통째로 fetch/POST 로 바뀌고 화면 코드는 `useCollection` 의 모양만 같으면 손대지 않는다.

> **여기서 고친 것은 시민용 화면에 반영되지 않는다.** 서버가 없어 둘이 자료를 주고받을 길이
> 없고, 덮개는 `sessionStorage` 에 있는데 그 저장소는 **탭 단위**라 시민용 화면을 다른 탭에서
> 열면 애초에 닿지 않는다. 화면 상단의 노란 띠가 이 사실을 늘 적어 둔다.

상단바에 있던 **[데모 데이터 초기화]** 는 뺐다 (2026-08-20). 모든 화면 맨 위, 늘 보이는
자리에 **고친 것을 통째로 되돌리는 버튼**이 서 있을 이유가 없다. 처음 상태로 돌리려면 탭을
닫았다 열면 된다 — 덮개가 `sessionStorage` 라 그것으로 끝난다. 되돌리는 함수 자체는
`store.js` 의 `resetAll` 로 남아 있어 필요하면 다시 어디든 붙일 수 있다.

### 삭제한 것을 되돌린다 (2026-08-24)

10장이 처음부터 물리 삭제를 금지했고 코드도 그랬는데(`removed` 에 id 만 넣는다),
**화면에는 되돌리는 길이 없었다.** 하나 있던 [데모 데이터 초기화]는 그날 고친 것을 전부
버리는 버튼이라, 한 건을 잘못 지운 담당자가 쓸 수 있는 길이 아니다 — 정책은 "되돌릴 수
있다"인데 담당자에게는 영구 삭제로 보였다.

목록 화면 다섯(상점가 · 점포 · 축제 · 공공시설 · 계정)의 제목 아래에
**「전체 | 삭제된 항목 n」** 탭이 선다. `pages/RemovedItems.jsx` 한 벌을 다섯이 함께 쓴다.

- **탭은 늘 있고 숫자만 바뀐다.** 처음에는 필터 줄의 단추였는데, 지운 것이 없으면
  없다가 하나 지우면 나타나서 **검색창 폭이 함께 움직였다** — 방금 한 일(삭제)과 아무
  상관 없는 자리다. 지우기 전에는 되돌릴 수 있다는 사실 자체가 안 보이기도 했다
- **같은 표, 같은 열이다.** 노출·사용 여부 토글만 빼고 관리 칸이 [되돌리기]로 바뀐다.
  이름만 늘어놓으면 같은 이름의 AED 둘 중 어느 것을 지웠는지 알 수 없다
- **되돌리기는 다시 묻지 않는다.** 되돌리기 어려운 일이 아니라 다시 지우면 그만이다
- **되돌린 항목은 노출이 꺼진 채로 돌아온다** (계정은 「사용 여부」). 되돌리는 일과 사용자
  화면에 다시 내보이는 일은 다른 결정이다 — [되돌리기]를 누르는 이유는 대개 "잘못
  지웠다"이지 "지금 이대로 다시 내보내라"가 아니고, 되돌린 자료는 값이 맞는지 아직 아무도
  다시 보지 않았다. 담당자가 그 사실을 모르면 화면에 안 나온다고 다시 지우게 되므로
  토스트가 **다음에 할 일**까지 적는다: 「둔전 골목형상점가를 되돌렸습니다. 목록에서
  [노출 여부]를 켜면 사용자 화면에 나타납니다.」 (`undoToast` · `HIDE_ON_RESTORE`).
  **연쇄 복구로 함께 돌아오는 것은 끄지 않는다** — 상점가가 이미 꺼져 있으므로 그 아래는
  어차피 사용자 화면에 없고, 점포 335곳을 하나씩 다시 켤 이유가 없다
- **새로 등록한 행도 되돌아온다.** 전에는 `added` 에서 통째로 빼서 흔적이 없었는데,
  그러면 가장 되돌리고 싶은 경우(방금 만든 것을 잘못 지움)만 유일하게 안 됐다
- **「휴지통」 화면을 만들지 않는다.** 지운 것은 그 목록에 속한 것이고, 화면을 하나 더
  만들면 여섯 컬렉션이 한 통에 섞여 어느 목록의 것인지 다시 골라야 한다
- 공공시설은 이미 유형 탭 줄이 있어 **그 줄 끝에 이어 붙인다.** 같은 모양의 탭 줄이
  위아래로 둘이 되면 어느 쪽이 무엇을 정하는지 눌러 봐야 안다
- 되돌린 것도 변경 이력에 **「복구」** 로 남는다 — 지운 것만 적히면 이력을 읽는 사람에게는
  아직 지워진 채로 보인다

### 상점가는 연결된 것과 함께 간다 (2026-08-24)

명세서 10장의 "진행 중 축제 또는 활성 QR이 연결된 상점가는 삭제를 차단한다"를 **뒤집었다.**
막다른 길이었기 때문이다 — 상점가 하나를 정리하려면 축제 화면에서 축제를 지우고, QR
화면에서 지점을 지우고, 다시 돌아와야 했다. 세 화면을 오가는 동안 무엇을 지웠는지 스스로
기억해야 하고, 중간에 그만두면 **축제만 없어진 상태**가 남는다.

이제 **축제 · QR 지점 · 소속 점포가 함께 지워진다.** 확인 창이 함께 갈 것을 먼저 보여주고,
되돌리면 함께 돌아온다. 한 번의 결정이 되고 그 결정을 통째로 되돌릴 수 있으므로 막을
이유가 없다.

- **점포도 함께 간다** (2026-08-24 같은 날 고침). 처음에는 점포만 빼 두었는데
  ("335곳을 한 번에 지우는 일은 이 화면이 할 일이 아니다"), **그러면 남은 점포를 볼 수
  있는 자리가 없다.** 시민 화면에서 점포에 닿는 길은 상점가 탭 하나뿐이고, 관리자 점포
  목록에서는 없어진 상점가 이름이 필터에 남는다. 어디에도 뜨지 않는 자료를 "남겼다"고
  말하는 것은 기록으로도 틀리다 — 그것은 남은 것이 아니라 찾을 수 없게 된 것이다
- **확인 창이 셋을 다르게 적는다.** 축제와 QR 지점은 **이름으로**(「축제 · 둔전 골목축제」
  「QR 지점 · 둔전 시장 입구」), 점포는 **곳수로**(「점포 · 335곳」). 335개 상호를 늘어놓으면
  확인 창이 아니라 목록이 되고, 여기서 알아야 하는 것도 어느 가게인지가 아니라
  **이만큼이 함께 간다**는 크기다
- **되돌리기는 "그 삭제로 실제로 지워진 것"만 되살린다.** 지운 그 순간의 id 목록을
  덮개의 `links` 에 적어 둔다 (`linkRemoval` · `takeRemovalLinks`). "지금 지워져 있고 이
  상점가를 가리키는 것 전부"로 두면, 지난주 폐업 오등록으로 따로 지운 점포 한 곳이
  상점가를 되돌리는 순간 **아무도 되살리지 않았는데 되살아난다** — 되돌리기가 자기가
  지운 것 이상을 건드리면 그 단추는 못 믿을 단추가 된다. 따로 지운 것은 각자의 목록 위
  [삭제된 항목]에서 되돌린다
- 컬렉션을 넘나드는 삭제라 `store.js` 의 `removeRows`/`restoreRows`(훅 밖 함수)를 쓴다.
  상점가 화면이 축제·QR·점포 컬렉션을 통째로 세울 이유가 없고, **여럿을 한 번에 넘기는
  것이 중요하다** — 점포 335건을 한 건씩 지우면 저장이 335번 돌고 변경 이력이 그것만으로
  가득 찬다 (`HISTORY_MAX` 200). 이력에는 「일괄 삭제 · 점포 · 둔전 골목형상점가 삭제」
  한 줄로 적는다
- **QR 지점에는 빠져나갈 길이 있다.** 축제는 상점가와 함께 없어지는 것이 맞지만 안내판은
  현장에 그대로 붙어 있다. 지우기 전에 [QR 지점 관리]에서 소속 상점가를 옮기거나
  [지정 안 함]으로 두면 지점이 남는다 (아래 「QR 지점의 소속 상점가」). 확인 창과 그 화면의
  안내 줄이 둘 다 이 방법을 적는다 — 그 화면에는 삭제 버튼이 없어서, 적지 않으면 어제
  있던 지점이 없어졌는데 화면 어디에도 이유가 없는 상태가 된다

### QR 지점의 소속 상점가만 읽기 전용에서 나왔다 (2026-08-24)

M12 에서 읽기 전용이던 다섯 값 중 **소속 상점가 하나만** 폼으로 내렸다. 나머지 넷(식별자 ·
지점명 · 주소 · 좌표)은 **안내판에 인쇄되어 현장에 붙은 것**이라 화면에서 고쳐야 종이와
어긋날 뿐이지만, 소속 상점가는 어디에도 인쇄되지 않는다 — 우리가 이 지점을 어느 상점가에
매어 두었는가일 뿐이고, 위의 연쇄 삭제 때문에 그것이 바뀌어야 하는 자리가 생겼다.

고르개에는 **지금 살아 있는 상점가만** 오른다 (`readCollection("districts", …)`). 지워진
상점가가 남아 있으면 지점을 옮겨 살려 두려던 담당자가 방금 지운 상점가를 다시 고르게 되고,
그 지점은 그대로 다시 사라진다.

## 계정 (2026-08-24 개정)

| 계정 | 볼 수 있는 화면 |
| --- | --- |
| `admin` / `admin1234!` — 최종 관리자 | 10개 전부 (**계정 관리 포함**) |
| `yongin` / `yongin1234` — 담당자 | 9개 (계정 관리 없음) |

**업무 화면에는 등급이 없다.** `CITY`(시청 담당자)와 `DEVELOPER`(개발자)를 없앤 v1.1 은
그대로다 — 시설·점포·축제·QR·신고를 고치는 일에 등급을 매길 이유는 지금도 없고, 아홉
화면은 두 계정이 똑같이 쓴다.

**갈리는 자리는 계정 관리(M15) 하나뿐이고, 이유는 업무가 아니라 자물쇠다.** 모든 계정이
계정을 지울 수 있으면 서로를 지울 수 있고, 마지막 하나를 쓰던 사람이 인사이동으로 떠나면
**아무도 못 들어온다.** 「마지막 계정 보호」는 계정이 0이 되는 것만 막을 뿐 *누가* 남는지는
정하지 못한다. 그래서 **지워지지 않는 계정 하나**(`admin`)를 두고 계정 관리를 그 계정에만
연다 — 담당자가 바뀌어도 돌아올 자리가 늘 남는다.

[삭제] 버튼만 감추는 것이 아니라 **화면을 통째로** 가린다. 화면이 열려 있으면 남의
비밀번호를 바꿀 수 있고, 그러면 `admin` 을 못 지우게 막아 봐야 그 비밀번호를 바꿔
들어가면 그만이라 자물쇠가 아무 것도 잠그지 않는다. 담당자가 비밀번호를 바꾸는 통로는
로그인 화면의 **초기화 요청**이다 (명세서 9장) — 최종 관리자가 계정 관리에서 넣어 준다.

**그 요청이 도착하는 자리가 계정 관리의 세 번째 탭이다** (2026-08-24). 제목 아래 탭이
「전체 | 삭제된 항목 n | **비밀번호 초기화 요청 n**」 셋이고, 좌측 메뉴의 [계정 관리]에도
대기 건수가 배지로 붙는다. 그 전에는 로그인 화면의 [요청 보내기]가 "접수했습니다"를 띄우고
**아무 데도 남기지 않았다** — 명세서가 적은 흐름의 앞쪽 절반만 화면에 있었던 셈이다.

요청 줄을 누르면 그 계정의 수정 창이 열리고, **새 비밀번호를 넣어 저장하면 요청이 자동으로
[처리 완료]가 된다** (비밀번호가 실제로 바뀌었을 때만이다 — 이름 한 글자를 고치려고 연 창이
요청을 닫으면 안 된다). 요청은 지우지 않고 상태로 닫으며, 잘못 눌렀으면 [대기로] 되돌린다.
저장은 다른 컬렉션과 같은 덮개다 (`data/passwordResets.js` · `data/store.js`) — 탭을 닫으면
함께 비워진다.

판정은 `can()` 한 함수에 있고(`data/account.js`), 부르는 쪽(내비 필터·라우터)은 v1.1
때와 같다. 다른 계정으로 `#/accounts` 를 쳐서 들어오면 대시보드로 되돌아간다. 실서비스의
권한은 어차피 서버가 막는다. 계정 표와 비밀번호가 소스에 있는 것도 그래서다 — 감출
가치가 있는 비밀이 아니라 검수용 열쇠이고, 로그인 화면이 그 사실을 적는다.

동작 규칙: 로그인 5회 실패 시 10분 잠금 ·
**세션에 시간 제한 없음** (탭을 닫을 때까지 유지. 2026-08-24 — 8시간이었다) ·
**최종 관리자(`admin`)는 중지·삭제 불가** ([삭제] 자리를 비우고 토글을 잠근다) ·
**그 밖의 계정도 쓸 수 있는 계정이 하나만 남으면 중지·삭제 불가** (아무도 못 들어오게 된다) ·
지금 로그인한 계정은 스스로 삭제할 수 없다.

## 실행

```bash
npm run serve      # 로컬 서버 — 기본 8000
npm run build      # 번들 2개 생성 (시민용 + 관리자)
```

→ http://localhost:8000/admin/

로컬 개발에는 빌드가 필요 없다 (번들이 없으면 로더로 떨어진다). 시민용과 같은 규칙이다 —
`?dev=1` 을 붙이면 번들이 있어도 원본을 읽는다.

**카카오 SDK 도메인을 새로 등록할 필요는 없다.** 등록 대상은 경로가 아니라 출처(origin)라
`/screens/main/` 과 `/admin/` 이 같은 도메인이다. 주소 검색(V-02)과 좌표 지정(V-01)이 지도를 쓴다.

### 검수 순서

| 확인할 것 | 방법 |
| --- | --- |
| 로그인 · 잠금 | 아무 비밀번호로 5회 틀리면 잠기고 남은 횟수가 화면에 뜬다 |
| 비밀번호 초기화 요청 | 로그인 화면 → [비밀번호를 잊으셨나요?] → 아이디 `yongin` 으로 보낸다. `admin` 으로 들어가면 좌측 [계정 관리]에 배지 ①이 붙고, 세 번째 탭에 그 요청이 있다. 줄을 누르면 계정 수정 창이 열리고 새 비밀번호를 넣어 저장하면 요청이 [처리 완료]로 바뀐다. 없는 아이디로 보내면 목록에 「등록되지 않은 아이디」 배지가 붙는다 |
| 마지막 계정 보호 | `#/accounts` 에서 하나뿐인 계정의 [사용]을 끄거나 삭제하려 하면 막힌다. 계정을 하나 더 만들면 풀린다 |
| 유형별 폼 | `#/facilities` → [시설 등록] → 유형을 화장실로 바꾸면 항목이 3개에서 11개로 갈린다 |
| 필수 · 조건부 검사 | 빈 채로 [저장] → 다이얼로그가 닫히지 않고 해당 칸에 오류가 뜬다. QR 지점에서 설치 상태를 「설치완료」로 바꾸면 설치일자가 필수가 된다 |
| 주소 검색 (V-02) | 폼의 [주소 검색] → "둔전시장" 으로 찾으면 실제 결과가 나오고, 고르면 좌표가 함께 들어와 지도가 뜬다 |
| 좌표 지정 (V-01) | 지도를 누르거나 핀을 끌면 좌표가 바뀐다. 용인시 밖으로 옮기면 경고가 뜬다 |
| 점포수 ⚙ | `#/stores` 에서 둔전 점포 하나의 [노출 여부] 토글을 끄고 `#/districts` 로 가면 점포수가 1 줄어 있다 |
| 조아용 겹침 (2-3) | `#/festivals` → 아무 축제나 → 다른 축제가 쓰는 그림에 「N건 사용 중」 |
| 근거 없이 완료 불가 (5-2) | `#/reports` → 미처리 건(rp-016 등) → 상태를 「처리완료」로 바꾸고 [저장] → 내부 메모를 요구하며 막힌다. **회신 내용이 받던 자리를 내부 메모가 이어받았다** |
| 3건 누적 강조 | `#/reports` 목록에서 둔전시장 공중화장실 세 줄이 붉게 선다 |
| 일괄 처리 | 목록에서 체크박스를 켜면 필터 줄 오른쪽에 [노출]·[숨김]이 선다 |
| 이탈 확인 | 폼에서 무언가 고친 뒤 [취소] → "저장하지 않고 닫을까요?" |
| 연쇄 삭제 (10장) | `#/districts` → 둔전 삭제 → 확인 창이 함께 갈 축제·QR 지점을 이름으로, 점포를 곳수로 적는다. 확인하면 `#/festivals` · `#/qr` · `#/stores` 에서 그것들이 함께 사라진다 |
| 연쇄 복구 (10장) | 이어서 `#/districts` → [삭제된 항목] 탭 → [되돌리기] → 넷이 함께 돌아오고 **둔전만 노출이 꺼져 있다.** 토스트가 "…[노출 여부]를 켜면 사용자 화면에 나타납니다" 를 적는다 |
| 따로 지운 것은 안 돌아온다 | 점포 하나를 먼저 지운 뒤 둔전을 지웠다 되돌리면, 그 점포는 `#/stores` 의 [삭제된 항목]에 그대로 남는다 |
| 잠긴 값 고치기 | `#/asof` 와 `#/settings` 는 **읽기 → [수정] → [제출] → 변경 내용 확인** 세 걸음이다. 확인 창에 바뀐 줄만 「무엇이 무엇으로」로 뜨고, 취소하면 아무 일도 없다 |
| 변경 이력 (10장) | 무언가 고친 뒤 `#/settings` 맨 아래 |
| 상점가별 둘러보기 매장 | `#/dashboard` 아래쪽. 상점가를 고르면 그곳의 「인기 매장 6곳 · 신규 매장 6곳」이 나온다 — **시민 화면 둘러보기 탭과 같은 여섯 곳**이다. 둔전 말고는 점포 자료가 없어 안내만 뜬다. `#/stores` 에서 1위 점포의 노출을 끄고 오면 목록에서 빠진다 |
| 처리 대기 배지 | 대시보드 맨 위. **「좌표 누락 점포」는 0건이라 뜨지 않는 것이 정상이다** — 명세서가 "0건이 아닐 때만 노출"이라고 정했다. 「미처리 오류신고」는 대시보드에서 뺐다 (2026-08-20) — 같은 숫자가 좌측 메뉴 [오류신고 관리] 옆에 늘 붙어 있어 한 화면에 두 번 적을 이유가 없다. 명세서 6장의 배지 목록과 이 한 줄이 다르다 |
| 세션 유지 | 무언가 고친 뒤 새로고침 → 남아 있다. 탭을 닫았다 열면 → 사라진다 |
| 좁은 화면 | 창을 1,024px 아래로 줄이면 안내 문구만 남는다 |
| 시민용 무영향 | `/screens/main/` 세 탭과 상세가 전과 같다 |

## 구조

```
admin/
├── admin_specifications.md  기준 문서 (기능명세서 v1.4)
├── index.html               부트스트랩 — screens/main/index.html 과 같은 규칙
├── AdminApp.jsx             로그인 게이트 + 없는 주소 되돌리기 + 셸 + 토스트
├── router.js                해시 라우터 (화면 목록은 data/account.js 의 ALL_PAGES 하나뿐)
├── pages/
│   ├── Login.jsx  Dashboard.jsx  Districts.jsx  Stores.jsx  Festivals.jsx
│   ├── Facilities.jsx  QrPoints.jsx  Reports.jsx  DataAsOf.jsx  Accounts.jsx
│   ├── ImageField.jsx       V-06 이미지 — **지금 쓰는 화면이 없다** (부스 배치도 칸을 뺐다)
│   ├── RecordForm.jsx       항목표 → 입력 칸
│   ├── EditorModal.jsx      등록·수정 다이얼로그 + 이탈 확인
│   ├── RemovedItems.jsx     「전체 | 삭제된 항목 n」 탭과 그 뷰의 열 (목록 다섯이 공유)
│   ├── useRecordEditor.js   등록·수정·삭제 상태 한 벌
│   └── useListState.js      목록 공통 규격 (검색 2자 · 20행 · 선택)
└── data/
    ├── fields.js       명세서 항목표 — 폼 열 개의 유일한 출처
    ├── store.js        덮개 저장소 + 변경 이력
    ├── account.js      계정 · 세션 · 잠금
    ├── settings.js     데이터 기준일 기본값 · API 한도(읽기 전용)
    ├── stats.js        대시보드 수치 (시드 난수 — 새로고침해도 같다)
    ├── reports.js      오류신고 16건
    └── assets.js       조아용 에셋 목록
```

디자인 시스템 쪽에 추가된 것은 `design-systems/components/admin/` 의
`AddressField` · `CoordField` · `Repeater` · `AssetPicker` 넷과,
기존 `FormGrid` · `DataTable` · `ConfirmDialog` · `AdminShell` · `Modal` 의 확장이다
(`design-systems/CHANGELOG.md` 참조).

## 아직 못 한 것

**「오류신고 회신 발송」이 이 목록에서 나갔다** (2026-08-24). 못 한 것이 아니라 **하지 않기로
한 것**이다 — 시민 쪽 신고 폼이 처음부터 연락처를 받지 않으므로(2026-08-18 결정) 회신처와
회신 내용 두 칸이 관리자 상세에서 함께 없어졌다 (명세서 5장). 닫을 때 근거를 남기라는
요구는 내부 메모가 이어받았다.

- **프로그램 일정 · 부스 위치** (2-4 · 2-5, `C`) — 용인시 자료 제공 범위 확정 후 반영 여부 결정.
  칸은 만들되 「조건부 · 자료 확보 시」 배지를 붙였다
- **데이터 일괄 적재 · 매칭 · 갱신 · 검수** — 관리자 범위 밖이다. 별도 문서
  (데이터 매칭 및 검수 기획)로 진행한다
