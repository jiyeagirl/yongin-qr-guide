# 관리자 웹 (`/admin/`)

기준 문서는 **`admin/admin_specifications.md` (관리자 웹 기능명세서 v1.15)** 이다.
화면은 **열여섯이고 번호가 M01 ~ M16 으로 이어진다.** 번호가 두 번 밀렸다 —
v1.5 가 없어진 환경 설정의 빈 번호를 메우려고 한 번, v1.14 가 골목 한바퀴 코스 둘을
넣고 데이터 갱신 현황 하나를 빼면서 한 번. **옛 문서의 번호는 그때의 것이다**
(명세서 1장의 대조표).

> **범위가 좁다.** 명세서 머리말이 그것을 못 박았다 —
> "개별 건의 조회·수정·신규 등록과 오류신고 기반 보정. **데이터 일괄 적재, 매칭, 갱신,
> 검수는 개발 쪽에서 처리하며 별도 문서(데이터 매칭 및 검수 기획)로 분리한다.**"
>
> 그래서 이 화면들이 하는 일은 한 가지다: **한 건씩 고치고, 한 건씩 새로 넣는다.**
> 무엇이 빠졌고 왜 빠졌는지는 아래 「범위 밖으로 나간 것」에 있다.

## 시민용과 왜 완전히 나누는가

**시민용 앱에서 들어갈 수 있는 입구를 두지 않는다.** QR 을 찍고 들어온 시민이 관리자
화면을 볼 이유가 없고, 로그인이 붙는 순간 그 링크는 실패하는 문으로만 남는다. 그래서
주소도 폴더도 나눈다 — `/screens/main/` 과 `/admin/` 은 서로를 가리키지 않는다.
(검수 입구인 저장소 루트 `index.html` 에만 링크가 있다.)

**디자인 시스템 진입점도 나눈다.** `design-systems/index.js` 는 시민용, `admin.js` 는
관리자용이다. 관리자 컴포넌트를 `index.js` 에 합치면 재수출을 타고 시민용 모바일 번들에
데이터 표와 다이얼로그가 들어간다 — 길에서 QR 을 찍는 사람이 내려받을 이유가 없는 것들이다.
합치지 않은 결과, 관리자 화면을 열 개 만든 뒤에도 시민용 번들은 **95모듈 751KB 그대로**다.

## 화면

**대상을 부르는 이름은 「골목형 상점가」다** (2026-08-24) — 메뉴 · 제목 · 단추 · 다이얼로그 · 토스트 ·
항목명까지 줄이지 않고 그대로 적는다. 지정 제도의 이름이고 시민 화면도 그렇게 부른다.
예외 하나: 오류신고의 「대상 유형」 값(`상점가`)은 **시민 신고 폼과 함께 쓰는 저장값**이라
바꾸지 않았다 — 바꾸면 이미 접수된 건의 값과 갈린다.

| 화면 | 명세서 | 파일 | 경로 |
| --- | --- | --- | --- |
| 로그인 · 비밀번호 초기화 요청 | M01 | `pages/Login.jsx` | (로그인 전 전체화면) |
| 대시보드 | M02 | `pages/Dashboard.jsx` | `#/dashboard` |
| 골목형 상점가 목록 · 등록 · 수정 | M03 · M04 | `pages/Districts.jsx` | `#/districts` |
| 점포 목록 · **수정** · 엑셀 일괄 등록 | M05 · M06 | `pages/Stores.jsx` | `#/stores` |
| 축제 목록 · 등록 · 수정 | M07 · M08 | `pages/Festivals.jsx` | `#/festivals` |
| 골목 한바퀴 코스 목록 · 등록 · 수정 | M09 · M10 | `pages/Courses.jsx` | `#/courses` |
| 공공시설 목록 · **수정** · 엑셀 일괄 등록 | M11 · M12 | `pages/Facilities.jsx` | `#/facilities` |
| QR 지점 목록 · 등록·수정 | M13 · M14 | `pages/QrPoints.jsx` | `#/qr` |
| 오류신고 관리 | M15 | `pages/Reports.jsx` | `#/reports` |
| 계정 관리 · 비밀번호 초기화 요청 | M16 | `pages/Accounts.jsx` | `#/accounts` |

**[등록]이 일곱 자리로 늘었다 — 상점가는 폼, 점포·공공시설은 엑셀** (2026-09-04, 사용자
요청. 명세서 v1.28). **골목형 상점가(M03) · 점포(M05) · 공공시설(M11)** 의 머리 오른쪽에
[골목형 상점가 등록] · [점포 등록] · [공공시설 등록]이 다시 섰다. 아래 「[등록]이 남는
곳」의 근거 — *원천이 알려주는 자료라 이 화면에서 만들지 않는다* — 는 **그 일이 일어나지
않는다**가 아니라 **드물게 일어난다**였다: 길이 없으면 담당자는 개발 쪽에 요청하고
기다린다.

**단추마다 여는 것이 다른 기준은 「한 번에 몇 줄이 오는가」다.** 새 상점가는 **한 달에
두세 곳**이라 M03 은 **폼**을 연다(축제·코스와 같은 모양. 등록 창에는 점포수 두 칸이
없다 — 셀 점포가 없어 답이 늘 0 이다). 점포는 둔전 한 곳이 335줄, 공공시설은 네 유형을
합쳐 수백 줄이라 M05 · M11 은 **엑셀 한 장을 받는 창**을 연다
(`pages/BulkUploadModal.jsx` · `design-systems/components/admin/FileField.jsx`).
엑셀 창 셋으로 먼저 만들었다가 상점가 쪽을 같은 날 폼으로 되돌린 자리다. **[삭제]는
돌아오지 않았다** — 늘리는 일과 지우는 일은 여기서 갈린다. 엑셀 쪽은 지금 **파일을 받는
데까지**다: 읽고 넣는 일이 서버 쪽이라 [업로드]는 받았다는 사실만 토스트로 적는다
(양식 파일 · 열 규칙 · 실패 행 회신은 다음 판). **폼 등록은 그렇지 않다** — 목록에 줄이
서고 저장된다.

**엑셀 창은 파일보다 먼저 한 칸을 고르게 하고, 고르기 전에는 파일 칸이 잠긴다** (같은 날) —
점포는 **소속 골목형 상점가**, 공공시설은 **시설 유형**이다. 먼저 고른 값이 그 파일을
**어떻게 읽고 어디에 넣을지**를 정한다: 시설은 유형마다 항목표가 통째로 다르고(AED 2항목 ·
화장실 10항목) 점포의 소속은 파일 밖에서 정해진다. 잠그되 **왜인지는 그 칸이 적는다** —
파일 칸 안의 규칙 줄 자리에 「시설 유형을 먼저 선택해주세요.」가 대신 서고, [업로드]로
막히면 같은 줄이 붉어진다(고르개 아래에 같은 말을 한 번 더 세우지 않는다).

소속을 창에서 받는 근거는 점포 쪽에 하나 더 있다 — 파일로 받는 쪽에만 있는
물음이고
(**이 파일의 가게들이 어느 상점가 것인가**), **화면에는 그것을 스스로 알아낼 방법이 없다**:
소속 판정은 도로명주소를 상점가의 **구역 주소 목록**과 맞추는 일인데(시민 명세서 3-2 —
둔전 66개 주소 → 335곳) 그 목록은 앱 어디에도 없고, 상점가가 가진 주소는 「…번지 **일원**」
대표주소 한 줄이라 그것으로 하는 짐작은 **틀려도 화면 어디에도 표시가 나지 않는다.**
**자동 매칭은 그대로 서버 쪽이다.** 파일에 열로 받지 않는 이유는 구역 자료가 상점가별
xlsx 로 오고(한 파일 = 한 상점가), 파일 안의 이름은 **한 글자만 달라도 아무 데도 걸리지
않기** 때문이다. 고르지 않으면 그 칸에서 막히고, 기본값은 목록의 소속 필터다
(「전체」면 「— 선택 —」으로 비워 연다 — 미리 골라져 있으면 확인 없이 눌린다).

**지우는 자리는 여전히 없다** (2026-08-25 오후 → 2026-08-26, 사용자 요청) —
**[삭제]는 어느 화면에도 없다**
(2026-08-26 에 마지막 둘 — QR 지점 · 계정 — 이 닫혔다. 아래 「지우는 자리가 없다」).
정보 관리 다섯(상점가 · 점포 · 축제 · 코스 · 공공시설)의 목록에서는 「관리」 열이 통째로
없어졌다 — 안에 [삭제] 하나뿐이었다. 남는 것은 [노출 여부] 토글이다.
점포·공공시설은 공공데이터가, 골목형 상점가는 지정 제도가 정하는 목록이라 **한 곳이 늘고
주는 일이 이 화면에서 일어나지 않고**, 축제·코스는 담당자가 만들지만 지나간 것이 곧
기록이다. 지운 줄은 다음 갱신에서 되살아나거나 영영 사라져 담당자가 한 일이 남지 않지만,
내린 줄은 그 자리에 그대로 있다.

**데이터 갱신 현황 화면이 있었다** — `pages/DataAsOf.jsx` · `#/asof` (2026-08-25 삭제,
사용자 요청. 그때의 번호가 M14 였다). 카테고리별 기준일과 그것이 시민 화면 하단에 어떤
문장으로 나가는지를 **보여주기만** 하던 화면인데, 보고 나서 담당자가 화면에서 할 수 있는
일이 없었다 — 오래된 기준일을 발견하면 개발 쪽에 알리는 것이 전부다. 값은 그대로 돈다
(`config.js` 의 `STORE_AS_OF` · `FACILITY_AS_OF` 를 시민 화면 하단 고지가 읽는다).

**환경 설정 화면이 있었다** — `pages/Settings.jsx` · `#/settings` (2026-08-24 삭제.
그때의 번호가 M15 였고, 지금 그 번호는 계정 관리의 것이다).
두 구획이었고 둘 다 이 화면의 일이 아니었다.

- **서비스 운영 설정** (안내 범위 · 배너 기준 · 상점가 임계 거리 · 신규 매장 판정 기간 ·
  코스 반경 · 탭별 지도 확대 단계) → **개발 쪽.** 개별 건이 아니라 화면 전체가 도는
  방식을 정하는 값이다. 게다가 화면에 서 있는 내내 아무 데도 연결되지 않았다 —
  `config.js` 는 모듈 상수라 관리자가 밀어 넣을 수 없어서, 담당자가 반경을 고쳐도
  시민 화면은 그대로였다. 값 자체는 그 코드에 그대로 있다.
- **변경 이력** → 기록은 계속 쌓인다 (`data/store.js` 의 `readHistory`). 없어진 것은
  **보는 자리**이고, 다시 둔다면 대시보드 아래가 자연스럽다.

**목록과 등록·수정을 한 파일에 둔다.** 명세서는 M03/M04 처럼 둘로 나눠 세지만, 폼은
목록 위에 뜨는 다이얼로그다. 페이지를 나누면 335곳에서 조건을 좁혀놓고 한 곳 눌러본 뒤
돌아왔을 때 필터·쪽 번호·스크롤이 통째로 초기화된다 — 명세서 1장의 "상세 복귀 시
필터·스크롤 유지"를 지키는 가장 짧은 방법이 애초에 목록을 언마운트하지 않는 것이다.
(시민용 셸이 상세를 오버레이로 여는 것과 같은 판단이다.)

**공공시설 관리가 기준 화면이다.** 골격(머리 → 필터 줄 → 표 → 다이얼로그 폼 → 삭제 확인)을
거기서 정하고 나머지가 복제한다. 새 관리 화면을 만들 때 배치를 다시 정하지 않는다.
**목록을 좁히는 것은 전부 필터 줄의 고르개다** (2026-08-24) — 공공시설의 유형만 제목 아래
탭 줄이었는데, 탭이 나쁜 것이 아니라 **혼자 다른 것**이 값이라 다른 화면과 같은 자리
(검색창 왼쪽)로 내렸다.

### 검색은 **한 칸만** 본다 (2026-08-26, 사용자 요청)

| 화면 | 보는 칸 | 검색창 문구 |
| --- | --- | --- |
| 골목형 상점가 | 상점가명 | 골목형 상점가명 검색 |
| 점포 | 상호명 | 상호명 검색 |
| 축제 | 축제명 | 축제명 검색 |
| 골목 한바퀴 코스 | 코스명 | 코스명 검색 |
| 공공시설 | 명칭 | 명칭 검색 |
| QR 지점 | **QR ID · 지점명** | QR ID, 지점명 검색 |
| 오류신고 | 내용 | 내용 검색 |
| 계정 (두 탭 모두) | 아이디 | 아이디 검색 |

여덟 화면이 전부 서너 칸을 한 문자열로 이어 훑고 있었다. 그중 하나는 거의 언제나
**바로 왼쪽 고르개가 이미 가르는 축**이었고 — 점포의 업종, 상점가의 구, 축제·코스의
소속 상점가, 오류신고의 대상 유형 — 겹치는 데서 그치지 않고 **두 길의 답이 달랐다**:
업종 고르개는 대분류로 정확히 가르고 검색은 소분류 문자열을 부분 일치로 훑어서,
「카페」를 **고른** 결과와 「카페」를 **친** 결과가 서로 다른 목록이다.

겹치지 않는 칸을 담고 있던 자리도 문제는 같았다 — 공공시설이 운영시간까지 훑어
「09」에 아홉 시 개방 시설이 전부 나왔고, 축제·코스는 설명 본문까지 훑어 **이름과 한
글자도 겹치지 않는 줄**이 목록에 남았다(왜 걸렸는지 표에서 읽을 수 없는 행이다).

목록에서 검색을 쓰는 일은 「조건에 맞는 것들을 훑기」가 아니라 **「내가 아는 그 한 건을
열기」**다. 훑는 일은 고르개가 맡는다.

### 위치로 좁히는 고르개 — 공공시설 · QR 지점 (2026-08-26, 사용자 요청)

둘 다 원천에 있는 것이 도로명주소 한 줄뿐이라 `gu` 를 항목으로 갖지 않는데, 그래서
목록을 지역으로 좁힐 자리가 없었다. 지금 더미가 열몇 건이라 티가 나지 않을 뿐,
공공데이터가 들어오면 AED 하나만으로 시 전역에서 수백 건이고 그때 남는 좁히기 수단은
이름 검색뿐이다 — **찾는 이름을 이미 알 때만 쓸 수 있는 수단**이다.

- 값은 **저장하지 않고 주소에서 읽는다** (`screens/main/data/districts.js` 의 `guOf`).
  구를 항목으로 받아 두면 주소를 고쳤을 때 두 값이 갈린다 — AED·대피소의 명칭을
  데이터가 아니라 규칙으로 둔 것과 같은 이야기다
- 고르개에는 `GU_ORDER` 셋이 늘 선다. 자료에 있는 구만 세우면 화면마다 길이가 달라져
  「우리 구가 없어졌다」로 읽힌다 (더미는 전부 처인구라 나머지 둘은 0건이다)
- QR 지점에서는 **설치 상태 고르개가 그 자리를 내줬다.** 그 값은 좁히는 축이 아니라
  읽는 값이다 — 표에 배지로 서 있고, 손이 가야 하는 조합(설치완료 + 비활성)은
  `rowTone` 이 줄째로 세운다. 「철거된 것만」을 골라 훑는 일은 없다

## 범위 밖으로 나간 것 (2026-08-20 명세서 개정)

명세서가 관리자의 일을 **한 건 단위**로 좁히면서 다섯 가지가 빠졌다. 하나씩 이유가 다르고,
그 이유가 전부 같은 곳을 가리킨다 — **화면에서 결과를 되돌릴 수 없는 일은 화면의 일이 아니다.**

| 빠진 것 | 어디 있었나 | 왜 빠졌나 |
| --- | --- | --- |
| **매칭 검수 큐** | 화면 하나 (구 M07) | 실패를 손보는 일이 곧 매칭을 다시 돌리는 일이다 |
| **공공데이터 동기화** | 화면 하나 (구 M18) | 원본 조회·비교·반영 전부 서버 일이다 |
| **점포 일괄 업로드** | M05 의 다이얼로그 | 5,000행을 한 번에 넣고 화면에서 되돌릴 방법이 없다 → **2026-09-04 에 파일을 받는 데까지만 돌아왔다** (M11 공공시설도 같은 창을 쓴다. 위 「[등록]이 일곱 자리로 늘었다」). 읽고 넣고 실패한 행을 돌려주는 일은 그대로 서버 쪽이라, 화면이 되돌릴 수 없는 일을 저지르지는 않는다 |
| **구역 주소 목록 · 재매칭** | M04 의 1:N 편집기 | 주소를 손보는 일이 곧 매칭을 다시 돌리는 일이다 |
| **게시값 대비 산출값 비교** | 상점가 목록의 경고 | 명세서 2-1: "340 대 335처럼 차이를 알아도 **어느 5곳인지 모르면 고칠 수 없기 때문**" |

함께 사라진 작은 것들:

- **`sort_order` 노출 순서** (▲▼ 로 옮기던 것) — 항목표에서 빠졌다. 32곳의 차례는
  시민 화면이 거리로 정한다
- **공공시설 `source`** (원본 · 원본수정 · 직접등록) — 동기화가 어느 필드를 덮어쓸지
  정하는 값이었다. 읽는 곳이 없어졌다
- **「매칭 검수 대기」 처리 대기 배지** — 누를 곳이 없는 배지는 숫자만 있고 할 일이 없다
- **`ref_store_count` · `ref_onnuri_count`** (홈페이지 게시값을 따로 받던 칸) — 점포수는
  ⚙ 산출값 둘로 합쳐졌다. 2026-08-25 에 그 둘이 잠깐 열렸다가 같은 날 다시 닫혔다
  (아래 「점포수는 점포 정보에서 나온다」) — 손으로 넣는 자리는 지금 어디에도 없다
- **권한 구분 자체** (`CITY` · `DEVELOPER`) — 이 화면을 쓰는 사람이 용인시 담당자뿐이라
  업무에 나눌 등급이 없다 (2026-08-20). 계정 항목표에서 `role` 이 빠졌다.
  2026-08-24 에 계정 관리 하나가 최종 관리자(`admin`)에게만 열렸지만 `role` 은 돌아오지
  않았다 — 판정을 아이디로 한다 (아래 「계정」)
- ~~**QR 식별자 자동생성**~~ — 「화면이 지어낸 코드는 안내판에 인쇄된 코드와 맞을 이유가
  없다」였다 (2026-08-20). 같은 줄에 있던 **[QR 지점 등록]은 2026-08-25 에 돌아왔고**
  (수정은 2026-08-24), **자동생성 조항은 2026-08-26 에 뒤집혔다** — 생성은 있고 다만 그
  일을 이 화면이 하지 않는다. ID 와 이미지 50개를 미리 만들어 두고 등록에서는 그중 하나를
  고른다 (아래 「QR ID 는 적는 것이 아니라 고르는 것이다」)
- **API 쿼터 설정 구획** — 한도·임계치는 카카오 계약과 서버가 정하는 값이라 화면에서 손댈
  자리가 아니다. 오늘 사용량을 보여주던 대시보드 막대도 **2026-08-25 오후에 나갔다** —
  보고 나서 이 화면에서 할 수 있는 일이 없다 (같은 날 「지점별 스캔 횟수」도 함께)
- **「조회수 상위 점포 10 · 조회수 상위 공공시설 10」 순위표** — 시민 화면에 그런 목록이
  없다 (2026-08-20). 그 자리는 상점가를 골라 **둘러보기 탭의 인기 매장을 그대로 보는**
  구획이 대신한다. 담당자가 보는 여섯 곳이 곧 시민이 보는 여섯 곳이다
  (나란히 서던 「신규 점포」 표는 2026-08-25 오후에 뺐다)

지운 파일: `pages/MatchQueue.jsx` · `pages/Sync.jsx` · `pages/BulkUpload.jsx` ·
`data/queue.js` · `data/bulkUpload.js` · `data/zones.js` · `data/districtRef.js`.
관리자 번들이 **120모듈 1,022KB → 113모듈 935KB** 로 줄었다.

## 입력 항목은 명세서가 정한다

`data/fields.js` 가 **명세서의 항목표를 그대로 들고 있고**, 폼 열이 전부 거기서 나온다.
화면이 항목을 하나 더 넣고 싶어도 넣을 자리가 없다 — 그게 이 구조의 요점이다.

폼이 지키는 것 여섯:

1. **명세서에 있는 항목만.** 없는 칸을 만들어 두면 담당자는 채워야 하는 것으로 읽고,
   채운 값의 출처는 아무도 모른다
2. **필수 열 네 값이 화면에서 서로 다르게 보인다.** ● 필수 · ○ 선택 · ◐ 조건부 · ⚙ 자동을
   각각 다른 배지로 적는다. 하나로 뭉개면 담당자가 ⚙ 칸을 비어 있는 필수 칸으로 읽고
   아무 값이나 채운다 — 그 순간 집계값이 수기값이 된다.
   **⚙ 는 곧 읽기 전용이다** (2026-08-25) — ⚙ 이면서 고칠 수 있던 예외가 없어졌다.
   업종 칩처럼 자동으로 채워지되 담당자가 정하는 값은 ● 이고, 자동으로 채워진다는 사실은
   배지가 아니라 칸 밑 한 줄이 적는다. 상점가의 점포수 둘은 같은 날 ● 을 거쳐 **다시
   ⚙ 로 돌아갔다** — 그 값의 출처는 점포 정보 하나다
3. **범위를 칸 아래에 그대로 적는다.** `2~40자` · `0~9999`. maxlength 로 막기만 하면
   담당자는 글자가 더 안 써지는 이유를 모른 채 지운다
4. **선택 항목을 비우면 시민 화면에 `-` 가 뜬다**고 폼 아래 한 줄로 적는다
5. **가부 항목은 Switch, 저장값은 불리언.** 표시(○ · ×)는 읽는 쪽이 정한다
6. **데이터 기준일 칸이 없다.** 카테고리 단위 값이고 자료를 적재하는 개발 쪽이 함께 갱신한다 (명세서 7장). 그것을 보여주던 화면도 2026-08-25 에 없어졌다

### 이름이 둘인 이유

명세서는 `address_road` · `store_count` 처럼 **DB 컬럼명**으로 항목을 부른다. 그런데
시민용 화면 열둘이 이미 `addr` · `stores` 를 읽고 있어, 이름을 바꾸면 잘 돌고 있는 화면
열둘을 뜯게 된다. 그래서 **키는 우리 것을 쓰고 명세서 이름을 `spec` 에 적어 둔다.**
서버 연동 때 이 한 표가 곧 매핑표가 된다.

## 명세서의 까다로운 대목을 어떻게 풀었나

| 명세서 | 어디서 | 어떻게 |
| --- | --- | --- |
| V-02 도로명주소 직접 타이핑 불가 | `AddressField.jsx` | 카카오 지도 SDK 의 `services`(Places · Geocoder)로 검색한다. **JavaScript 키로 도는 기능이라 서버 없이 실제로 동작한다** — 이 화면에서 유일하게 더미가 아닌 부분이다. 고른 주소가 좌표를 함께 들고 온다 |
| 입력 원칙 3 좌표 입력란 없음 | `CoordField.jsx` | 위경도 칸 대신 지도만 준다. 자동 변환된 자리가 틀렸으면 눌러서 옮기거나 핀을 끈다. V-01 범위 밖이면 막지 않고 경고한다 |
| 2-1 점포수 ⚙ | `pages/Districts.jsx` | 노출 상태인 점포를 **실제로 세어** 보여준다. 점포 관리에서 한 곳을 숨기면 이 숫자가 줄어드는 것으로 정의가 확인된다. 점포 자료가 없는 31곳은 세지 못하므로 상점가 레코드의 값을 그대로 보여준다 (「점포 미등록」 배지는 뺐다 — 31곳 전부에 붙어 아무것도 가려내지 못했다). 2026-08-25 에 잠깐 손으로 고칠 수 있었다가 같은 날 닫혔다 — 아래 「점포수는 점포 정보에서 나온다」 |
| 2-1 `onnuri_market_name` | (화면에 없음) | 온누리 원본의 `소속 시장명` 표기를 그대로 들고 있는 **매칭 키**다. 값은 남아 있지만 관리자 화면에서 고치지 않는다 (2026-08-20) — 고쳐도 그 결과(가맹 몇 건이 걸리는지)가 이 화면에서는 보이지 않고, 0건이 나왔을 때 실제로 손볼 곳은 매칭 설정이다 |
| 2-3 조아용 이미지 선택 | `AssetPicker.jsx` · `data/assets.js` | 업로드가 아니라 사전 등록 에셋 8종의 썸네일 그리드. **다른 축제가 쓰는 그림에 「N건 사용 중」이 붙는다** — 등록하는 사람은 자기 축제만 보고 있어 겹침을 알 방법이 없다 |
| 2-4 · 2-5 프로그램 · 부스 | `Repeater.jsx` | 1:N 편집기. 화면에서는 **그냥 선택 항목**이다 (2026-08-20) — 우선순위 `C` 는 우리 쪽 자료 사정이고 담당자가 하는 행동은 선택 항목과 같다. **부스는 글로 안내한다** — 좌표 · 배치도 이미지(V-06) · 번호 · 유형을 뺐다. 시민 화면이 그대로 인쇄하는 칸만 남는다 (2026-08-24): 두 목록 모두 「시작 일시 · 종료 일시 · 이름 · 위치」로 차례가 같고, **프로그램에만 「설명」이 아랫줄로 붙는다**(`row2`). 시각은 글자가 아니라 날짜·시각 고르개로 받고 달력이 축제 기간 밖을 막는다. 부스의 「규모」는 없앴다 — 무엇을 하는 곳인지는 부스명이 이미 말한다 |
| 5장 동일 대상 3건 강조 | (없어졌다) | `fc-004` 에 미처리 세 건을 넣어 두고 그 줄들을 붉게 세웠다 (2026-08-25 오후 삭제, 사용자 요청). **끌 수 없는 표시였다** — 붉은색은 「지금 손대라」인데 신고가 쌓인 것 자체는 담당자가 없앨 수 있는 상태가 아니고, 같은 이름이 세 줄 나란히 선 것으로 이미 보인다. 더미 세 건은 그대로 있다 |
| 8-1 두 거리 값 | `data/settings.js` | 안내 범위 · 배너 기준 1km 를 `facilities.js` 에서 가져왔다. **시민 화면이 이미 그렇게 돌고 있어** 숫자를 지어내지 않았다. 안내 범위는 2026-08-26 부터 **사용자가 지도에서 고른다** (`RADIUS_STEPS` 2·3·4km. 옛 `NEAR_LIMIT` 2km 가 그 첫 칸이다) |
| 10장 삭제가 남는 곳 | `pages/QrPoints.jsx` · `pages/Accounts.jsx` | **QR 지점과 계정 관리 둘뿐이다** (2026-08-25 오후). 정보 관리 다섯에서는 [삭제]와 「관리」 열이 함께 없어졌고 [노출 여부]만 남는다 — 위 「만들고 지우는 자리」 |
| 10장 삭제는 영구 | `data/store.js` · `ConfirmDialog` | 남은 두 화면에서 **되돌리는 자리를 두지 않는다** (2026-08-24 뒤집음. 그 전에는 "물리 삭제 없음 · 목록 다섯의 「삭제된 항목 n」 탭"이었다). 덮개의 `removed` 는 원본 배열을 지울 수 없어 남은 **비석**이고, 확인 창의 첫 문장이 되돌릴 수 없다는 사실을 적는다 |
| 10장 연쇄 삭제 | (없어졌다) | 상점가를 지우면 축제·QR 지점·점포가 함께 가던 자리다 (2026-08-24 신설 → 2026-08-25 오후 삭제). 상점가를 지우는 길이 없어지면서 그 앞에 세워 두던 확인 창도 함께 나갔다 |
| 10장 변경 이력 | `data/store.js` | 등록·수정·삭제·복구마다 대상 · 변경 필드 · 주체 · 일시를 쌓는다. **열람 화면은 없다** (환경 설정 화면과 함께 나갔다) |

## 데이터

읽기 원본은 **시민용과 같은 파일**이다. 복사본을 만들면 둘이 갈라져, 관리자에서 고친 값이
시민 화면에 없는 상태가 곧바로 만들어진다.

```
읽기  screens/main/data/{facilities,dunjeon,districts,qr}.js
      admin/data/sources.js                  ← 위 표에 빠진 기본값을 채운 것
쓰기  admin/data/store.js                    ← 원본 위에 덮개(overlay)
관리자 전용  admin/data/{fields,account,stats,reports,settings,assets}.js
```

**화면은 `sources.js` 를 읽는다** (2026-08-25). 시민 화면의 표는 시민 화면이 쓰는 값만
갖고 있어서, 「노출 여부」가 아무 줄에도 없고 점포의 「소속 골목형 상점가」도 없다. 전에는
목록 화면들이 그 빈자리를 **읽는 자리마다** 메웠다(`visible !== false` · `districtId || 둔전`).
목록은 그래서 맞게 보였는데 **폼은 아니었다** — 폼은 값을 그대로 읽으므로 스위치가 전부
꺼진 채로 서고, 필수인 소속 상점가가 빈 칸이라 손대지도 않은 항목이 저장을 막았다.
메우는 자리를 한 곳으로 옮겨 목록과 폼이 같은 줄을 읽는다. 원천 배열은 손대지 않는다 —
시민 화면이 그것을 읽고 있다.

`store.js` 의 덮개가 갖는 것은 다섯이다 — `edits`(바뀐 필드만) · `added`(통째 행) ·
`removed`(id) · `links`(연쇄 삭제로 함께 지운 id) · `history`(변경 이력). 읽을 때 원본에 겹쳐 목록을 만든다. 실연동 때는 이
파일이 통째로 fetch/POST 로 바뀌고 화면 코드는 `useCollection` 의 모양만 같으면 손대지 않는다.

> **여기서 고친 것은 시민용 화면에 반영되지 않는다.** 서버가 없어 둘이 자료를 주고받을 길이
> 없고, 덮개는 `sessionStorage` 에 있는데 그 저장소는 **탭 단위**라 시민용 화면을 다른 탭에서
> 열면 애초에 닿지 않는다. 화면 상단의 노란 띠가 이 사실을 늘 적어 둔다.

상단바에 있던 **[데모 데이터 초기화]** 는 뺐다 (2026-08-20). 모든 화면 맨 위, 늘 보이는
자리에 **고친 것을 통째로 되돌리는 버튼**이 서 있을 이유가 없다. 처음 상태로 돌리려면 탭을
닫았다 열면 된다 — 덮개가 `sessionStorage` 라 그것으로 끝난다. 되돌리는 함수 자체는
`store.js` 의 `resetAll` 로 남아 있어 필요하면 다시 어디든 붙일 수 있다.

### 지우는 자리가 없다 (2026-08-26, 사용자 요청)

**[삭제]가 마지막 둘 — QR 지점 · 계정 — 에서도 없어졌다.** 정보 관리 다섯이 2026-08-25
오후에 간 길을 이틀 만에 따라갔다. 두 화면 다 삭제 확인 창의 각주가 **대안을 권하고**
있었는데(QR 지점은 「[철거] + 비활성」, 계정은 「[사용 여부] 해제」), **각주는 [삭제]를
누르고 들어온 사람에게 하는 말**이라 읽든 안 읽든 그 다음에 일어나는 일은 영구 삭제였다.
권하는 것이 「끄기」라면 화면에 끄는 스위치만 두는 편이 그 권고를 실제로 지킨다.

2026-08-25 가 두 화면을 예외로 둔 근거(「안내판을 철거하면 그 코드가 실제로 없어지고,
사람이 떠나면 계정은 지워야 한다」)는 **둘 다 반대였다** — 안내판을 철거해도 종이는
현장에 남아 그 코드를 찍는 사람이 있고(대시보드의 지점별 스캔), 계정의 아이디는
**변경 이력의 주체**라 지우면 지난 기록의 「누가」가 없는 사람을 가리킨다.

- 함께 없어진 것 — 두 화면의 「관리」 열과 삭제 확인 창, `useRecordEditor` 의 삭제
  기계(`onRemove` · `askRemove` · `confirmRemove` · `pending`). **부르는 화면이 하나도
  없는 기계를 남겨 두면 다음 사람이 「여기 있으니 써도 되는 것」으로 읽는다**
- `store.js` 의 `remove` 는 **남긴다** — 그쪽은 덮개가 무엇을 할 수 있는가이지 화면이
  무엇을 하느냐가 아니고, 실연동에서 서버가 갖는 능력이기도 하다
- `ConfirmDialog` 도 남는다 — **고치다 만 것을 버릴 때**(수정 중 이탈)가 그 쓰임이다

### 삭제는 영구였다 (2026-08-24, 사용자 요청. 아래는 그 자리가 있던 동안의 기록이다)

같은 날 앞선 판에서 **「전체 | 삭제된 항목 n」 탭**을 목록 화면 다섯(상점가 · 점포 · 축제 ·
공공시설 · 계정)에 세웠다가 **통째로 걷어냈다.** `pages/RemovedItems.jsx` 도 함께 지웠다.
지금 [삭제]는 확인 창에서 한 번 더 확인하면 그것으로 끝이고, 되돌리는 자리는 화면에 없다.

- **확인 창의 첫 문장이 그 사실이다** — 「삭제한 항목은 복구할 수 없습니다.」 마지막 줄이
  아니라 **맨 앞**인 것이 요점이다: 담당자는 이미 [삭제]를 누르고 들어왔고 상자를 끝까지
  읽지 않을 수도 있다. 순서는 **되돌릴 수 없다 → 더 나은 길**(폐업·폐쇄라면 [노출 여부]를
  끈다)이고, 그것이 "멈춰야 하나"를 판단하는 순서다 (`DELETE_NOTE`). 자기 문장을 쓰는
  화면 셋(축제 · QR 지점 · 계정)도 첫 줄은 같다.
  **두 문장이 된 것은 2026-08-25 다** (사용자 요청) — 가운데 있던 「사용자 화면과 현재
  목록에서 즉시 사라집니다」는 [삭제]를 누른 사람이 이미 아는 일이다
- **덮개에는 `removed` 배열이 남아 있다.** 원본 배열(`STORES` · `FACILITIES`)이 모듈 상수라
  거기서 행을 뺄 수가 없어서, 원본에서 온 행은 id 를 적어 두고 `merge` 에서 거른다.
  종전과 다른 점은 그 목록이 **비석이지 서랍이 아니라는 것**이다 — 읽는 화면도 되돌리는
  함수도 없다. 실연동에서 이 자리는 서버의 DELETE 다
- **새로 등록한 행은 진짜로 없어진다** — `added` 에서 빼내고 남은 `edits` 도 함께 지운다
- **저장 키를 `v2` → `v3` 으로 올렸다.** v2 덮개에는 되돌리기용으로 `added` 에 남겨 둔 채
  `removed` 에도 오른 행과 연쇄 삭제 기억(`links`)이 들어 있어서, 그 모양을 새 코드가
  읽으면 지운 줄이 목록에 되살아난다
- **잃는 것**: 잘못 지운 한 건을 되찾는 길이 없다. 남는 것은 변경 이력의 「삭제」 한 줄
  (무엇이 없어졌는지는 알 수 있으나 되살리지는 못한다)과 상단바의
  **[데모 데이터 초기화]**(그날 고친 것을 **전부** 버린다)뿐이다. 그래서 **지우기 전에
  보여주는 것**이 유일한 안전장치가 되고, 아래 상점가 연쇄 삭제의 목록이 그만큼 무거워진다

### 정보 관리 다섯에서 만들고 지우는 자리가 없어졌다 (2026-08-25 오후, 사용자 요청)

- **[등록]이 남는 곳** — 축제 · 골목 한바퀴 코스 · QR 지점 · 계정. 넷 다 **담당자가 만드는
  것**이다(코스는 짜는 것이고, QR 지점은 안내판을 붙이고 나면 어딘가에 반드시 등록되어야
  한다). 상점가 · 점포 · 공공시설에서는 [등록]이 없어졌다 — 원천이 알려주는 자료다
  (**셋 다 2026-09-04 에 돌아왔다** — 상점가는 폼, 점포·공공시설은 엑셀. 위 「[등록]이
  일곱 자리로 늘었다」)
- **[삭제]가 남던 곳** — QR 지점 · 계정. 정보 관리 다섯에서는 목록의 **「관리」 열이
  통째로** 없어졌다(안에 [삭제] 하나뿐이었다). **그 둘도 2026-08-26 에 닫혔다** —
  근거였던 「안내판을 철거하면 그 코드가 실제로 없어진다 · 사람이 떠나면 계정은 지워야
  한다」가 둘 다 반대였다 (위 절)
- **[노출 여부]가 그 자리를 대신한다.** 지운 줄은 다음 갱신에서 되살아나거나 영영 사라져
  담당자가 한 일이 남지 않지만, 내린 줄은 그 자리에 그대로 있다. 축제도 마찬가지다 —
  지난 축제는 기록이고, 내년에 같은 축제를 열 때 그 줄을 열어 날짜만 고치면 된다
- 함께 없어진 것 — 다섯 화면의 삭제 확인 창, 그 안의 각주(「종료된 축제는 …」 ·
  「코스만 없어지고 점포는 그대로 …」), 그리고 **상점가 연쇄 삭제**(아래 절)

### 상점가는 연결된 것과 함께 갔다 (2026-08-24 → 2026-08-25 오후 삭제)

**지금은 상점가를 지우는 길 자체가 없다** (위 절). 아래는 그 자리가 있던 동안의 기록이다.

명세서 10장의 "진행 중 축제 또는 활성 QR이 연결된 상점가는 삭제를 차단한다"를 **뒤집었었다.**
막다른 길이었기 때문이다 — 상점가 하나를 정리하려면 축제 화면에서 축제를 지우고, QR
화면에서 지점을 지우고, 다시 돌아와야 했다. 세 화면을 오가는 동안 무엇을 지웠는지 스스로
기억해야 하고, 중간에 그만두면 **축제만 없어진 상태**가 남는다.

이제 **축제 · QR 지점 · 소속 점포가 함께 지워진다.** 확인 창이 함께 갈 것을 먼저 보여준다.
한 번의 결정이 되므로 막을 이유가 없다.

**되돌리기는 없어졌다** (2026-08-24, 위 절). 이 길을 열 때의 근거 절반이 "그 결정을 통째로
되돌릴 수 있다"였는데 그 절반이 사라졌으므로, 남은 안전장치는 **지우기 전에 보여주는
목록 하나**다. 그래서 이 화면의 확인 창은 다른 화면보다 할 일이 많다.

- **점포도 함께 간다** (2026-08-24 같은 날 고침). 처음에는 점포만 빼 두었는데
  ("335곳을 한 번에 지우는 일은 이 화면이 할 일이 아니다"), **그러면 남은 점포를 볼 수
  있는 자리가 없다.** 시민 화면에서 점포에 닿는 길은 상점가 탭 하나뿐이고, 관리자 점포
  목록에서는 없어진 상점가 이름이 필터에 남는다. 어디에도 뜨지 않는 자료를 "남겼다"고
  말하는 것은 기록으로도 틀리다 — 그것은 남은 것이 아니라 찾을 수 없게 된 것이다
- **확인 창이 셋을 다르게 적는다.** 축제와 QR 지점은 **이름으로**(「축제 · 둔전 골목축제」
  「QR 지점 · 둔전 시장 입구」), 점포는 **곳수로**(「점포 · 335곳」). 335개 상호를 늘어놓으면
  확인 창이 아니라 목록이 되고, 여기서 알아야 하는 것도 어느 가게인지가 아니라
  **이만큼이 함께 간다**는 크기다
- **QR 지점의 빠져나갈 길은 여기 적지 않는다** (2026-08-25, 사용자 요청). 2026-08-24 까지는
  QR 이 걸렸을 때 한 줄이 더 섰다 — 「안내판은 현장에 그대로 남습니다. 지점을 지우지
  않으려면 [QR 지점 관리]에서 소속 골목형 상점가를 먼저 옮겨 주세요.」 이 창이 하는 일은
  **함께 지워지는 것을 보여주는 것**이고, 위 목록이 QR 지점을 이름으로 이미 적는다.
  옮기는 절차까지 적으면 지울지 말지를 정하는 자리에서 다른 화면의 사용법을 읽게 된다 —
  안내판이 현장에 남는다는 사실은 **QR 지점 관리의 삭제 확인 창**이 그 일을 하는 자리에서
  적는다 (`DELETE_NOTE_QR`). 길 자체는 그대로 있다: 소속을 옮기거나 [지정 안 함]으로 두면
  그 지점은 남는다
- 컬렉션을 넘나드는 삭제라 `store.js` 의 `removeRows`(훅 밖 함수)를 쓴다.
  상점가 화면이 축제·QR·점포 컬렉션을 통째로 세울 이유가 없고, **여럿을 한 번에 넘기는
  것이 중요하다** — 점포 335건을 한 건씩 지우면 저장이 335번 돌고 변경 이력이 그것만으로
  가득 찬다 (`HISTORY_MAX` 200). 이력에는 「일괄 삭제 · 점포 · 둔전 골목형상점가 삭제」
  한 줄로 적는다
- **QR 지점에는 빠져나갈 길이 있다.** 축제는 상점가와 함께 없어지는 것이 맞지만 안내판은
  현장에 그대로 붙어 있다. 지우기 전에 [QR 지점 관리]에서 소속 상점가를 옮기거나
  [지정 안 함]으로 두면 지점이 남는다 (아래 「QR 지점의 소속 상점가」). 확인 창과 그 화면의
  안내 줄이 둘 다 이 방법을 적는다 — 그 화면에는 삭제 버튼이 없어서, 적지 않으면 어제
  있던 지점이 없어졌는데 화면 어디에도 이유가 없는 상태가 된다

### 점포수는 점포 정보에서 나온다 (2026-08-25 오후, 사용자 요청)

「점포수」와 「온누리 가맹 점포수」는 **⚙ 산출값이고 고치지 않는다.** 상점가에 걸린 점포 중
노출 상태인 건수가 점포수고, 그중 온누리 가맹이 온누리 가맹 점포수다.

이 칸은 하루 사이에 열렸다가 닫혔다 — ⚙ 읽기 전용 → ⚙ 이면서 열림(`editable`) → ● →
다시 ⚙. 열었던 근거는 **틀린 수를 고칠 자리가 없다**는 것이었다(점포 자료가 있는 상점가가
32곳 중 한 곳뿐이라 나머지 31곳의 수는 세어 나온 값이 아니다). 맞는 말이지만 그것은
**자료가 아직 다 안 들어왔다는 우리 쪽 사정**이지 이 칸의 성격이 아니다. 같은 사실을 두
곳에서 받으면 반드시 어긋난다 — 점포 목록에는 335줄이 보이는데 상점가 목록에는 340곳이라고
적혀 있고, 어느 쪽이 맞는지 화면이 답하지 못한다.

- 수정 창에는 **센 값이 ⚙ 로 서 있다.** 점포 관리에서 한 곳을 숨기면 이 수가 줄어든다 —
  세는 규칙이 화면에서 확인되는 자리가 그것이다
- 점포 자료가 없는 31곳은 셀 것이 없으므로 **상점가 레코드에 적힌 값**을 그대로 보여준다
- 함께 없어진 것 셋 — 목록의 **「수기」 배지**와 그 기억(`countManual`), 그리고 「점포수보다
  많을 수 없습니다」 검사(`extraValidate`). 마지막 것은 두 수를 **따로 받을 때만** 생기는
  어긋남이라, 세는 쪽에서는 셈 자체가 그것을 보장한다

### QR ID 는 적는 것이 아니라 고르는 것이다 (2026-08-26. 2026-08-25 회의 결정사항)

**QR ID 와 QR 이미지 50개를 먼저 만들어 두고**(`data/qrCodes.js` — 목데이터는 `qr-001` ~
`qr-050`), QR 지점 등록·수정은 그중 아직 아무 지점에도 매칭되지 않은 하나를 **골라서 잇는
일**이 됐다. 담당자가 코드를 적는 자리는 화면에 없다.

[QR 지점 등록]을 열 때(2026-08-25) 코드만은 담당자가 적게 두었다. 근거는 「화면이 지어낸
코드는 인쇄된 코드와 맞을 이유가 없다」였고 그 걱정은 맞았는데, **답이 반대였다** — 지어내지
않는 방법은 담당자에게 받는 것이 아니라 한 곳에서 미리 만들어 두고 그중에서 고르게 하는
것이다. 손으로 옮겨 적는 한 오타는 반드시 나고, **오타 난 코드는 아무 데도 걸리지 않는다**:
형식만 맞으면 저장되고(`dunjeon02` → `dunjoen02`), 중복 검사도 통과한다 — 어느 지점도 그
코드를 쓰지 않으니 겹치지 않는다. 안내판은 현장에 붙어 있는데 표에는 한 글자 다른 코드가
앉고, 그 안내판을 찍은 시민은 「등록되지 않은 QR 코드」를 본다. **아무도 그 사실을 모른다.**

- **형식 검사와 범위가 함께 없어졌다** (`V.qrCode` · `4~12자 · 영문 소문자+숫자`).
  느슨해진 것이 아니라 **검사할 것이 남지 않았다** — 오타 · 표기 흔들림 · 중복 셋 다 사람이
  치는 칸에만 있는 문제다. 중복 검사(`extraValidate`)만 남기는데, 그것은 이제 검사라기보다
  **빗장**이다: 걸린다면 담당자의 실수가 아니라 우리가 목록을 잘못 만들었다는 뜻이다
- **이미 걸린 ID 도 목록에 남는다** — 흐린 글씨로 서고 오른쪽에 「◯◯에 매칭됨」이 붙는다.
  빼 버리면 그 ID 가 인쇄된 안내판을 손에 든 담당자가 검색해 놓고 「없습니다」를 읽는데,
  **없는 것과 이미 쓰는 것은 할 일이 다르다.** 고를 수 있는 것을 앞에 세우는 것은 목록이
  열 줄만 보이기 때문이다 (`OptionPicker` 에 `disabled` · `note` 를 그때 열었다)
- **평소에 「몇 개 남았다」를 적지 않는다** — 읽고 나서 할 일이 없다. 0 이 되면 할 일이
  생기고(새 발번 요청) 그 일은 이 화면에서 할 수 없으므로, 그때만 등록 창 맨 위에 붉은
  상자가 서서 어디로 가야 하는지를 적는다
- **[발번] 단추를 두지 않는다** — 그 단추가 만드는 것은 ID 하나가 아니라 인쇄해서 현장에
  붙일 안내판이다. **QR 이미지도 이 화면에 없다**: 코드를 그림으로 인코딩해 인쇄용 파일을
  떨구는 일은 개발 쪽 일괄 작업이고, 여기가 하는 일은 그 결과물을 지점에 잇는 것이다
- 시민용 더미의 QR 지점 세 곳도 그 표의 값을 쓴다 (`qr-001` · `qr-002` · `qr-003`).
  지점의 성격을 코드에 적던 옛 이름(`dunjeon-2019-04` = 둔전, 2019년, 넷째)은 없어졌다 —
  **발번은 뜻을 담지 않는다.** 검수 주소도 함께 바뀌었다 (저장소 루트 `index.html`)

### QR 지점을 전부 고친다 (2026-08-24, 사용자 요청)

M14(그때 M12)는 오래 **현황만** 갱신했다 — 고칠 수 있는 것이 소속 상점가 · 설치 상태 · 설치일자 ·
사용 여부 · 설치 상세 위치 · 관리 메모 여섯뿐이었고, 지점을 가리키는 넷(QR ID · 지점명 ·
주소 · 좌표)은 창 위쪽에 읽기 전용으로 적기만 했다. 근거는
**안내판에 인쇄되어 현장에 붙은 값**이라는 것이었다.

**그 사실은 지금도 맞지만 잠그는 방식이 아니게 됐다.** 인쇄물이 잘못 나갔거나 지점명이
실제와 다르거나 좌표가 100m 틀린 경우에 담당자에게 남는 길이 없었다 — 그 좌표 하나에
시민이 보는 모든 거리가 매달려 있는데도. 이제 화면은 **무슨 일이 일어나는지를 적고
결정은 담당자가 한다**:

- **칸 이름은 「QR ID」다** (2026-08-26, 사용자 요청. 그전에는 「QR 식별자」였다) —
  「식별자」는 이 값이 무슨 일을 하는지를 설명하는 말이지 담당자가 부르는 이름이 아니다.
  안내판 제작 의뢰서에도 인쇄물에도 개발 쪽과 주고받는 말에도 이 값은 ID 라고 적힌다
- **그 칸은 2026-08-26 부터 고르는 칸이다** (위 「QR ID 는 적는 것이 아니라 고르는
  것이다」). 아래 두 줄은 적는 칸이던 동안의 기록이다.
- 그 칸에는 **안내 줄을 두지 않았다** (2026-08-25, 사용자 요청. 하루 전까지는
  「안내판에 인쇄된 코드입니다. 바꾸면 …」을 적었다) — 코드를 고치는 일은 드문데 그 줄은
  모든 열람에 서 있었다. 2026-08-26 오전에 고르는 칸이 되면서 이 칸이 **하는 일**을
  적는 줄을 한 번 더 세웠다가(「안내판에 인쇄된 QR ID 를 검색해 고릅니다 …」) **같은 날
  다시 걷어냈다** — 그것도 칸이 이미 하는 말이었다: 누르면 목록이 펼쳐지고, 못 고르는
  줄은 흐린 글씨로 서서 「◯◯에 매칭됨」을 스스로 적는다. **이 칸 아래에는 아무 줄도 없다**
- **그 「누르면」을 2026-08-27 에 실제로 지키게 했다** (사용자 요청) — 그전에는 창이 열리면서
  포커스가 이 칸에 닿는 것만으로 목록이 내려와, [등록]을 누른 담당자가 **처음 보는 화면이
  폼이 아니라 QR ID 오십 줄**이었다 (다이얼로그는 열릴 때 본문의 첫 칸으로 포커스를 보내고,
  QR 지점 폼의 첫 칸이 이 칸이다). 여는 길은 셋이다: 칸을 누르거나 · ↓ 나 Enter 를 치거나 ·
  글자를 치거나 (`OptionPicker`, 코스의 「들르는 점포」 칸도 같다)
- **QR ID 는 전역 유일**이라 저장 전에 겹치는지 검사한다 (`extraValidate`) — 겹치면 시민이
  그 코드를 찍었을 때 어느 지점으로 갈지가 표의 차례로 정해진다
- 주소를 바꾸면 좌표가 따라온다 (점포·공공시설과 같은 `onAddress` 한 줄)
- **[사용 여부]가 배지에서 토글이 됐다** (2026-08-26, 사용자 요청. 그전에는 「활성/비활성」
  배지였다) — 정보 관리 다섯의 [노출 여부]와 같은 모양·같은 자리다. 배지는 읽는 값의
  모양인데 이 칸은 담당자가 켜고 끄는 값이라, 바꾸려면 줄을 열어 폼까지 들어가야 했다.
  **설치 상태는 그대로 배지다** — 그쪽은 실제로 현장의 사실이다
- 2026-08-24 에 함께 생겼던 표의 [삭제] 열과 확인 창은 **2026-08-26 에 없어졌다** (위
  「지우는 자리가 없다」). 아래는 그 자리가 있던 동안의 기록이다.
- 확인 창의 각주가 **이 화면만의 대안**을 적었다 (두 문장.
  2026-08-25, 사용자 요청): 「삭제한 항목은 복구할 수 없습니다. QR 코드를 교체했다면
  삭제 대신 설치 상태를 [철거]로 두고 [활성 여부]를 비활성으로 돌려주세요.」
  가운데 있던 「안내판은 현장에 그대로 남아 있어 …」는 **왜 안 되는지를 설명하는 문장**
  이라 뺐다 — [삭제]를 누르고 들어온 사람은 이 상자를 끝까지 읽지 않을 수 있다.
  **「지우지 말고 끄라」는 원칙은 그대로이고, 지키게 하는 방법만 바뀌었다** —
  버튼을 없애는 것에서 결정하는 자리에서 말하는 것으로

**[QR 지점 등록]도 열었다** (2026-08-25, 사용자 요청). 하루 전까지는 「붙어 있는 지점을 고치는
것과 없던 코드를 지어내는 것은 다른 일」이라며 남겨 두었던 자리다. 그 말은 맞지만 결론이
틀렸다 — **안내판이 현장에 서고 나면 그 코드는 어딘가에 반드시 등록되어야 한다.** 화면에
자리가 없으면 그 일은 없어지지 않고 개발 쪽 요청으로 넘어가며, 담당자는 안내판을 붙여 놓고
기다린다. 푸는 방식은 위와 같다:

- **등록 창에 따로 안내를 세우지 않는다.** 여는 순간 넉 줄짜리 안내 상자(「안내판은 이
  화면에서 만들어지지 않습니다 …」)와 식별자 칸의 등록 전용 안내 줄(`NEW_CODE_HINT`)을
  세웠다가 **같은 날 걷어냈다** (사용자 요청). 거기 적힌 것이 전부 화면에 이미 서 있다 —
  QR ID 칸은 누르면 고를 수 있는 것이 목록으로 펼쳐지고, 기본값은
  창이 열릴 때 설치 상태 고르개와 꺼진 스위치가 보여주며, 설치를 마치고도 켜지 않은 채 두면
  수정 창의 경고 상자가 그 사실과 할 일을 적는다
- ~~**자동생성은 그대로 두지 않는다.**~~ 「등록 창에서도 코드는 담당자가 적는다」였는데
  **2026-08-26 에 뒤집혔다** (위 「QR ID 는 적는 것이 아니라 고르는 것이다」)
- **소속 없음 안내 상자는 등록 창에 서지 않는다** — 아직 아무것도 고르지 않은 것이 당연하다
- 덮개의 열쇠가 여기서만 갈린다: 원본 표의 행은 `id === code` 지만 화면에서 등록한 행의
  id 는 덮개가 매기는 `qr-new-001` 이다. 화면과 시민 화면이 보는 값은 언제나 `code` 다

**소속 상점가는 2026-08-24 앞선 판에 먼저 나와 있었다.** 어디에도 인쇄되지 않는 값이고,
위의 연쇄 삭제 때문에 바뀌어야 하는 자리가 먼저 생겼기 때문이다. 고르개에는 **지금 살아
있는 상점가만** 오른다 (`readCollection("districts", …)`) — 지워진 상점가가 남아 있으면
지점을 옮겨 살려 두려던 담당자가 방금 지운 상점가를 다시 고르게 되고, 그 지점은 그대로
다시 사라진다.

### M12 수정 창의 설명 세 줄을 정리했다 (2026-08-24)

| 어디 | 무엇 |
| --- | --- |
| 읽기 전용 목록 아래 | *"위 네 값은 안내판에 인쇄되어 … 아래 항목만 고칠 수 있습니다"* — **삭제** (그 목록 자체도 곧 없어졌다. 위 절) |
| 활성 여부 | *"끄면 이 코드로 들어온 사용자에게 교체 안내가 뜹니다. 기본값은 꺼짐입니다"* — **삭제** |
| 설치일자 | *"설치완료를 고르면 필수가 됩니다"* → **"설치완료를 선택하면 필수 항목으로 설정됩니다"** |

**첫 줄은 화면이 이미 하고 있는 말이다.** 위 넷은 입력칸이 아니라 글자로 적혀 있고 아래는
입력칸이라, 어느 쪽을 고칠 수 있는지는 눌러 보기 전에 생김새로 갈린다. 고칠 수 없는
이유(인쇄물)는 담당자가 이 창에서 할 일을 바꾸지 않는다.

**둘째 줄은 틀린 말이 됐다.** 시민 화면이 훼손·철거(S11)와 준비 중(S11-A)을 가르면서,
꺼져 있을 때 뜨는 것이 **설치 상태에 따라 갈린다** — 설치완료인데 꺼져 있으면 「교체 안내」가
아니라 「아직 준비 중」이다. 그 조합은 이 창의 경고 상자가 맡는다 (`QrPoints.jsx`).
그 상자는 **사용자 화면 문구를 인용하지 않는다** (2026-08-25, 사용자 요청) — 「설치는
완료되었으나 현재 비활성 상태입니다. 운영을 시작하려면 [활성]을 켜 주세요.」 두 문장이다.
인용은 저쪽이 바뀔 때마다 여기도 고쳐야 하는 줄이었고(S11-A 가 생기면서 실제로 한 번
고쳤다), 담당자가 지금 할 일과는 상관이 없다 — 켜면 그 안내는 사라진다.
뒷 문장(기본값)은 새로 만드는 자리의 이야기인데, **이 화면은 지점을 만들지 않는다.**

셋째 줄에서 「설치완료」를 붙여 쓰는 것은 그것이 **바로 위 칸에서 고르는 선택지의 이름**이기
때문이다. 띄어 적으면 목록에 없는 값을 가리키는 줄이 된다.

## 계정 (2026-08-24 개정)

| 계정 | 볼 수 있는 화면 |
| --- | --- |
| `admin` / `admin1234!` — 최종 관리자 | 10개 전부 (**계정 관리 포함**) |
| `yongin` / `yongin1234` — 담당자 | 9개 (계정 관리 없음) |

**업무 화면에는 등급이 없다.** `CITY`(시청 담당자)와 `DEVELOPER`(개발자)를 없앤 v1.1 은
그대로다 — 시설·점포·축제·QR·신고를 고치는 일에 등급을 매길 이유는 지금도 없고, 아홉
화면은 두 계정이 똑같이 쓴다.

**갈리는 자리는 계정 관리(M16) 하나뿐이고, 이유는 업무가 아니라 자물쇠다.** 모든 계정이
계정을 지울 수 있으면 서로를 지울 수 있고, 마지막 하나를 쓰던 사람이 인사이동으로 떠나면
**아무도 못 들어온다.** 「마지막 계정 보호」는 계정이 0이 되는 것만 막을 뿐 *누가* 남는지는
정하지 못한다. 그래서 **지워지지 않는 계정 하나**(`admin`)를 두고 계정 관리를 그 계정에만
연다 — 담당자가 바뀌어도 돌아올 자리가 늘 남는다.

[삭제] 버튼만 감추는 것이 아니라 **화면을 통째로** 가린다. 화면이 열려 있으면 남의
비밀번호를 바꿀 수 있고, 그러면 `admin` 을 못 지우게 막아 봐야 그 비밀번호를 바꿔
들어가면 그만이라 자물쇠가 아무 것도 잠그지 않는다. 담당자가 비밀번호를 바꾸는 통로는
로그인 화면의 **초기화 요청**이다 (명세서 9장) — 최종 관리자가 계정 관리에서 넣어 준다.

**그 요청이 도착하는 자리가 계정 관리의 두 번째 탭이다** (2026-08-24). 제목 아래 탭이
「전체 | **비밀번호 초기화 요청 n**」 이고, 좌측 메뉴의 [계정 관리]에도 대기 건수가
배지로 붙는다. (셋이었다 — 가운데 「삭제된 항목 n」이 같은 날 함께 빠졌다.) 그 전에는 로그인 화면의 [요청 보내기]가 "접수했습니다"를 띄우고
**아무 데도 남기지 않았다** — 명세서가 적은 흐름의 앞쪽 절반만 화면에 있었던 셈이다.

**그래서 `admin` 자신은 이 요청을 보낼 수 없다** (2026-08-25, 사용자 요청). 요청이 쌓이는
그 탭이 `admin` 에게만 열리므로, `admin` 이 보낸 요청을 받을 사람은 자기 자신이다 — 들어오지
못해서 보낸 요청이라 아무도 볼 수 없고, 화면은 「접수했습니다」라고 답한 뒤 아무 일도 하지
않는다. 판정은 `account.js` 의 `canRequestReset()` 이고, 「최종 관리자는 비활성화·삭제 불가」
(`isProtectedAccount`)와 나란히 둔다 — **같은 종류의 자물쇠**이고 셋이 흩어지면 하나가 빠진다.
잊어버린 최종 관리자 비밀번호를 되찾는 길은 화면 안에 없다(서버, 지금은 소스다).

요청 줄을 누르면 그 계정의 수정 창이 열리고, **새 비밀번호를 넣어 저장하면 요청이 자동으로
[처리 완료]가 된다** (비밀번호가 실제로 바뀌었을 때만이다 — 이름 한 글자를 고치려고 연 창이
요청을 닫으면 안 된다). 요청은 지우지 않고 상태로 닫는다.
저장은 다른 컬렉션과 같은 덮개다 (`data/passwordResets.js` · `data/store.js`) — 탭을 닫으면
함께 비워진다.

**닫는 길은 [저장] 하나다** (2026-08-25, 사용자 요청). 그 전에는 세 갈래였다 — 표의 「관리」
칸에 선 [비밀번호 재설정], 수정 창 안내 상자의 [비밀번호 변경 없이 처리 완료]와 [대기로
되돌리기]. 셋 다 걷어냈다: 표의 단추는 **줄을 누르는 것과 똑같은 일**을 했고(다른 목록 여섯에서
「관리」는 [삭제]라 이 목록에서만 뜻이 달랐다), 창 안의 둘은 **안내 상자 안에 든 결정**이라
상태에 따라 서 있는 것이 달랐다. 남은 예외 하나 — 계정이 없는 요청(오타·삭제된 계정)은 열
창이 없어 안내창이 [처리 완료로 표시]를 들고 간다. 그것마저 없으면 그 요청이 대기로 영원히
남아 배지의 숫자가 줄지 않는다. **되돌리는 길은 없다.**

판정은 `can()` 한 함수에 있고(`data/account.js`), 부르는 쪽(내비 필터·라우터)은 v1.1
때와 같다. 다른 계정으로 `#/accounts` 를 쳐서 들어오면 대시보드로 되돌아간다. 실서비스의
권한은 어차피 서버가 막는다. 계정 표와 비밀번호가 소스에 있는 것도 그래서다 — 감출
가치가 있는 비밀이 아니라 검수용 열쇠이고, 로그인 화면이 그 사실을 적는다.

동작 규칙: 로그인 5회 실패 시 10분 잠금 ·
**세션에 시간 제한 없음** (탭을 닫을 때까지 유지. 2026-08-24 — 8시간이었다) ·
**최종 관리자(`admin`)는 중지·삭제 불가** ([삭제] 자리를 비우고 토글을 잠근다) ·
**그 밖의 계정도 쓸 수 있는 계정이 하나만 남으면 중지·삭제 불가** (아무도 못 들어오게 된다) ·
지금 로그인한 계정은 스스로 삭제할 수 없다.

## 실행

```bash
npm run serve      # 로컬 서버 — 기본 8000
npm run build      # 번들 2개 생성 (시민용 + 관리자)
```

→ http://localhost:8000/admin/

로컬 개발에는 빌드가 필요 없다 (번들이 없으면 로더로 떨어진다). 시민용과 같은 규칙이다 —
`?dev=1` 을 붙이면 번들이 있어도 원본을 읽는다.

**카카오 SDK 도메인을 새로 등록할 필요는 없다.** 등록 대상은 경로가 아니라 출처(origin)라
`/screens/main/` 과 `/admin/` 이 같은 도메인이다. 주소 검색(V-02)과 좌표 지정(V-01)이 지도를 쓴다.

### 검수 순서

| 확인할 것 | 방법 |
| --- | --- |
| 로그인 · 잠금 | 아무 비밀번호로 5회 틀리면 잠기고 남은 횟수가 화면에 뜬다 |
| 비밀번호 초기화 요청 | 로그인 화면 → [비밀번호를 잊으셨나요?] → 아이디 `yongin` 으로 보낸다. `admin` 으로 들어가면 좌측 [계정 관리]에 배지 ①이 붙고, **두 번째 탭**에 그 요청이 있다(v1.7 에서 가운데 「삭제된 항목」 탭이 빠져 둘이다). 줄을 누르면 계정 수정 창이 열리고 새 비밀번호를 넣어 저장하면 요청이 [처리 완료]로 바뀐다 — **닫는 길은 그것 하나다**(관리 칸도, 창 안의 단추도 없다). 없는 아이디로 보내면 목록에 **「미등록」** 배지가 붙고, 그 줄을 누르면 안내창이 [처리 완료로 표시]를 들고 뜬다.<br>**아이디를 `admin` 으로 적으면 막힌다** — 「최종 관리자 계정은 이 요청으로 초기화할 수 없습니다.」가 아이디 칸에 뜬다 (한 줄이다) |
| 마지막 계정 보호 | `#/accounts` 에서 하나뿐인 계정의 [사용]을 끄거나 삭제하려 하면 막힌다. 계정을 하나 더 만들면 풀린다 |
| 유형별 폼 | `#/facilities` 에서 화장실 한 줄과 AED 한 줄을 차례로 열면 항목이 11개와 3개로 갈린다 (유형을 고르는 고르개는 [시설 등록]과 함께 없어졌다 — 유형은 그 줄이 이미 갖고 있다) |
| 필수 · 조건부 검사 | 빈 채로 [저장] → 다이얼로그가 닫히지 않고 해당 칸에 오류가 뜬다. QR 지점에서 설치 상태를 「설치완료」로 바꾸면 설치일자가 필수가 된다 |
| 주소 검색 (V-02) | 폼의 [주소 검색] → "둔전시장" 으로 찾으면 실제 결과가 나오고, 고르면 좌표가 함께 들어와 지도가 뜬다 |
| 좌표 지정 (V-01) | 지도를 누르거나 핀을 끌면 좌표가 바뀐다. 용인시 밖으로 옮기면 경고가 뜬다 |
| 온누리 두 칸 | `#/stores` → 아무 점포나 → **[온누리 상품권 종류]에서 「지류」를 켜면 위의 [온누리 가맹여부] 토글이 함께 켜진다.** 체크를 다 풀면 꺼지고, 토글을 직접 끄면 골라 둔 종류가 비워진다. 토글만 켜고 저장하면 **「온누리 상품권 종류를 선택해주세요.」** 로 막힌다. 목록의 온누리 열은 종류를 적는다(「지류·디지털」) |
| 점포수 ⚙ | `#/stores` 에서 둔전 점포 하나의 [노출 여부] 토글을 끄고 `#/districts` 로 가면 점포수가 1 줄어 있다 |
| 점포수 ⚙ | `#/districts` → 둔전 → [점포수]와 [온누리 가맹 점포수]가 **점선 테두리에 읽기 전용**으로 서 있다(335 곳 · 139 곳). `#/stores` 에서 한 곳을 숨기고 오면 그 수가 1 줄어 있다 |
| 조아용 겹침 (2-3) | `#/festivals` → 아무 축제나 → 다른 축제가 쓰는 그림에 「N건 사용 중」. 그 그림을 고르면 **「조아용 이미지 ●」 이름 줄 옆**에 「이미 다른 축제에서 사용 중인 이미지입니다 …」가 뜬다. **격자 아래에는 아무 줄도 없다** (2026-08-26. 「사전 등록된 조아용 이미지 내에서 선택가능합니다. 파일 업로드는 지원하지 않습니다.」가 서 있었다 — 앞 문장은 격자가 이미 답하고, 뒤 문장은 없는 단추에 대한 설명이었다) |
| 근거 없이 완료 불가 (5-2) | `#/reports` → 미처리 건(rp-016 등) → 상태를 「처리완료」로 바꾸고 [저장] → 내부 메모를 요구하며 막힌다. **회신 내용이 받던 자리를 내부 메모가 이어받았다** |
| 누적 강조가 없다 | `#/reports` 목록에서 둔전시장 공중화장실 세 줄(`rp-012`~`rp-014`)은 **다른 줄과 똑같이 서 있다** — 붉은 바탕도 「3건 누적」 배지도 없고, 그 줄을 열어도 붉은 안내 상자가 없다 (2026-08-25 오후 삭제). 같은 대상이 셋이라는 것은 대상 이름이 세 줄 나란히 선 것으로 읽는다 |
| 일괄 처리 | 목록 다섯(점포 · 공공시설 · 골목형 상점가 · 축제 · QR 지점)에서 체크박스를 켜면 필터 줄 오른쪽에 단추가 선다 — 앞 넷은 [노출]·[숨김], QR 지점은 [활성]·[비활성]이다. **일괄 삭제는 없다** (삭제는 영구다) |
| QR 지점 등록 | `#/qr` → 제목 오른쪽 [QR 지점 등록] → 빈 폼이 뜬다. 설치 상태는 **설치예정**, [사용 여부]는 **꺼짐**에서 시작하고, 좌표 자리에 「도로명주소를 선택하면 좌표가 자동으로 입력되며 …」가 적혀 있다. 저장하면 목록 끝에 새 줄이 생긴다 (탭을 닫으면 사라진다 — 서버가 없다) |
| QR ID 고르기 | 등록 창의 [QR ID] 칸을 누르면 사전 생성된 50개가 펼쳐진다 (2026-08-26). **칸 아래에는 아무 줄도 없다** — 무엇을 고르는 자리인지는 펼쳐지는 목록이 답한다. 위쪽은 고를 수 있는 ID, 아래로 내려가면 `qr-001` ~ `qr-003` 이 흐린 글씨로 서고 오른쪽에 「둔전 시장 입구 버스정류장에 매칭됨」처럼 어느 지점이 쓰는지가 적혀 있다 — **눌리지 않는다.** 「002」를 쳐도 그 줄이 나오되 고를 수 없고, 「099」처럼 없는 번호를 치면 「그런 QR ID 가 없습니다.」다. 아무것도 고르지 않고 저장하면 「사전 생성된 QR ID 중 하나를 선택해주세요.」<br>**목록이 아래 좌표 지도에 가리지 않는다** (2026-08-26 수정) — 지도가 z 100, 그 위 [핀 위치로 이동]이 z 400 인데 목록은 표에 없는 z 5 였다. 목록은 `--z-popover`(550)로, 지도 칸은 자기 층을 밖으로 흘리지 않게 `zIndex:0` 으로 고쳤다 |
| QR 지점 수정 | `#/qr` → 줄을 열면 **열 항목 전부**가 입력칸이다. [QR ID] 칸에서는 **지금 이 지점의 ID 가 고를 수 있는 줄로 서 있고**(자기 것은 걸린 것으로 세지 않는다) 다른 지점의 것은 눌리지 않는다. 주소를 다시 고르면 좌표가 따라온다. **[삭제]는 없다** (2026-08-26) |
| 사용 여부 토글 | `#/qr` 목록의 「사용 여부」 열을 표에서 바로 누른다 — 줄을 열 필요가 없다 (2026-08-26. 그전에는 「활성/비활성」 배지였다). `qr-003`(설치완료 + 꺼짐) 줄은 노란 바탕으로 서 있고, 그 줄의 토글을 켜면 색이 빠진다 |
| 검색은 한 칸만 (2026-08-26) | 어느 목록에서나 검색창 문구가 곧 보는 칸이다. `#/stores` 에서 「카페」를 **쳐도** 걸리는 것이 없고(상호명에 그 글자가 든 가게만 나온다), 업종으로 훑는 일은 왼쪽 [전체 업종] 고르개가 한다. `#/facilities` 에서 「09」를 쳐도 아홉 시 개방 시설이 나오지 않는다 |
| 소속 구 고르개 (2026-08-26) | `#/facilities` · `#/qr` 필터 줄 맨 왼쪽. 셋(처인구·기흥구·수지구)이 늘 서고 **더미는 전부 처인구**라 나머지 둘은 0건이다. 값은 저장된 항목이 아니라 도로명주소에서 읽는다 — 폼에서 주소를 다른 구로 바꾸고 목록으로 돌아오면 그 줄이 새 구로 걸린다 |
| 오류신고 「내용」 열 | `#/reports` 에서 브라우저 창을 넓히면 **잘리는 자리가 함께 넓어진다** (2026-08-26. 그 전에는 320px 상수라 넓혀도 같은 자리에서 끊겼다) |
| 폼이 저장된 값을 그대로 보여준다 | 목록 넷(점포 · 공공시설 · 골목형 상점가 · 축제)에서 아무 줄이나 열면 **[노출 여부] 스위치가 켜져 있다** (목록의 토글과 같은 상태다). 점포는 [소속 골목형 상점가]에 「둔전골목형상점가」가, [등록 일자]에 날짜가 들어 있어 **아무것도 고치지 않고 [저장]을 눌러도 막히지 않는다** |
| 1:N 줄 삭제 확인 | `#/festivals` → 아무 축제나 → [프로그램 일정]의 휴지통 → **그 줄 아래에 「〈프로그램명〉 을(를) 지울까요? [취소] [삭제]」가 편다.** 지울 줄이 위에 그대로 보인다. 상자를 겹치지 않는 것은 ESC 가 폼까지 닫기 때문이다 (`Repeater` 머리말) |
| 이탈 확인 | 폼에서 무언가 고친 뒤 [취소] → "저장하지 않고 닫을까요?" |
| 만들고 지우는 자리 | `#/districts` · `#/stores` · `#/facilities` 의 머리에 **[등록] 단추가 없고**, **일곱 목록 어디에도 「관리」 열이 없다.** [등록]은 `#/festivals` · `#/courses` · `#/qr` · `#/accounts` 넷에만 있고, **[삭제]는 어느 화면에도 없다** (2026-08-26). 줄을 내리는 것은 [노출 여부], QR 지점과 계정에서는 [사용 여부] 토글이다 |
| 지우는 자리가 없다 (10장) | 열여섯 화면 어디에도 [삭제]가 없다. 되돌릴 수 없는 일을 화면이 아예 제공하지 않는다 — 그날 고친 것을 전부 버리려면 상단바 [데모 데이터 초기화]이거나 탭을 닫았다 열면 된다 |
| 골목 한바퀴 코스 | `#/courses` → 아무 코스나 → [코스]의 손잡이(칸 오른쪽, 휴지통 앞)를 끌어 차례를 바꾼다 (↑/↓ 키로도 옮겨진다). 줄 맨 앞의 순번이 시민 화면의 ①②③④ 이고, 놓기 전에 그 번호가 미리 바뀐다. 점포는 상호명으로 **검색해서** 고른다 — 「김밥」처럼 가운데 토막으로도 걸린다. **코스는 넷으로 고정이다** — [코스 등록]을 눌러도 **빈 줄 넷이 이미 서 있고** [점포 추가]도 휴지통도 없다. 한 곳을 바꾸려면 그 줄에서 다시 고른다. 비운 채로 저장하면 「n번째 줄의 점포를 채워 주세요.」로 막히고, 같은 점포를 두 번 고르면 몇 번째 줄인지로 막는다. 상점가를 바꾸면 그 상점가에 없는 줄이 몇인지 경고 상자가 적는다 (지우지는 않는다) |
| 상점가별 둘러보기 매장 | `#/dashboard` 아래쪽. 고르개의 각 줄은 「**처인구 - 둔전골목형상점가** (점포 335곳)」이고(2026-08-26. 그전에는 가운뎃점이었다 — 구와 상점가는 나란한 두 값이 아니다), 고르면 그곳의 **「조회수 상위 점포 6곳」**이 나온다. 열은 **순위 · 상호명 · 상권업종대분류명 · 상권업종중분류명 · 도로명주소 · 조회수** 여섯이고 표와 카드가 **화면 폭을 다 쓴다** (2026-08-27. 2026-08-26 하루 동안은 열이 넷이고 표가 내용 너비였다 — 줄이고 나니 오른쪽 절반이 통째로 비어 「무언가 빠진 자리」로 읽혔고, 답은 자리를 없애는 것이 아니라 읽을 것을 채우는 것이었다) — **시민 화면 둘러보기 탭의 인기 매장과 같은 여섯 곳**이다 (옆에 나란히 서던 「신규 점포」 표는 2026-08-25 오후에 뺐다). 둔전 말고는 점포 자료가 없어 안내만 뜬다. `#/stores` 에서 1위 점포의 노출을 끄고 오면 목록에서 빠진다 |
| 요약 카드 넷 | `#/dashboard` 맨 위. 앞의 둘은 **「누적 QR 스캔」과 「최근 7일 QR 스캔」**이다 (2026-08-26. 둘째가 「최근 7일 스캔」이었다 — 기간만 다를 뿐 같은 수인데 이름이 갈려 있었다). 기간 고르개를 「오늘」로 옮기면 「오늘 QR 스캔」이 된다 |
| 대시보드에 없는 것 | `#/dashboard` 맨 아래까지 내려도 **「지점별 스캔 횟수」 표와 「카카오맵 API 일일 사용량」 막대가 없다** (2026-08-25 오후 삭제). 보고 나서 이 화면에서 할 수 있는 일이 없던 둘이다 — 남은 것은 요약 카드 넷 · 차트 둘 · 둘러보기 매장이다 |
| 처리 대기 배지 | 대시보드 맨 위. **남은 배지는 「좌표 누락 점포」 하나이고, 자료가 온전하면 0건이라 이 줄이 통째로 안 보이는 것이 정상이다** — 명세서가 "0건이 아닐 때만 노출"이라고 정했다. 「미처리 오류신고」는 2026-08-20 에 뺐고(같은 숫자가 좌측 메뉴 [오류신고 관리] 옆에 늘 붙어 있다), **「설치 미완료 QR」은 2026-08-24 에 뺐다** — 설치예정과 「설치완료 + 활성 꺼짐」은 밀린 일이 아니라 정상 상태이고, S11-A 검수용 미활성 지점(`qr-003`) 때문에 0 이 되지도 않아 「1」이 붙박이로 떠 있었다. 없어지지 않는 배지는 읽히지 않고, 그러면 좌표 누락이 진짜로 떴을 때 같이 묻힌다 |
| 세션 유지 | 무언가 고친 뒤 새로고침 → 남아 있다. 탭을 닫았다 열면 → 사라진다 |
| 좁은 화면 | 창을 1,024px 아래로 줄이면 안내 문구만 남는다 |
| 시민용 무영향 | `/screens/main/` 세 탭과 상세가 전과 같다 |

## 구조

```
admin/
├── admin_specifications.md  기준 문서 (기능명세서 v1.15)
├── index.html               부트스트랩 — screens/main/index.html 과 같은 규칙
├── AdminApp.jsx             로그인 게이트 + 없는 주소 되돌리기 + 셸 + 토스트
├── router.js                해시 라우터 (화면 목록은 data/account.js 의 ALL_PAGES 하나뿐)
├── pages/
│   ├── Login.jsx  Dashboard.jsx  Districts.jsx  Stores.jsx  Festivals.jsx
│   ├── Courses.jsx  Facilities.jsx  QrPoints.jsx  Reports.jsx  Accounts.jsx
│   ├── ImageField.jsx       V-06 이미지 — **지금 쓰는 화면이 없다** (부스 배치도 칸을 뺐다)
│   ├── RecordForm.jsx       항목표 → 입력 칸
│   ├── EditorModal.jsx      등록·수정 다이얼로그 + 이탈 확인
│   ├── useRecordEditor.js   등록·수정·삭제 상태 한 벌
│   └── useListState.js      목록 공통 규격 (검색 2자 · 20행 · 선택)
└── data/
    ├── fields.js       명세서 항목표 — 폼 열 개의 유일한 출처
    ├── sources.js      원천 표 + 빠진 기본값(노출 여부 · 소속 · 등록 일자 · QR 열쇠)
    ├── store.js        덮개 저장소 + 변경 이력
    ├── account.js      계정 · 세션 · 잠금
    ├── settings.js     데이터 기준일 기본값 · API 한도(읽기 전용)
    ├── stats.js        대시보드 수치 (시드 난수 — 새로고침해도 같다)
    ├── reports.js      오류신고 16건
    └── assets.js       조아용 에셋 목록
```

디자인 시스템 쪽에 추가된 것은 `design-systems/components/admin/` 의
`AddressField` · `CoordField` · `Repeater` · `AssetPicker` 넷과,
기존 `FormGrid` · `DataTable` · `ConfirmDialog` · `AdminShell` · `Modal` 의 확장이다
(`design-systems/CHANGELOG.md` 참조).

## 아직 못 한 것

**「오류신고 회신 발송」이 이 목록에서 나갔다** (2026-08-24). 못 한 것이 아니라 **하지 않기로
한 것**이다 — 시민 쪽 신고 폼이 처음부터 연락처를 받지 않으므로(2026-08-18 결정) 회신처와
회신 내용 두 칸이 관리자 상세에서 함께 없어졌다 (명세서 5장). 닫을 때 근거를 남기라는
요구는 내부 메모가 이어받았다.

- **프로그램 일정 · 부스 위치** (2-4 · 2-5, `C`) — 용인시 자료 제공 범위 확정 후 반영 여부 결정.
  칸은 만들되 「조건부 · 자료 확보 시」 배지를 붙였다
- **데이터 일괄 적재 · 매칭 · 갱신 · 검수** — 관리자 범위 밖이다. 별도 문서
  (데이터 매칭 및 검수 기획)로 진행한다
