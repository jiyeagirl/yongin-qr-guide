In-page tabs — underline for content, pill for filters.

```jsx
<SegmentedTabs items={[{id:"a",label:"진행중"},{id:"b",label:"마감"}]} value={v} onChange={setV} />
```

Underline variant spans the content width; pill variant is inline.
