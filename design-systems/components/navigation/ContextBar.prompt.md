The QR-anchor context bar — the user's "내 위치" is always the scanned sticker, not live GPS.

```jsx
<ContextBar place="둔전 시장 입구 버스정류장" onReset={recenter} />
```

Sits directly under the AppBar, above any map or list. Never omit it on a QR-entered screen.
