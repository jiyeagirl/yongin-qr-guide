조아용 artwork in one of 14 supplied poses.

```jsx
<Mascot pose="thumbsup" size={120} bob />
```

`base` prefixes the asset path when the page is not at the project root. Never recolour, rotate, crop or redraw the character.
