Circular icon-only tap target for app bars and cards.

```jsx
<IconButton name="notifications" label="알림" variant="ghost" />
```

Variants ghost / soft / outline / brand. Default 48px keeps the tap target legal.
