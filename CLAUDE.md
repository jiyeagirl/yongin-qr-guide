# 프로젝트: QR 기반 공공시설 및 골목형 상점가 위치 안내 스마트 서비스 구축 (모바일 웹)

## 참고 문서
- **시민용 기능명세서: `docs/functional_specifications.md` (v1.23, 2026-09-02 개정)** —
  v1.23 은 **팜플렛 코드의 앵커를 소속 상점가에서 읽는다** (사용자 요청). **팜플렛은
  종이라 붙어 있는 자리가 없다** — v1.22 가 S14 를 열면서 팜플렛 코드를 QR 지점 표에
  넣었는데 그 표가 도로명주소를 필수로 두고 있어 비울 수가 없었고, 그래서 **안내판과 같은
  주소**가 들어갔다(그 팜플렛이 그 기둥에 붙어 있다는 뜻이 되어 사실이 아니다). 팜플렛에
  언제나 정해져 있는 것은 **「어느 상점가의 축제 팜플렛인가」**이고, 그 상점가의 대표주소가
  앵커다(32곳 대표주소는 이미 `districts.js` 의 `구 + 소재지`다). **옮겨 적지 않고 읽는다**
  — 같은 사실을 두 곳에서 받으면 반드시 어긋나는데, 앵커는 **이 서비스가 적는 모든 거리의
  기준점**이라 어긋나면 목록·지도·도보 시간이 통째로 옛 자리 기준이 되고 **화면 어디에도
  그 표시가 나지 않는다.** **안내판은 반대다** — 그 기둥은 대표주소와 다른 자리에 서 있고
  그것이 사실이므로 자기 주소를 갖는다. 그래서 필수가 코드의 성격을 따라 갈린다:
  **안내판 = 주소 ● · 좌표 ● · 상점가 ○ / 팜플렛 = 주소 받지 않음 · 좌표는 상점가에서 ·
  상점가 ●** (관리자 항목표에 **「연계 축제」**가 함께 섰다. `isPamphlet` 이 가른다).
  **표기 성격도 애초에 다르다** — 상점가 주소는 「…124번길 2 **일원**」처럼 구역 주소이고
  QR 지점 주소는 V-02 로 검색해 고르는 순수 도로명주소라, 「일원」이 붙은 값은 검색 결과에
  없어 옮겨 적을 수도 없다. **화면은 아직 「연계 축제」 칸을 그리지 않는다** — 이번 판은
  항목표와 규칙까지이고, 그때까지 팜플렛 코드는 관리자 화면에서 만들 수 없다(이미 있는
  줄을 열어 고치는 것만 된다). 명세서가 화면보다 앞선 자리라 그 사실을 4장에 적어 두었다.
  v1.22 는 **축제 팜플렛을 찍고 들어온 사람에게 갈림길을 준다** (U-CM-20 · **S14**,
  사용자 요청). 안내판이 아니라 **축제 팜플렛에 인쇄된 QR** 로 들어오면 셸 위에 대화상자가
  한 장 덮인다 — 조아용 · 축제명 한 줄 · 기간 한 줄 · **[축제 정보보기]**(그 축제의 S09 로) ·
  **[주변 안내보기]**(닫고 공공시설 탭). 근거: **진입이 언제나 공공시설 탭인 것은
  안내판 앞에 선 사람을 위한 규칙(U-CM-03)인데, QR 이 붙는 자리가 안내판만은 아니다.**
  팜플렛을 든 사람이 찍은 이유는 「주변에 뭐가 있나」가 아니라 「이 축제 뭐지」라, 그에게
  공공시설 목록이 먼저 열리는 것은 시키지 않은 화면이다. **그렇다고 축제 상세로 곧장
  보내지도 않는다** — 그러면 이번엔 주변 안내가 있다는 사실 자체가 안 보인다(팜플렛 QR 을
  찍은 사람도 골목에 서 있고 화장실과 AED 는 그때 찾는다). 어느 쪽으로 자동으로 보내든
  한쪽을 잃으므로 **화면 하나를 건너뛰게 하는 대신 갈림길을 한 번 보여준다.**
  **문구는 축제 상태를 따라 셋이다** — 진행중 「{축제명}이 열리고 있어요!」 · 예정
  「{축제명}이 열릴 예정이에요」 · 종료 「{축제명}은 지난 축제예요」. **팜플렛은 종이라서
  축제보다 오래 남는다**: 늘 「열리고 있어요」라고 적으면 [축제 정보보기]로 들어간 상세에
  「종료」 배지가 서 있어 **두 번 눌러 스스로 뒤집히는 화면**이 된다. 이름은 화면 제목용
  짧은 이름이 아니라 **실제 축제명**이다(이 줄이 하는 일이 손에 든 팜플렛과 맞춰 보는
  것이다. 조사는 `josa` 가 붙인다). **단추 차례는 세 상태가 모두 같고**([×] 도 막 누르기도
  없다 — 단추 둘이 이미 나가는 길이다), **봤다는 사실을 적어두지 않는다** — 코치마크가
  묻는 것은 「이 서비스를 처음 보는가」지만 이쪽이 답하는 것은 「이번에 무엇으로
  들어왔는가」라, 한 번 보면 안 뜨게 하면 **팜플렛 QR 이 그 다음부터는 그냥 안내판 QR 이
  된다.** 코치마크(U-CM-19)는 이 상자가 닫힌 뒤에 뜬다(막을 두 겹 덮지 않는다). 새 부품은
  `WelcomeDialog`(feedback)이고, QR 지점 표에 **팜플렛 코드 `qr-004` 와 `festivalId` 칸**이
  함께 열렸다 — **관리자 항목표에는 「연계 축제」가 아직 없다**(실서비스 전 추가 필요).
  검수는 `?qr=qr-004`(진행중) · `?pamphlet=<축제 id>`(상태별).
  v1.21 은 **교육이 제 칩으로 서고, 「미용/생활」이 「생활/편의」가 된다** (사용자 요청).
  **업종 칩이 여섯에서 일곱**이 됐다. 3-4 표가 갈라지는 자리를 옮겼다: **생활/편의 =
  수리 및 개인 + 보건의료** · **교육 = 교육** · **기타 = 부동산 + 시설관리 및 임대 +
  과학 및 기술 + 숙박**. 뒤집힌 조항은 3-4 의 「교육과 의료는 방문객보다 주민 대상
  업종이므로 기타로 묶고 검색으로 접근하게 한다」다: **이 서비스를 쓰는 사람이 안내판 앞의
  방문객만은 아니고**(골목에 사는 사람이 찍는 QR 이 더 많다), 기타는 **이름이 무엇도
  약속하지 않는 자루**라 거기 들어간 업종은 칩 줄에서 찾아지지 않고 검색으로만 열린다.
  **교육을 다시 꺼낸 근거는 곳수다** — 한 번은 보건의료와 함께 생활/편의에 넣었는데 그
  자루에서 교육이 차지하는 몫이 **혼자 설 만큼 컸다**(둔전 22곳). 섞어 두면 학원을 찾는
  사람이 [생활/편의]를 눌러 봐야 하는데 그 이름은 학원을 부르지 않는다 — **덮는 이름을
  찾는 것보다 꺼내는 편이 나은 크기**가 있고, 반대쪽 끝의 규칙은 그대로다(**0~2곳짜리 칩은
  두지 않는다** — 숙박이 기타에 남는 이유). 꺼낼지 묶을지를 가르는 것은 업종의 성격이 아니라
  **그 칩이 설 만한 곳수인지**다. 이름이 바뀐 이유도 같은 줄기다 — 「미용/생활」은 대분류
  하나(수리 및 개인)를 받는 칩이라 이름의 앞 낱말이 그 안의 대표 업종(미용실)이었는데,
  보건의료가 들어오면 그 이름은 **담는 것의 절반만 가리킨다.** **이름이 값의 일부만
  가리키면 나머지는 없는 것처럼 읽힌다** — 의원을 찾는 사람이 [미용/생활]을 눌러 볼 이유가
  없다. **아이콘도 가위에서 벨(`concierge-bell`)로 옮겼다** — 가위는 그중 하나만 그린
  것이라 이름에서 고친 문제를 그림이 되풀이한다(새 칩은 `graduation-cap`). **약국은
  예외째 따라 옮겼다** — 대분류가 소매인데 칩이 기타였던 근거가 「성격이 보건의료라서」였고
  (3-5 의 2-2), 그 보건의료가 옮겨 갔다. 남겨 두면 약국만 부동산·세무사무소 옆에 서서 같은
  골목의 의원과 갈린다(서점 → 여가/문화 예외는 그대로다). **키도 `beauty` 에서 `life` 로
  바꿨다** — 저장되는 값이지만 화면과 더미 안에만 있고, `beauty` 는 의원이 든 자루의
  이름일 수 없다. **개수는 재배분한 값이다**(132/18/59/63/22/23/18) — 원천을 다시 집계할
  자리가 없으므로 옮겨 간 업종이 옛 자루에서 차지하던 몫만큼 덜어 옮겼고, **합 335 와
  온누리 139 는 그대로다.** 같은 판에서 **더미가 표와 어긋나 있던 자리 셋**도 맞췄다 —
  통신(소매)이 기타에, 정비소(수리 및 개인)가 기타에, 공방·화실(교육)이 여가/문화에 있었다.
  지금은 더미 335곳이 산출 규칙(`deriveChip`)과 **한 곳도 어긋나지 않는다**.
  v1.20 은 **처음 온 사람에게 탭 셋을 한 번 짚어준다** (U-CM-19, 사용자 요청). 걸음은
  셋이고 **탭 하나에 하나**다 — 공공시설 「내 주변 시설을 확인해요」 · 상점가 「이 동네 상점
  찾기」 · 둘러보기 「다른 상점가도 둘러봐요」. 근거: **이 화면에서 처음 온 사람이 못
  보는 것은 지금 보고 있는 탭이 아니라 나머지 둘이다.** 진입은 언제나 공공시설 탭이고
  거기 깔린 지도와 목록은 눈앞에 있으니 스스로 읽히는데, 나머지 두 탭에 무엇이 있는지는
  **눌러 보기 전에는 알 수 없다**(탭 이름 넉 자가 전부다). 안내판 앞에서 한 번 쓰고 마는
  서비스라, 눌러 보지 않으면 있는 줄도 모르고 떠난다. **자리를 밝히고 그 위에 말을 얹는다**
  — 설명만 화면 한가운데 띄우면 읽은 사람이 그 말이 가리키는 곳을 **스스로 찾아야 한다**.
  카드에 제목 · 설명 · 진행 점(지금 걸음만 알약) · **[이전]**(둘째 걸음부터) · [다음](마지막은
  **[시작하기]** — 더 갈 곳이 없는 자리에서 [다음]은 다음이 있다고 말한다) · 오른쪽 위 [×] 다.
  **[이전]을 둔 이유**: [다음]만 있으면 한 걸음 넘어간 순간 앞 걸음을 영영 못 보는데, 그때
  할 수 있는 일이 [×] 로 끄고 다시 여는 것뿐이고 **그렇게 끄면 「봤다」로 적혀 다시 열리지도
  않는다.** 진행 점은 누르는 것이 아니다 — 6px 에 44px 손가락 자리를 주면 점 사이 여백이
  전부 눌리는 자리가 된다. **본문은 문장마다 줄을 새로 시작한다**(마침표 뒤에 빈칸이 올
  때만 — 「1.4km」에서 갈리지 않게). **걸음이 넘어가도
  탭은 바뀌지 않는다** — 실제로 탭을 열어 보여주면 시키지 않은 이동이 어두운 막 뒤에서
  일어나고(카메라·시트·마커가 통째로), [×] 로 그만둔 사람이 자기가 가지 않은 탭에 서 있게
  된다. **막을 눌러도 닫히지 않고 밝힌 칸을 눌러도 아무 일이 없다.** 저장은 `localStorage`
  다 — 묻는 것이 「이 서비스를 처음 보는가」라 세션에 두면 **QR 을 찍을 때마다 처음**이
  된다. 저장이 막힌 브라우저에서는 매번 처음으로 친다(잘못 보이는 값은 [×] 한 번이면
  끝나고, 잘못 안 보이는 값은 되살릴 길이 없다). 검수 플래그 `?coach=1`. **상점가가 없는
  지점(S03-E)에서는 둘째 걸음을 뺀다** — 그 탭에 깔리는 것이 점포 목록이 아니라 U-ST-16
  안내라, 업종과 온누리를 말하는 문장이 그 위에 설 수 없다. 상점가 **이름도 적어 넣지 않고
  받는다**(안내판은 시 전역에 선다). **조아용은 카드에 세우지 않는다** — 이 화면의 캐릭터
  자리는 상단 띠 하나이고 그 띠는 코치마크가 떠 있는 동안에도 그 자리에 있다. 새 부품은
  `CoachMarks`(feedback)이고, 탭 한 칸의 자리를 내주는 `TabBar` 의 `itemRefs` 가 함께 열렸다.
  v1.19 는 **업종 칩 매핑에서 숙박이 빠져 있었다** (사용자 지적). 3-4 표가 상권업종
  대분류 **열 중 아홉만** 덮고 있었고, 빠진 하나가 숙박이다. **빠진 대분류는 「분류되지
  않음」이 되는 것이 아니라 그 점포가 목록에서 사라진다** — 업종 칩을 하나라도 걸면
  걸리지 않는 점포는 없는 가게가 된다(`chip_category` 를 비울 수 없게 한 것과 같은
  이유다). **문서만의 공백이 아니었다**: 매핑을 수행하는 유일한 코드
  (`admin/data/fields.js` 의 `deriveChip`)에 `if (/숙박|음료/.test(L)) return "cafe";`
  한 줄이 있어 **숙박업이 카페/디저트 칩으로 갔다** — 모텔이 카페 목록에 서는 것이고
  명세서 어디에도 근거가 없었다. **표에 적히지 않은 규칙은 사라지는 것이 아니라 아무도
  모르는 곳에서 지어진다.** ⑴ **숙박 → 기타** — 그 자루가 담는 것들(교육·보건의료·
  부동산·시설관리·과학기술)과 성격이 같다(**골목을 걷다 들러가는 가게가 아니다**). 별도
  칩을 두지 않은 것은 상점가에 숙박이 한둘뿐이면 **늘 0~2곳짜리 칩**이 서고 칩 줄이 한 칸
  더 잘리기 때문이다. ⑵ **카페/디저트 = 비알코올 중분류 전체 + 제과·빵·도넛·아이스크림·
  빙수 등 디저트 분류**로 문구를 확정했다 — 「비알코올 중분류, 빵/도넛 소분류」가
  **「비알코올 중에서 빵/도넛만」으로도 읽혔고**, 그러면 커피전문점이 카페 칩에 안
  들어간다. **두 조각은 더하는 것**이다(빵집·도넛·아이스크림은 비알코올이 아니라 제과점
  쪽 가지라 따로 끌어와야 한다). **디저트 조각에 아이스크림·빙수를 적은 것**은 사용자
  요청이다 — **칩 이름이 「카페/디저트」인 이상 디저트는 그 칩이 받아야 하고**, 두 낱말을
  걸어 놓고 그중 하나를 데이터에서 비우면 그 칩을 누른 사람에게 이름이 거짓말이 된다.
  그 조각은 **중·소분류를 함께 본다**(빙수집은 중분류가 「빙수·아이스크림」, 디저트 카페는
  중분류가 「디저트」라 소분류만 보면 소분류가 빈 줄에서 조용히 음식으로 떨어진다).
  ⑶ 같은 함수에서 표와
  어긋나던 셋도 맞췄다 — **교육**이 여가/문화로 가던 것(표는 기타) · **옛 표기**(`학문`)로
  매칭하던 것(3-5 의 2-1) · **서점과 약국**이 대분류가 소매라 쇼핑으로 가던 것(3-5 의
  2-2 가 든 예외). 분류명 뒤 공백을 터는 `strip()` 도 넣었다(3-5 의 1번. `"음식 "` 이 오면
  **음식 대분류가 통째로 기타로 떨어지는** 자리였다). **더미는 건드리지 않았다** — 둔전에
  숙박이 0곳이라 칩 개수(132/18/53/43/30/59)도 합 335 도 그대로다.
  v1.18 은 **안내 반경 고르개가 지도에서 내려온다** (사용자 요청). 자리가 지도 위에서
  **바텀시트 헤더 오른쪽 끝**(`Sheet` 의 `titleAside`)으로 간다. 근거: **반경은 지도의
  조작이 아니라 목록의 조건이다** — 고르면 바뀌는 것은 목록의 곳수이고(카메라는 그대로다),
  그 곳수를 읽는 자리가 시트다. **조건과 결과가 같은 판에 있으면 누른 것과 달라진 것이
  한눈에 들어온다.** **하루 동안 지도 위에서 자리를 세 번 옮긴 끝**이다 — 지도 우측 세로
  고르개(`RadiusSlider`, 200px) → 상단 필터 바 드롭다운(108px) → 지도 우측 세로 눈금자
  (`RadiusScale`, 46px). 폭을 줄이는 동안 문제는 작아졌을 뿐 없어지지 않았다: **셋 다 지도
  위였고, 지도를 보라고 만든 화면에서 지도를 가린다.** v1.17 이 [스캔 위치로]에서 닿은
  결론과 같다 — **가장 덜 차지하는 크기는 0 이다.** 얻는 것 셋: ⑴ `titleAside` 는 제목과
  한 줄을 나눠 쓰므로 **목록이 한 줄도 밀리지 않는다**(제목 아래 전체 폭인 `headerExtra`
  는 시설 유형별 개수 줄이 이미 쓴다) ⑵ **전체 스냅에서도 살아 있다** — 목록을 끝까지
  펴 놓고 보다가 반경을 넓히고 싶어지는 것이 바로 그때인데, 지도 위에 있던 시절엔 그때
  함께 감췄다 ⑶ 바탕이 흰 카드라 **지도 위에서 읽히게 하려고 두르던 것이 전부 필요
  없어진다**(34px 알약 · .82 비치는 막 · 흰 테 · 채운 삼각형) — 줄 안에 서는
  `InlineSelect` 그대로다. **지도 위에 남는 것은 [스캔 위치로] 하나**이고 그것도 앵커를
  놓쳤을 때만 뜬다. 손님이 없어져 함께 나간 것들: **`RadiusScale` 부품 통째로**,
  `InlineSelect` 의 `floating` · `faceMinWidth`, `FloatingControls` 의 `onWidthChange`,
  `MapFilterOverlay` 의 `trailing`, `Icon` 의 `caret-down`(`LOCAL` 표가 다시 비었다),
  그리고 v1.16 이 맞춰 둔 두 알약의 폭. **하루에 지도 부품 둘을 만들고 둘 다 지웠고**,
  남는 규칙은 한 줄이다 — **지도 위에 무엇을 얼마나 작게 놓을지 궁리하기 전에, 그것이
  지도 위에 있어야 하는 것인지 묻는다.**
  v1.17 은 **[스캔 위치로]를 놓쳤을 때만 띄운다** (사용자 요청). QR 지점 앵커가 지도 칸
  밖으로 나갔을 때만 뜬다 — 전에는 늘 떠 있었다. 근거: **이 단추가 하는 일은 지도를 QR
  지점으로 되돌리는 것 하나뿐이라, 지점이 화면에 보이는 동안에는 눌러도 아무 일이
  일어나지 않는다.** 그동안 이 알약은 지도를 가리기만 하고, **진입 직후의 화면이 정확히
  그 상태다**(카메라가 앵커에 맞춰 서 있다) — 처음 보는 사람에게 가장 먼저 눈에 드는
  떠 있는 것이 아무 일도 하지 않는 단추였다. v1.11 부터 이 단추를 작게·비치게·같은
  알약으로 손질해 온 줄기의 끝이다: **가장 덜 차지하는 크기는 0 이고, 필요 없는 동안에는
  그 크기여도 된다.** 재는 것은 **「보이는 띠」가 아니라 지도 칸 전체**다 — 시트가 덮어
  가려진 것과 지도를 끌어 놓친 것은 다른 일이고(앞은 시트를 내리면 된다), 띠로 재면
  **시트를 끌어올리는 동안 단추가 떴다 사라진다.** 가장자리 여백 28px 은 앵커 그림의
  크기다(잘린 핀은 「보인다」가 아니다). `KakaoMap` 의 새 `onAnchorOffscreen` 이
  `bounds_changed` 로 재서 알린다(`idle` 은 손을 뗀 뒤에야 온다). 전체 스냅에서 감추던
  규칙은 그대로 — **두 조건은 같은 이야기의 두 경우**다. 감추는 방법은 언마운트가 아니라
  `opacity` 라 **자리는 그대로 차지한다** (v1.16 이 재 둔 폭이 살아 있어야 한다는 이유도
  있었는데 v1.18 에서 그 폭 자체가 없어졌고, 남는 이유는 시트 앵커링이다 — 언마운트했다
  되살리면 나타나는 순간의 앵커가 옛 값이라 화면을 가로질러 흘러든다).
  v1.16 은 **자리를 되돌리고 폭을 맞췄다** (사용자 요청). ⑴ [스캔 위치로]가 **다시 지도
  우측 하단**으로 간다 — v1.15 를 되돌린다. 붙여 놓고 보니 **화면 오른쪽 위가 검색창 ·
  칩 줄 · 고르개 · 단추로 넉 줄**이 됐고, 지도를 보라고 만든 화면에서 위쪽이 통째로 막히는
  것이 v1.11 이 내내 고쳐 온 바로 그 문제다 — 그 값은 「닮은 둘이 붙어 있다」로 갚아지지
  않는다. 5-3 결정 3 이 다시 서고 `FloatingControls` 의 `place="top"` 은 지웠다.
  ⑵ 두 알약의 **폭이 같아진다**. v1.14 가 높이·바탕·테두리·그림자·글자색을 맞췄는데
  **폭만 글자 수를 따라 달랐다.** 나란히 놓였다면 오른쪽 끝만 맞춰도 같아 보이지만
  **화면의 반대쪽 끝에 떨어져 서면 한 벌인지를 말하는 단서는 각자의 덩어리 크기뿐이다.**
  맞추는 방향은 **[스캔 위치로] → 고르개**다 — 글자가 긴 저쪽이 늘 넓으므로 제 폭 그대로
  서고, 그 **실측 폭**을 고르개가 받아 최소 폭으로 삼는다 (`FloatingControls` 의
  `onWidthChange` → `InlineSelect` 의 `faceMinWidth`). 남는 폭은 좌우로 고르게 나뉜다.
  **상수 토큰(`--float-pill-min:108px`)으로 먼저 했다가 같은 날 걷어냈다** — 한글 글자
  폭을 눈대중으로 셈한 수라 고르개에는 20px 짜리 빈 자리가 생기고 단추는 그 수를 넘겨
  **둘이 어긋났고**, 남는 폭을 `space-between` 으로 양끝에 민 것까지 겹쳐 고르개가
  **한가운데 뚫린 알약**으로 보였다. **글꼴과 2차 글자 확대에 따라 달라지는 값은 토큰이
  아니다 — 재야 하는 값이다.** 남기는 한 줄: **통일감은 자리가 아니라 모양이 낸다.**
  v1.15 는 **한 벌이 된 둘을 붙여 세웠다** (v1.16 이 되돌렸다). [스캔 위치로]가 지도 우측
  하단에서 **안내 반경 고르개 바로 아래**로 옮겨가고, 그러면서 **시트 앵커링을 놓는다**
  (시트를 어디로 끌든 자리가 같다). **닮게 만들었으면 붙여야 한다** — v1.14 가 둘을 같은
  알약으로 맞추고 나니 똑같이 생긴 것 둘이 화면 위아래 끝에 하나씩 서 있었고, 그 거리에서
  닮음은 한 벌로 묶어 주는 대신 「같은 것이 왜 두 군데 있나」가 된다. **5-3 결정 3 의
  「되돌리고 싶어지는 자리는 지도 위이지 화면 맨 위가 아니다」가 뒤집힌다** — 그 말 자체는
  지금도 맞지만 그때 이 단추는 화면에서 혼자였다. 자리는 필터 바 실측 높이 바로 아래,
  오른쪽 여백이 `--float-inset`(16) 이 아니라 **`--gutter-screen`(20)** 이다 (위 검색창·칩
  줄과 같은 세로선이라야 「그 밑」이 된다). **바 안(`MapFilterOverlay` 의 `trailing`)으로
  들여놓지는 않는다** — 바가 44px 높아지면 `Sheet` 의 `topInset` 이 그것을 받아 전체 스냅의
  목록이 짧아지고, 전체 스냅에서 감추면 바가 줄어 **시트가 뛴다.** `FloatingControls` 의
  새 `place="top"` 이고, 기본값 `"bottom"`(시트 앵커링 · 트랜지션 없음)은 관리자 좌표
  지정 지도가 그대로 쓴다.
  v1.14 는 **지도 위에 뜨는 둘을 한 벌로 묶었다** (사용자 요청). [스캔 위치로] 단추가
  **안내 반경 고르개와 같은 알약**이 된다 — 34px · `--radius-pill` · 같은 바탕(.82 +
  흐림) · 테두리 · 그림자 · 글자색. 근거: **지도 위에 뜨는 것이 둘뿐이면 둘은 같은 것이어야
  한다.** 두 부품이 같은 화면의 같은 지도 위에 함께 서는데 서로 다른 시스템에서 온 것처럼
  보였다(34px 알약 + 13px 글자 vs 48×44 둥근 네모 + 아이콘을 인 12px 글자 — 바탕색만
  같았다). **v1.11 의 쌓기가 없어졌다** — 글자가 다시 아이콘 옆으로 오고
  `--fs-micro`(12px) · 아이콘 14px 이라 **100px 남짓**이다. 쌓았던 이유(아침에 옆에
  붙였다가 120px 을 넘겨 지도 오른쪽 아래를 덮었다)는 그 글자 크기의 이야기였고,
  세로 두 줄은 **알약으로 말 수 없어** 모서리 둥근 네모가 되고 높이도 44 를 넘어
  **같은 지도 위의 다른 하나와 닮을 수 없는 값**이었다. 높이 48 → 34 로 내려간 만큼을
  폭으로 돌려준 셈이고, 폭 50px 보다 **둘이 한 벌로 보이는 것**이 낫다. 글자를 떼지
  않는 것(v1.9)과 44px 손가락 자리는 그대로다.
  v1.13 은 **반경 고르개의 삼각형과 바탕**을 정했다 (사용자 요청). ⑴ 오른쪽 꺾쇠가
  **채워진 삼각형**이 됐다 — 지도 위에서 2px 짜리 윤곽선 획은 길·구획선과 굵기가 같아
  섞이는데, 채워진 덩어리는 같은 크기에서도 배경과 갈린다. lucide 에 없어 **`Icon.jsx`
  의 자체 제작 표(`LOCAL`)가 v1.9 이후 처음 다시 찼다**(`caret-down`, 모서리 반지름 1.1).
  v1.8 이 `qr-pin` 에서 얻은 교훈(「처음 보는 그림은 한 번 배워야 안다」)은 걸리지
  않는다 — 아래를 가리키는 채운 삼각형은 배운 적이 없어도 아는 그림이다. **줄 안에 서는
  고르개(정렬·출발지)는 꺾쇠 그대로다** — 그쪽은 흰 카드 위라 섞일 것이 없다.
  ⑵ 바탕이 **[스캔 위치로]와 같은 값**(`--surface-float-translucent` .82 + 흐림)이 됐다.
  **하루에 네 번 고친 자리다**: 흰 판(v1.11) → .70 + 흐림(v1.12) → **완전 투명** → .82.
  셋째와 넷째가 이 판 안에서 일어났다 — **투명은 안 됐다.** 알약을 걷고 글자에 흰 테를
  둘렀는데(`--halo-on-map`, 지도 라벨의 수법) 화면에서 읽히지 않았다: 테는 획 둘레
  1~2px 를 밝히는 것이라 글자와 배경의 밝기가 비슷하면 **획만 도드라지고 덩어리가
  배경에서 떨어지지 않는다**(지도 라벨이 그걸로 버티는 것은 획이 굵고 자간이 넓어서다).
  **막을 얇게 하는 것과 없애는 것은 다른 일이고, 없앤 자리에는 글자를 앉힐 바닥이 없다.**
  요청은 「80% 정도」였고 .82 가 그 값이라 **2 포인트 차이로 토큰을 하나 더 두는 것보다
  지도 위에 떠 있는 둘이 같은 바탕인 편이 낫다** — v1.12 의
  `--surface-float-translucent-soft`(.70)와 흰 테 토큰 둘은 지웠다.
  v1.12 는 **반경을 고르는 것이 지도를 움직이는 일이 아니게 했다** (사용자 요청).
  ⑴ **고르면 축척을 맞추지 않는다** — 고르는 즉시 그 반경의 원이 화면에 꽉 차도록
  줌을 잡았는데, 화면에서 일어난 일은 **넓히려고 누른 사람 앞에서 지도가 작아지는
  것**이었다. 4km 원이 다 들어오려면 그만큼 멀어져야 하고, 멀어진 화면에서는 방금까지
  보던 골목이 사라진다 — 곧바로 다시 확대하게 되니 **한 번 고를 때마다 손동작이 둘
  딸려온다.** 넓어졌다는 것은 **목록이 이미 말한다**(곳수가 늘고 새 마커가 가장자리에
  나타난다). 탭을 옮겨 돌아올 때 반경에 축척을 맞추던 줄도 함께 나갔다 — 그것만 남으면
  탭을 다녀오는 것이 축척을 바꾸는 길이 된다. ⑵ **점선 반경 원이 없어졌다** — 축척을
  사람 손에 돌려주고 나면 3·4km 원은 대개 화면 밖이라 **가장자리에 걸친 호 한 조각**만
  남는다. `KakaoMap` 의 `radius` 와 `fitRadius` 가 함께 삭제됐다. ⑶ **반경 고르개의
  왼쪽 아이콘(`circle-dot`)을 떼고 바탕이 비쳐 보인다**(.70 + 흐림) — 그 그림이
  가리키던 원이 없어졌고 단추 글자가 이미 「**반경** 2km」라고 축을 적는다.
  `InlineSelect` 의 `icon={null}` 과 새 토큰 `--surface-float-translucent-soft` 다.
  v1.11 은 **지도 위의 것들이 자리를 덜 차지하게 했다** (사용자 요청). 셋 다 같은
  이야기다 — 지도를 보라고 만든 화면에서 지도가 가려지고 있었다. ⑴ **반경 고르개가
  지도 오른쪽 세로 기둥에서 상단 필터 바의 칩 줄 아래 오른쪽 드롭다운으로** 내려갔다
  (세 칸이 각각 손가락 자리를 가지려니 200px 기둥이 되어 지도 오른쪽 3분의 1을 가렸다 —
  **고를 것이 셋뿐이면 접어 두는 것이 맞다**). 부품 `RadiusSlider` 는 삭제됐고
  `InlineSelect` 의 새 `floating` + `MapFilterOverlay` 의 새 `trailing` 이 그 자리를
  맡는다. ⑵ **[스캔 위치로] 단추가 작아지고 비쳐 보인다** — 글자를 아이콘 아래로 내려
  작게 적고(폭 120px → 아이콘만큼) 바탕이 .82 + 흐림이다 (`FloatingControls` 의 새
  `subtle`). 글자를 다시 떼지는 않는다(v1.9 의 근거 그대로) — 자주 쓰는 단추가 아니라
  **필요할 때 있으면 되는 단추**라 그 사이에는 지도가 보여야 한다. ⑶ **미리보기 카드가
  뜨면 시트가 접힘으로 내려간다** (전에는 전체일 때만 절반으로). 카드의 [x] 를 누르면
  내리기 전 자리로 돌아간다 — 그 사이 시트를 직접 끌었으면 기억을 버린다.
  v1.10 은 **안내 반경(U-FC-08)을 사용자가 고르게 했다 — 2 · 3 · 4km.** 고정 2km
  (`NEAR_LIMIT`)였다. 2km 는 도보 30분이라는 셈에서 나온 수인데 **그 30분이 누구에게나
  같은 30분이 아니다** — 급히 AED 를 찾는 중인지 오후 내내 골목을 돌 참인지에 따라
  걸어갈 마음이 있는 거리가 달라지고, 그것은 우리가 대신 정할 값이 아니었다. 고르개가
  받고(자리는 v1.11 이 옮겼다 — 위), 지도에는 점선 반경 원이 서며 고르면 그 원에 맞춰
  축척이 잡힌다. 기본값은 2km 라 **건드리지 않은 화면은 종전 그대로다.** 하한이 2km 인 것이 요점이다 —
  더 좁게 열면 U-FC-09 폴백(반경 안 0건이면 최근접 2건)이 상시로 걸려 **고른 값과
  어긋나는 목록**이 일상이 된다. **1km 「가깝다」의 선(`NEAR_ENOUGH`)과 원거리 안내는
  그대로다** — 안전시설이 그 선 밖이면 반경을 얼마로 두었든 뜬다. 고르개는 공공시설
  탭에만 있다(상점가 목록은 반경이 아니라 소속 상점가가 정한다).
  v1.9 는 지도 우측 하단 버튼을 **lucide `qr-code` + 「스캔 위치로」** 로 바꿨다.
  과녁(`crosshair`)이 어느 지도 앱에서나 **GPS 로 지금 선 자리를 찾는다**는 뜻이라
  누르는 사람이 이 버튼도 그런 것으로 읽었는데, 이 앱은 GPS 를 쓰지 않는다 — 「내 위치」는
  언제나 QR 을 찍은 그 지점이다. v1.8 은 그 답을 **자체 제작 아이콘**으로 냈다가
  (`qr-pin` — 핀 윤곽 안의 QR 모듈 넷) 하루 만에 걷어냈다: 20px 짜리 버튼에서 모듈이
  점으로 뭉개지고, 뭉개지지 않아도 **처음 보는 그림은 한 번 배워야 안다.** 두 마디로
  적으면 될 것이었다. `aria-label` 도 보이는 글자와 같다. `Icon.jsx` 의 자체 제작
  아이콘 표(`LOCAL`)는 이것으로 **비었다** — 표는 남고 든 것이 없다.
  v1.7 은 **골목 한바퀴 코스를 관리자 큐레이션으로 정했다** — 「자동 생성 또는 관리자
  큐레이션」이던 6장 #2 가 큐레이션 쪽으로 닫혔다. **시민 화면은 한 줄도 바뀌지 않는다**
  (바뀐 것은 그 넷이 어디서 오는가다). 시민용 더미는 그대로 자동 생성 결과다 — 서버가
  없어 관리자에서 짠 코스가 이쪽으로 오지 않으므로, 여기서 다른 넷을 적어 두면 검수하는
  사람이 두 화면에서 다른 코스를 보게 된다.
  v1.6 은 **원거리 안내(U-FC-09) 말풍선을 다시 여는 자리를 아이콘과 주의 배지로 좁혔다.**
  「⚠🛡 대피소 2곳」 줄 전체가 단추라 **개수를 보려고 스친 손끝에** 방금 닫은 경고가
  되살아났다. 이름과 개수는 읽는 값이라 이제 눌리지 않고, 과녁은 아이콘보다 사방 8px 씩
  넓다(자리는 그대로다). 같은 판에서 S08 코스 **설명 줄이 굵어졌고**(배지 줄과 순번 목록
  사이에 눌려 있었다), 진행률 줄의 **「표시는 선택입니다」가 없어졌다** — 아직 아무 곳도
  표시하지 않은 화면에서 표시가 의무가 아니라고 먼저 말하면, 하지 않아도 되는 일을
  **있다고 알려주는** 줄이 된다.
  v1.5 는 **공공시설 목록을 10곳씩 끊는다** (8 → 10, 사용자 요청). 8 은 시설 줄이 세 줄
  짜리라는 데서 거꾸로 셈한 수인데, **그 셈은 만든 사람만 안다** — 화면에는 여덟에서 끊길
  이유가 보이지 않고, 같은 앱의 S13 상점가 전체보기가 10 씩 끊으니 두 목록이 다른 수로
  끊기는 것만 눈에 남는다. 자르는 자리·`scrollKey`·마커가 쪽을 따르지 않는 규칙은
  그대로고 수만 바뀐다.
  v1.4 는 **칩 줄의 양 끝을 위 검색창에 맞췄다.** v1.3 은 폭을 벌려고 왼쪽으로 8px
  나가고 오른쪽은 화면 끝까지 흘렸는데, 벌어 놓은 폭이 도움이 되지 않았다 — 상점가
  탭에서 [쇼핑]이 딱 맞게 들어차고 [미용/생활]은 통째로 밖에 있어 **밀 수 있다는 것이
  화면에 보이지 않았다**(v1.3 이 고치려던 바로 그 상태다). 목적은 그대로고 방법이
  바뀌었다 — 폭을 벌어 한 칩을 더 넣는 대신 줄을 28px 좁혀 **잘린 칩을 확실히 만든다.**
  같은 판에서 하단 고지에서 문장을 하나씩 뺐다 — **공공시설 탭은 기준일 줄**(4종이 각각
  달라 날짜 넷이 늘어서면서 정작 읽어야 할 참고용·119 를 뒤로 밀었다. 기준일은 상세가
  적는다), **오류신고 폼은 119 안내**(자료가 틀렸다고 알리는 자리이지 급한 일을 처리하는
  자리가 아니다).
  v1.3 은 **상점가 목록 머리를 둘부터 세어 적는다** — 「음식 · 카페·디저트 150곳」이
  두 줄로 접히면서 sticky 제어 줄의 높이가 조건에 따라 달라졌다. 이제 하나면 이름
  (「음식 132곳」), 둘부터는 「업종 2종 150곳」이라 글자 수가 고정이다. 시설 유형 칩은
  이름이 짧고 그 줄에 정렬 컨트롤이 없어 그대로다. 같은 판에서 **둘러보기의 신규·인기
  매장 카드가 업종 칩을 더하지 않고 지운다** — 더하기는 걸어둔 조건을 지키려던 것인데
  실제로는 **고르지 않은 칩이 저 혼자 켜졌다**(음식만 보다가 미용실 카드를 누르면 돌아온
  목록에 미용실 43곳이 섞인다). 지름길로 왔으면 전체에서 시작한다.
  v1.2 는 하단 두 줄만 손질했다 — **필터 결과가 없을 때 한 줄만 적고**(뒤에 붙던 "칩을
  바꿔 보세요"와 전체 곳수는 화면에 이미 있는 것을 말로 옮긴 것이었다), **하단 고지의
  줄바꿈을 뺐다**(기준일자와 참고용 고지는 같은 것에 대한 말이다).
  시민용 화면의 기준 문서. v1.1 은 새 기능을 더한 판이 아니라 **완성된 화면에 문서를 맞춘**
  판이다 — 화면과 문서가 갈린 자리는 화면 쪽이 정본이고, 갈린 이유가 조항마다 적혀 있다.
  조항 번호는 옮기지 않았다(없어진 U-CM-18 도 자리에 남아 왜 없어졌는지를 적는다).
  문서 맨 앞의 개정 이력 참조.
- **관리자 웹 기능명세서: `admin/admin_specifications.md` (v1.28, 2026-09-04 개정)** —
  v1.28 은 **[등록]이 돌아온다 — 상점가는 폼, 점포·공공시설은 엑셀 한 장**이다 (사용자
  요청). M03 · M05 · M11 의 머리 오른쪽에 [골목형 상점가 등록] · [점포 등록] ·
  [공공시설 등록]이 서는데 **여는 것이 다르다**: M03 은 **폼**(M04 등록 창 — 축제·코스와
  같은 모양), M05 · M11 은 **파일 하나를 받는 창**이다(`.xlsx` · `.xls` · 최대 10MB). v1.15 가 이 둘을 없앤 근거(「점포·공공시설은
  공공데이터가, 상점가는 지정 제도가 정하는 목록이라 한 곳이 늘고 주는 일이 이 화면에서
  일어나지 않는다」)는 **일어나지 않는다가 아니라 드물게 일어난다**였다 — 길이 없으면
  담당자는 개발 쪽에 요청하고 기다린다(v1.9 의 [QR 지점 등록]과 같은 줄기다).
  **가르는 것은 자료의 성격이 아니라 「한 번에 몇 줄이 오는가」다** — 새 상점가는
  **한 달에 두세 곳**이라 한 건씩 폼으로 받는 편이 짧고(담당자가 배울 것이 하나 줄어든다),
  점포는 둔전 한 곳이 **335줄**, 공공시설은 네 유형을 합쳐 수백 줄이라 그럴 수가 없다:
  표를 보며 칸을 옮겨 적는 한 **오타는 반드시 난다**(v1.18 이 QR ID 를 고르는 칸으로 바꾼
  근거). **엑셀 창 셋으로 먼저 만들었다가 상점가 쪽을 같은 날 폼으로 되돌렸고**, 남는
  규칙은 한 줄이다 — **여러 줄이 한꺼번에 오는 자료만 파일로 받는다.** 단추 이름에 「일괄」을 넣지 않은 것은 담당자가 하려는 일이
  「등록하는 것」 하나라 가를 것이 없어서다. **상점가 등록 창에는 점포수 두 칸이 없다**
  (새 상점가에는 셀 점포가 없어 답이 늘 0 인데, 고칠 수 없는 0 을 칸으로 세워 두면 채워야
  할 칸으로 읽힌다 — v1.9 가 그렇게 열었던 규칙이 그대로 돌아왔다). **2026-08-20 이 일괄
  업로드를 범위 밖으로 보낸 근거와 부딪히지 않는다** — 그때의 이유(「5,000행을 넣고 화면에서
  되돌릴 방법이 없다」)는 적재를 화면이 곧장 반영할 때의 이야기이고, 이 창이 하는 일은
  **파일을 받는 데까지**다(폼 등록은 그렇지 않다 — 목록에 줄이 서고 저장된다). **[삭제]는
  돌아오지 않는다.** 새 부품은 `FileField`(admin — 끌어다 놓거나 눌러서 고르는 칸. 브라우저
  기본 파일 칸은 글자와 모양을 우리가 정하지 못하고 **끌어다 놓기가 되지 않는다**)와
  `admin/pages/BulkUploadModal.jsx`(점포와 공공시설이 같은 창을 쓴다). **엑셀 창은 파일보다
  먼저 한 칸을 고르게 하고, 고르기 전에는 파일 칸이 잠긴다** — 점포는 **소속 골목형
  상점가**, 공공시설은 **시설 유형**이다(그 값이 **파일을 어떻게 읽고 어디에 넣을지**를
  정한다 — 시설은 유형마다 항목표가 통째로 다르다: AED 2항목 · 화장실 10항목). 잠그되
  **왜인지는 그 칸이 적는다**(`FileField` 의 `blocked` — 규칙 줄 자리에 「…를 먼저
  선택해주세요.」가 서고 [업로드]로 막히면 같은 줄이 붉어진다. 고르개 아래에 같은 말을
  한 번 더 세우지 않는다). 소속을 창에서 받는 근거는 점포 쪽에 하나 더 있다 — 파일로 받는
  쪽에만 있는 물음이고(이 파일의 가게들이 어느 상점가 것인가) **화면에는 그것을 스스로
  알아낼 방법이 없다**: 소속 판정은 도로명주소를 상점가의
  **구역 주소 목록**과 맞추는 일인데(시민 명세서 3-2 — 둔전 66개 주소 → 335곳) 그 목록은
  앱 어디에도 없고, 상점가가 가진 주소는 「…번지 **일원**」 대표주소 한 줄이라 그것으로
  하는 짐작은 **틀려도 화면 어디에도 표시가 나지 않는다**(`districtId` 를 폼에서 담당자가
  고르는 이유이고, 더미의 335곳이 전부 둔전인 것도 매칭이 아니라 `sources.js` 가 채운
  값이다). **자동 매칭은 그대로 서버 쪽이다.** 파일에 열로 받지 않는 이유는 구역 자료가
  상점가별 xlsx 로 오고(한 파일 = 한 상점가) 파일 안의 이름은 **한 글자만 달라도 아무 데도
  걸리지 않기** 때문이다. 고르지 않으면 그 칸에서 막히고(창은 파일만 본다 — 오류는 그것이
  가리키는 칸 밑에 선다), 기본값은 **목록의 소속 필터**다(「전체」면 「— 선택 —」으로
  비워 연다). **[업로드]를 흐리게 두지 않는다**(v1.23 의 [추가]와 같은 결론 — 흐린 단추는 왜인지 말하지 않아 「파일을 안 골랐다」와
  「권한이 없다」가 같은 모양이 된다). **화면은 아직 열 규칙도 양식 파일도 그리지 않는다** —
  읽는 쪽이 없는 상태에서 「첫 줄은 열 이름」을 적으면 **어디에도 없는 규칙이 담당자에게는
  약속**이 되므로, 지금 [업로드]는 파일을 받았다는 사실만 토스트로 적는다.
  v1.27 은 **팜플렛 코드의 앵커를 소속 상점가에서 읽는다** (사용자 요청). 4장 QR 지점
  항목표에 **`festival_id`(연계 축제)** 한 줄이 서고 — 값이 있으면 그 코드는 안내판이
  아니라 **축제 팜플렛**이다 — 그것을 따라 **`address_road` 와 좌표가 ● 에서 ◐**,
  **`market_id` 가 ○ 에서 ◐** 가 된다. 근거는 시민 v1.23 과 같다. **목록의 구 고르개도
  팜플렛에서는 소속 상점가의 구를 따른다** — 주소에서 읽을 수 없기 때문이다(`guOf`).
  **화면은 아직 그 칸을 그리지 않는다**(4장 각주에 명시) — 이번 판은 항목표와 규칙까지이고
  「연계 축제」 입력칸과 「상점가를 고르면 앵커가 따라온다」는 다음 판이다. 그때까지
  팜플렛 코드는 관리자 화면에서 **만들 수 없고 고칠 수만 있다**(조건부 필수는 지금도
  걸린다 — 값이 레코드에 있기 때문이다). **명세서가 화면보다 앞선 자리를 적어 두는 이유**는,
  적지 않으면 다음 사람이 **화면의 공백을 규칙으로 읽기** 때문이다.
  v1.26 은 **`chip_category` 선택지가 여섯에서 일곱이 됐다** (사용자 요청). **「교육」**이
  새로 서고, 넷째가 **「미용생활」에서 「생활편의」**가 되며, 산출 규칙(`deriveChip`)이
  **보건의료·약국을 생활편의로, 교육을 교육으로** 보낸다 — 셋 다 기타였다. 산출 규칙 자체는
  시민 명세서 3-4 표이고 **그 표가 바뀌었다**(시민 v1.21). **담당자가 손으로 고른 칩은 그대로
  고정된다** — 산출 규칙이 바뀌어도 덮지 않는다(「수기」 배지, v1.10 그대로). 저장되는
  여섯 값의 자리는 그대로이고 바뀐 것은 **선택지의 이름과 무엇이 어느 칩으로 산출되는가**다.
  v1.25 는 **검색해 고르는 칸이 포커스로는 열리지 않는다** (사용자 요청). `OptionPicker`
  에서 `onFocus={() => setOpen(true)}` 한 줄이 나갔다. **v1.21 이 이미 그렇게 적어 두었던
  것을 화면이 지키지 않고 있었다** — QR ID 칸 아래 안내 줄을 뺀 근거가 「**누르면** 목록이
  펼쳐지고 글자를 치면 좁혀지니 검색해 고르는 칸이라는 것은 한 번 눌러 보면 안다」였는데,
  실제로는 **누르기 전에 이미 펼쳐져 있었다.** 다이얼로그는 열릴 때 본문의 첫 입력칸으로
  포커스를 보내고(1장) QR 지점 폼의 첫 칸이 QR ID 라, [등록]을 누른 담당자가 **처음 보는
  화면이 폼이 아니라 QR ID 오십 줄**이고 그 밑에 무슨 칸이 있는지는 목록을 닫아야 보였다.
  1:N 목록(코스의 들르는 점포)에서는 더 나빴다 — 줄을 Tab 으로 훑기만 해도 줄마다 목록이
  떴다 진다. **포커스는 「여기 있다」는 뜻이고 여는 것은 「고르겠다」는 뜻이라, 둘은 같은
  일이 아니다.** 여는 길 셋은 그대로다(누르거나 · ↓/Enter 를 치거나 · 글자를 치거나)라
  키보드만 쓰는 사람도 잃는 것이 없다.
  v1.24 는 **「조회수 상위 점포」 표에 읽을 것을 채운다** (사용자 요청). 열이 넷에서
  여섯이 된다 — **상권업종대분류명 · 도로명주소**가 더해지고, 표와 카드가 **다시 화면 폭을
  다 쓴다**(v1.19 가 내용 너비로 줄인 것을 되돌린다). **v1.19 의 진단은 절반만 맞았다**:
  「순위 배지와 조회수 사이가 손바닥만큼 벌어진다」는 사실이었고 줄이면 그것은 없어지지만,
  **없어진 자리에 화면 오른쪽 절반이 통째로 빈 채로 남는다** — 그 빈 절반은 「알맞게 선
  표」가 아니라 **「무언가 빠진 자리」**로 읽힌다. 카드까지 함께 줄인 것도 그래서였는데
  그러면 이번에는 카드가 왼쪽에 치우쳐 붙는다. **줄이는 쪽으로는 끝이 나지 않는 자리였다.**
  원인은 폭이 아니라 **열의 수**였고, 길은 둘이었다 — 자리를 없애거나(v1.19) **읽을 것을
  채우거나**. 이 표에서 담당자가 하는 일은 「이 가게가 맞나」를 확인하는 것이고, 그러려면
  이름 말고도 볼 것이 있다: **어느 업종이고 어디에 있는가**다(2-2 목록이 그 둘을 열로 두는
  이유와 같다). 함께 바뀐 이름 둘 — **「조회」 → 「조회수」**(세는 값이라 단위가 붙어야 하고
  절 제목도 「**조회수** 상위 점포」다) · **「업종」 → 「상권업종중분류명」**(그 열이 그리던
  값은 처음부터 중분류였는데, 옆에 대분류가 서면서 그 이름이 **어느 분류를 말하는지 답하지
  못하게** 됐다 — 「음식」과 「한식」이 나란한데 머리글 하나만 「업종」이면 매번 짐작한다).
  **`DataTable` 의 `fit` 이 통째로 없어졌다** — 손님이 이 표 하나뿐이었다. 남는 한 줄:
  **열이 서넛뿐인 표가 흩어져 보이면, 표를 줄이기 전에 그 자리에 담당자가 읽을 것이 빠져
  있지 않은지 먼저 본다.**
  v1.23 은 **프로그램·부스의 일시 칸이 연도를 묻지 않는다** (사용자 요청). 축제 폼의 1:N
  목록이 쓰던 `datetime-local` 한 칸이 **「축제 기간 중 어느 날」 고르개 + 「몇 시」 칸**
  둘로 나뉜다 (`Repeater` 의 새 `daytime`, `Festivals.jsx` 의 `daysBetween` → `withPeriod`).
  근거: **답이 정해진 것을 묻는 칸이었다** — 그 칸은 연·월·일을 다 물어 놓고 **연도는
  언제나 위 폼의 시작일과 같은 해**다. 축제 기간을 바로 위에서 받아 놓고 아래에서 연도를
  다시 묻고 있었다. 묻는 것이 셋에서 하나로 줄고, **축제가 하루면 고를 것이 하나뿐이라
  사실상 시각만 넣는다**(더미의 축제 여섯 중 다섯이 하루짜리다). **「축제 기간 내」가 더
  강하게 지켜진다** — 달력의 `min`/`max` 는 밖의 날을 흐리게 보여줄 뿐이지만 목록에는 그
  날이 아예 없다. 해를 넘기는 축제(12/28~1/3)에서 「연도가 같다」가 틀리는 것도 **날을
  통째로 목록으로 만들면 짐작할 것이 없어져** 함께 풀린다. **저장되는 값의 모양은
  그대로**(`2026-10-17T15:00`)이고, 그래서 한쪽만 든 **반쪽 값**이 잠깐 생기는데 그것은
  저장으로 나가지 못한다 — 필수 검사가 「비어 있는가」가 아니라 **「다 찼는가」**로 본다
  (`fields.js` 의 `filled`). **고를 날이 없어도 칸 모양은 같다** — 고르개가 칸 이름
  (「일자」)만 달고 잠기고, **무엇을 먼저 해야 하는지는 [추가]를 누른 순간 적는다**
  (`Repeater` 의 새 `addBlocked` — 「시작일과 종료일을 먼저 입력해 주세요.」). 그때만 종전
  한 칸짜리로 떨어지게 두었다가 걷어낸 자리다: **[등록] 창이 정확히 그 상태라** 담당자가
  처음 보는 화면에서 연도를 받는 칸을 봤다. **흐린 단추는 못 누른다는 것만 말하고 왜인지는
  말하지 않는다** — 그래서 [추가]는 눌리게 두고 이유를 적는다.
  같은 판에서 **시민 화면의 축제 요일이 하루씩 밀려 있던 것을 고쳤다** — 2026-10-17(토)이
  「10.17 (금)」으로 나왔다. `+09:00` 으로 못박은 Date 에 `getUTCDay()` 를 물어 **KST 로
  만드는 것과 UTC 로 읽는 것을 한 줄에 섞은** 탓이다 (`districts.js` 의 `label`).
  v1.22 는 **업종 칩 산출이 시민 명세서 3-4 표와 어긋나 있었다** (사용자 지적).
  `chip_category` 는 「자동 산출 후 수기 변경 가능」이고 **산출 규칙 자체는 시민 명세서
  3-4 표**인데, 그것을 코드로 옮긴 유일한 자리(`admin/data/fields.js` 의 `deriveChip`)에
  **표에 없는 규칙이 지어져 있었다** — `숙박` 을 카페/디저트로 보내는 줄이다. 시민 명세서
  v1.19 가 표에 숙박을 더했고(기타) 이 판은 코드가 그 표를 따르게 한다. 카페/디저트의
  디저트 조각도 같은 판에서 **아이스크림·빙수를 포함하고 중·소분류를 함께 보게** 됐다.
  함께 맞춘 것:
  **교육**(표는 기타로 보낸다) · **옛 표기**(`학문`) · **서점·약국 예외**(대분류가 소매라도
  칩은 여가/문화·기타다) · **분류명 뒤 공백**(`strip()`). **담당자가 손으로 고른 칩은 그대로
  고정된다** — 산출 규칙이 바뀌어도 덮지 않는다(「수기」 배지, v1.10 그대로).
  v1.21 은 **QR ID 칸 아래 안내 줄도 뺐다** (사용자 요청) — 같은 날 v1.18 이 세운 줄이다
  (「안내판에 인쇄된 QR ID 를 검색해 고릅니다. 이미 다른 지점에 매칭된 ID 는 고를 수
  없습니다」). **두 문장 다 칸이 그 자리에서 하고 있는 말**이다: 누르면 목록이 펼쳐지고
  글자를 치면 좁혀지니 검색해 고르는 칸이라는 것은 한 번 눌러 보면 알고, 이미 걸린 ID 는
  그 목록에 흐린 글씨로 서서 「◯◯에 매칭됨」을 달고 있다 — **못 고른다는 사실을 못 고르는
  그 줄이 직접 적는다.** v1.20 의 조아용 이미지 칸과 같은 이야기다 (격자나 목록이 눈앞에
  펼쳐지는 칸 위에 「무엇을 고르는 자리인지」를 글로 한 번 더 적지 않는다).
  v1.20 은 **조아용 이미지 칸 아래 안내 줄을 뺐다** (사용자 요청) — 「사전 등록된 조아용
  이미지 내에서 선택가능합니다. 파일 업로드는 지원하지 않습니다.」 **두 문장 다 화면이
  이미 하고 있는 말**이다: 앞은 격자에 선 그림 여덟 장이 답하고, 뒤는 **없는 단추에 대한
  설명**이라 오히려 「어딘가에 있을 것 같은데 막혔다」로 읽힌다. v1.11 이 이 줄을 고쳐
  쓴 판이었는데(「에셋」 → 「조아용 이미지」) **고칠 것이 아니라 뺄 것이었다.** 이름 줄의
  겹침 안내는 그대로다 — 늘 있는 설명이 아니라 방금 고른 것에 대한 말이다.
  v1.19 는 **대시보드에서 이름과 폭 셋을 손봤다** (사용자 요청). ⑴ 둘째 요약 카드가
  **「최근 7일 스캔」에서 「최근 7일 QR 스캔」**이 됐다 — 왼쪽이 「누적 QR 스캔」인데
  기간만 다를 뿐 같은 수를 다른 이름으로 부르고 있었고, 이름이 갈리면 담당자는 **다른
  것을 세는 두 카드**로 읽는다. ⑵ **「조회수 상위 점포」 표와 그 카드가 내용 너비에
  맞춰 선다** (`DataTable` 의 새 `fit`) — 열이 넷뿐인데 화면 폭을 다 써서, 폭이 고정된
  셋을 뺀 나머지를 상호명 열이 통째로 먹고 순위 배지와 조회수 사이가 손바닥만큼 벌어져
  있었다. **표가 넓은 것이 아니라 읽을 것이 흩어진 것**이다 (목록 화면의 표는 그대로
  폭을 다 쓴다 — 거기는 열이 예닐곱이고 하는 일이 훑는 것이다). 카드도 함께 줄어드는
  것은 표만 줄이면 반쪽만 찬 카드가 무언가 빠진 자리로 읽히기 때문이다. ⑶ 상점가
  고르개의 구와 이름 사이가 **가운뎃점에서 줄표**가 됐다 — 「처인구 - 둔전골목형상점가
  (점포 335곳)」. 이 화면의 가운뎃점은 **나란한 두 값**을 잇는 부호인데 상점가는 그 구
  **안에** 있다.
  v1.18 은 **QR ID 를 미리 만들어 두고 고르게 했다** (2026-08-25 회의 결정사항).
  **QR ID 와 QR 이미지 50개를 사전 생성하고, 지점 등록·수정에서는 그중 아직 아무 지점에도
  매칭되지 않은 하나를 검색해 고른다** — 담당자가 코드를 적는 자리가 화면에서 없어졌다.
  v1.9 가 [QR 지점 등록]을 열면서 「**식별자 자동생성은 두지 않는다** — 화면이 지어낸
  코드는 인쇄된 코드와 맞을 이유가 없다」로 닫아 둔 조항이 뒤집힌 것이다. 걱정은 맞았고
  **답이 반대였다**: 지어내지 않는 방법은 담당자에게 받는 것이 아니라 **한 곳에서 미리
  만들어 두고 그중에서 고르게 하는 것**이다. 손으로 옮겨 적는 한 오타는 반드시 나는데
  **오타 난 코드는 아무 데도 걸리지 않는다** — 형식만 맞으면 저장되고(`dunjeon02` →
  `dunjoen02`), 어느 지점도 그 코드를 쓰지 않으니 중복 검사도 통과한다. 안내판은 현장에
  붙어 있는데 표에는 한 글자 다른 코드가 앉고, 그것을 찍은 시민은 「등록되지 않은 QR
  코드」를 보며, **아무도 그 사실을 모른다.** 고르는 칸이 되면서 형식 검사(`V.qrCode`)와
  범위(4~12자)가 함께 없어졌다 — 느슨해진 것이 아니라 **검사할 것이 남지 않았다**:
  오타 · 표기 흔들림 · 중복 셋 다 사람이 치는 칸에만 있는 문제다. **이미 걸린 ID 는
  목록에서 빼지 않고 흐린 글씨로 세워 「◯◯에 매칭됨」을 적는다** — 그 ID 가 인쇄된
  안내판을 든 담당자에게 「없다」와 「이미 쓴다」는 할 일이 다르다 (`OptionPicker` 에
  `disabled` · `note` 를 그때 열었다). **[발번] 단추도 QR 이미지도 이 화면에 없다** —
  그 단추가 만드는 것은 ID 하나가 아니라 인쇄해서 현장에 붙일 안내판이고, 코드를 그림으로
  인코딩하는 일은 개발 쪽 일괄 작업이다. 목데이터의 발번은 `qr-001` ~ `qr-050` 이고
  **시민용 더미의 QR 지점 세 곳도 그 표의 값을 쓴다**(`qr-001` · `qr-002` · `qr-003`) —
  지점의 성격을 코드에 적던 옛 이름(`dunjeon-2019-04`)은 없어졌다. **발번은 뜻을 담지
  않는다.**
  v1.17 은 **[삭제]를 마지막 둘(QR 지점 · 계정 관리)에서도 없앴다 — 관리자 웹에 지우는
  자리가 하나도 없다** (사용자 요청). 두 화면 다 삭제 확인 창의 각주가 **대안을 권하고**
  있었는데(「[철거] + 비활성」 · 「[사용 여부] 해제」), **각주는 [삭제]를 누르고 들어온
  사람에게 하는 말**이라 읽든 안 읽든 다음에 일어나는 일은 영구 삭제였다. 권하는 것이
  「끄기」라면 화면에 끄는 스위치만 두는 편이 그 권고를 실제로 지킨다. v1.15 가 이 둘을
  예외로 둔 근거(「안내판을 철거하면 그 코드가 실제로 없어지고, 사람이 떠나면 계정은
  지워야 한다」)는 **둘 다 반대였다** — 안내판을 철거해도 **종이는 현장에 남아** 그 코드를
  찍는 사람이 있고(대시보드의 지점별 스캔), 계정의 아이디는 **변경 이력의 주체**라 지우면
  지난 기록의 「누가」가 없는 사람을 가리킨다. 같은 판에서 **`is_active` 의 이름이
  「활성 여부」에서 「사용 여부」**가 됐고(계정 관리가 처음부터 그렇게 불렀다 — 같은 일에
  화면마다 다른 이름이었다. 저장되는 필드는 그대로다), **QR 지점 목록의 그 열이 배지에서
  토글**이 됐다(정보 관리 다섯의 [노출 여부]와 같은 모양 — 배지는 읽는 값의 모양인데
  담당자가 켜고 끄는 값이었다). **설치 상태는 그대로 배지다** — 그쪽은 현장의 사실이다.
  남는 자물쇠 둘은 그대로다: 최종 관리자(`admin`)와 마지막으로 남은 쓸 수 있는 계정은
  [사용 여부]를 끌 수 없다.
  v1.16 은 **목록 검색을 「이름 한 칸」으로 좁혔다** (사용자 요청). 여덟 화면이 전부 서너
  칸을 한 문자열로 이어 훑고 있었는데, 그중 하나는 거의 언제나 **바로 왼쪽 고르개가 이미
  가르는 축**이었다 — 그리고 두 길의 답이 달랐다: 업종 고르개는 대분류로 정확히 가르고
  검색은 소분류를 부분 일치로 훑어 「카페」를 **고른** 결과와 **친** 결과가 서로 다른
  목록이다. 이제 상점가명 / 상호명 / 축제명 / 코스명 / 명칭 / QR ID·지점명 / 내용 /
  아이디 하나씩만 본다 — **검색은 「아는 한 건을 여는」 일이고 훑는 일은 고르개가 맡는다.**
  같은 판에서 **공공시설과 QR 지점에 「소속 구」 고르개**가 생겼다 (QR 지점에서는 설치
  상태 고르개가 그 자리를 내줬다 — 그 값은 표의 배지와 줄 강조로 읽는 값이지 좁히는
  축이 아니다). 둘 다 `gu` 를 항목으로 갖지 않으므로 **저장하지 않고 도로명주소에서
  읽는다** (`districts.js` 의 `guOf`). 또 **`qr_code` 의 이름이 「QR 식별자」에서
  「QR ID」**가 되고, 오류신고 「내용」 열이 320px 상수 대신 **칸 폭에 맞춰** 잘린다
  (`DataTable` 의 새 `fixed`).
  v1.15 는 **대시보드에서 세 구획을 뺐다** (사용자 요청) — 지점별 스캔 횟수 · 카카오맵
  API 일일 사용량 · 신규 점포. 앞의 둘은 **보고 나서 이 화면에서 할 일이 없다**(v1.3 의
  환경 설정 · v1.7 의 탭별 조회 비중 · v1.14 의 데이터 갱신 현황과 같은 기준), 신규 점포는
  둘러보기 두 레일 중 하나라 남은 「조회수 상위 점포」가 폭을 다 쓴다. 값 셋
  (`SCANS_BY_POINT` · `API_USED_TODAY` · `QUOTA_DEFAULTS`)은 자료 파일에 그대로 둔다 —
  지금 지우면 되살릴 자리가 생겼을 때 다시 지어내야 한다.
  v1.15 는 또 **오류신고의 동일 대상 누적 강조를 없앴다** (사용자 요청) — 붉게 서던 줄 ·
  접수번호 칸의 「n건 누적」 배지 · 상세 맨 위의 붉은 안내 상자 셋 다. **끌 수 없는
  표시였다**: 붉은색은 「지금 손대라」는 말인데 신고 셋이 쌓인 것 자체는 담당자가 없앨 수
  있는 상태가 아니고, 그 사실은 **같은 이름이 세 줄 나란히 선 것**으로 이미 보인다
  (같은 판에서 미처리만 세도록 좁혀 봤지만 남는 문제가 그것이다). 접수번호 열은 176 →
  112 로 돌아갔다.
  v1.15 는 또 **만들고 지우는 자리를 그 일이 실제로 일어나는 곳에만 남겼다** (사용자 요청).
  **[등록]은 축제·코스·QR 지점·계정 넷**에만 있고 (상점가·점포·공공시설은 조회와 수정만
  한다 — 화면 이름도 M04·M06·M12 가 「등록·수정」에서 「수정」이 됐다), **[삭제]는 QR
  지점·계정 둘**에만 있다. 정보 관리 다섯의 목록에서 **「관리」 열이 통째로** 없어졌다
  (안에 [삭제] 하나뿐이었다). 근거: 점포·공공시설은 공공데이터가, 상점가는 지정 제도가
  정하는 목록이라 늘고 주는 일이 이 화면에서 일어나지 않고, 축제·코스는 담당자가 만들지만
  **지나간 것이 곧 기록**이다. **지운 줄은 다음 갱신에서 되살아나거나 영영 사라져 담당자가
  한 일이 남지 않고, 내린 줄은 그 자리에 그대로 있다** — 그래서 정리는 [노출 여부]가 한다.
  같은 판에서 **상점가 연쇄 삭제가 없어졌다**(축제·QR 지점·점포를 함께 지우던 길과 그것을
  이름·곳수로 늘어놓던 확인 창) — 지우는 길이 없어지면서 그 앞의 안전장치도 함께 나갔다.
  v1.15 의 다른 두 변경은 **한 사실을 한 자리에서 받는다**는 같은 이야기다. 상점가의
  **점포수·온누리 가맹 점포수가 다시 ⚙ 이 되었다** — 그 둘의 출처는 **점포 정보 하나**이고
  (걸린 점포 중 노출 상태인 건수, 그중 온누리 가맹), 같은 사실을 두 곳에서 받으면 반드시
  어긋난다(점포 목록에는 335줄인데 상점가 목록에는 340곳). v1.9 가 열었던 근거 —
  「점포 자료가 있는 상점가가 한 곳뿐이라 나머지 31곳의 수를 고칠 자리가 없다」 — 는
  **자료가 아직 안 들어왔다는 우리 쪽 사정**이지 이 칸의 성격이 아니다.
  목록의 「수기」 배지와 그 기억(`countManual`), 「점포수보다 많을 수 없습니다」
  검사도 함께 없어졌다 — 마지막 것은 두 수를 따로 받을 때만 생기는 어긋남이다.
  같은 판에서 **온누리 두 칸을 한 항목으로 묶었다** — 종류(지류·디지털)를 고르면 가맹여부
  토글이 켜지고, 다 풀면 꺼지고, 토글을 끄면 종류가 비워진다. 하나의 사실을 두 칸으로
  나눠 받으면 **어긋난 상태가 반드시 생긴다**(꺼진 토글 옆에 켜진 체크박스). 남는 어긋남
  하나 — 켜고 아무것도 고르지 않은 상태 — 만 검사로 막는다: 종류가 ○ 에서 **◐** 이 되고
  「온누리 상품권 종류를 선택해주세요.」가 그 칸에 뜬다(항목표에 `requiredMsg` 를 열었다 —
  고르는 칸에서 「필수 항목입니다」는 무엇을 눌러야 하는지 말해주지 않는다). 켜는 쪽은
  자동으로 채우지 않는다 — 지류인지 디지털인지는 담당자만 안다. 같은 판에서 점포 목록의
  **온누리 배지가 종류를 적고**(넘기던 글자가 배지 안에서 덮여 「온누리」만 서 있었다),
  더미 자료의 가맹 139곳에 **지류·디지털 둘 다**를 넣었다 (시민 화면 상세가 가맹 점포마다
  「지류 및 디지털」이라고 적는다 — 비워 두면 두 화면이 같은 점포를 다르게 말한다).
  v1.15 는 또 **골목 한바퀴 코스를 넷으로 고정했다** (3곳 이상 → 4곳). 셋은 코스의 길이가
  아니라 **버리는 기준**이었다 — 시민 화면이 셋보다 적은 코스를 그리지 않는다는 데서 온
  수인데, 실제로 그리는 코스는 넷짜리다(①②③④). 하한만 두면 셋짜리와 여섯짜리가 섞여
  저장돼 둘러보기 탭에서 카드마다 코스 길이가 달라진다. **빈 줄 넷이 처음부터 서 있고
  [점포 추가]도 휴지통도 없다**(`Repeater` 의 `fixedRows`) — 줄 수가 값이 아니라 **틀**이면
  틀은 처음부터 서 있어야 한다. 「채우면 [추가]가 없어진다」로 먼저 만들었다가 바꾼
  자리다: 그러면 담당자가 정해져 있는 수를 손으로 맞추는 일을 한다(세 번 눌러 네 줄을
  만들고, 하나를 바꾸려면 지웠다 다시 만든다). 지우는 단추가 없어도 잃는 것이 없다 —
  한 곳을 빼는 일은 다른 곳으로 **바꾸는** 일이라 그 줄에서 다시 고르면 된다.
  목록 머리도 **「4건」 한 마디**다: 「4건 ·
  최소 3건」은 다 채운 화면에서 규칙이 계속 서 있어 할 일이 남은 줄로 읽힌다(`minRows`
  가 하던 일이 그것뿐이라 함께 없앴다). 같은 판에서 **순번이 줄 맨 앞, 손잡이가 칸
  오른쪽(휴지통 앞)으로** 갔다 — 손잡이가 맨 앞이면 줄 첫머리에 오는 것이 누르는 것이라
  차례를 읽으려는 눈이 그것을 넘어가야 ①②③④ 에 닿는다. 읽는 값은 왼쪽, 누르는 것 둘은
  오른쪽 끝이다.
  v1.14 는 **[골목 한바퀴 코스 관리]를 만들고(M09 · M10) [데이터 갱신 현황]을 없앴다 —
  화면이 열여섯이다.** 코스는 지금까지 자료가 아니라 계산이었는데(300~500m 밴드에서 세
  축으로 갈라 가까운 순 넷), 규칙이 뽑는 코스는 **가깝다는 것 말고 아무 뜻이 없다.**
  담당자가 상점가마다 코스를 짜고 넣을 점포와 그 **차례**를 정한다. 저장하는 것은 점포의
  id 와 차례뿐이다 — 도보 시간과 거리는 QR 지점에서 재는 값이라 시민 화면의 몫이고,
  이름·좌표를 굳히면 점포를 고쳤을 때 코스만 옛 값을 든다. 이 목록에서는 줄의 자리가
  값이라 1:N 편집기에 **순번과 ↑ ↓** 가 처음 생겼다 (`Repeater` 의 `ordered`).
  [데이터 갱신 현황]은 **보고 나서 할 일이 없는 화면**이었다 — 오래된 기준일을 발견해도
  담당자가 하는 일은 개발 쪽에 알리는 것뿐이다(v1.7 이 대시보드의 「탭별 조회 비중」을
  뺀 기준과 같다). 값은 그대로 돈다. **번호를 다시 당겼다** — 공공시설 M09·M10 →
  M11·M12, QR 지점 M11·M12 → M13·M14, 오류신고 M13 → M15, 계정 관리 M15 → M16.
  옛 번호 대조표가 명세서 1장에 있다.
  v1.13 은 오류신고 상세의 이동 버튼 이름을 **좌측 메뉴의 화면 이름 그대로** 적는다
  (「점포 관리로 이동」 → 「점포 정보 관리로 이동」). 대상 유형에 「관리로 이동」을 붙여
  글자를 지어내고 있었는데, 그렇게 나온 이름은 **메뉴 어디에도 없는 이름**이다.
  같은 판에서 **그 버튼이 한 건을 곧장 여는 길을 만들었다가 걷어냈다** — 대상 유형이
  「기타」인 신고가 있고 유형이 적혀 있어도 대상 ID 가 빈 신고가 있어, 같은 버튼이
  어떤 신고에서는 창을 열고 어떤 신고에서는 목록에 내려놓는데 **그 갈림을 누르기 전에
  알 방법이 없다.** 버튼은 화면까지만 데려다 놓는다. 같은 판에서 점포 목록의
  **업종 칩 열이 180px** 이 됐다 (「카페/디저트」만 아이콘이 윗줄에 홀로 남았다 —
  칩은 그림과 이름이 함께여야 한다).
  v1.12 는 **도로명주소 칸에 도로명만 들어가게 했다.** V-02 가 타이핑을 막는 이유는 표기가
  흔들리지 않게 하기 위해서인데(점포의 소속 상점가 판정이 도로명주소 문자열 매칭이다),
  화면이 **도로명 없는 검색 결과에 지번을 대신 넣어 주고** 있었다 — 담당자는 고른 적도
  없는 표기를 갖게 되고 검색으로 들어온 값이라 더 믿는다. 이제 그런 줄은 버리고, 대신
  **빈 목록이 왜 비었는지를 갈라 적는다**(도로명이 없어서 / 못 찾아서). 검색창 예시도
  「도로명 + 건물번호」다 — 도로명만으로는 거의 나오지 않는다. 같은 판에서 **주소를
  고르면 지도가 그 자리로 따라간다** — `KakaoMap` 이 지도를 한 번만 만들어, 이미 좌표가
  있는 자료에서는 값과 핀만 새 자리로 가고 화면은 옛 자리에 남아 있었다(좌표가 없던
  자료에서는 그때 지도가 처음 서므로 신규 등록으로만 검수하면 놓친다). 같은 판에서 점포 목록의
  **「조회」 열이 가운데 정렬**이 됐다: 정렬 꺾쇠가 달린 열이라 오른쪽 정렬이 머리글만
  안쪽으로 밀어 값과 한 세로선에 서지 않았다(꺾쇠 없는 대시보드의 같은 열은 그대로다).
  v1.11 은 **색으로 하던 말과 인용으로 하던 말을 줄였다.** 1:N 목록의 줄 카드가 회색
  바탕(#f1f3f5)에서 **흰 바탕 + 가는 테두리**가 됐다 — 카드가 푸는 물음(「한 줄이 어디서
  끝나는가」)은 닫힌 도형이 답하는 것이지 색이 답할 것이 아니었고, 흰 입력칸을 넷씩
  얹으니 그 밑의 회색이 배경이 아니라 **덩어리**로 보였다. 같은 판에서 QR 지점의
  「설치완료 + 활성 꺼짐」 경고 상자가 **사용자 화면 문구 인용을 뺐다** — 저쪽이 바뀌면
  여기도 고쳐야 하는 줄이었고(S11-A 가 생기면서 실제로 한 번 고쳤다), 담당자가 지금 할
  일과 상관이 없다. 남는 둘은 상태와 할 일이다. 같은 판에서 **조아용 이미지의 겹침
  안내가 격자 아래에서 이름 줄로 올라갔다** — 격자 아래에 경고와 안내가 늘 두 줄로
  서 있었고, 색만 다른 두 줄이 나란하면 어느 쪽이 지금 일어난 일인지 흐려진다.
  폼 항목의 이름 줄에 한 마디 더 다는 자리(`FormField` 의 `badge`)를 그때 열었다.
  v1.10 은 **⚙ 인 채로 열려 있던 칸들을 ● 으로 바꿨다.** v1.2~v1.9 는 「⚙ 이면서 고칠 수
  있는 칸」을 예외로 두었는데(⚙ 는 값의 출처, 읽기 전용은 권한 — 둘은 다른 이야기라는
  근거다), **화면에서는 그 둘이 갈리지 않았다**: 담당자가 보는 것은 「사람이 넣지 않는
  값」이라는 배지와 그 밑에 열려 있는 입력칸이다. 상점가의 점포수·온누리 가맹 점포수와
  점포의 표시 업종 칩이 ● 이 되면서 예외를 쓰던 항목이 없어졌고, 화면 쪽 `editable`
  플래그도 함께 빠졌다. **좌표도 ● 이다 — 점포·공공시설·QR 지점 셋 다.** 들어오는 길은
  그대로고(주소를 고르면 채워진다) 달라지는 것은 끝내 비어 있으면 저장이 막힌다는 것뿐이다:
  좌표 없는 줄은 지도에 없는 줄이고, QR 지점이면 그 코드로 들어온 시민이 보는 **모든 거리**의
  기준점이 없다. 같은 판에서 축제의 **프로그램 일정·부스 위치 제목 옆에 「선택」 배지**를
  세우고(열 이름의 별표는 「줄을 만들었으면 채우라」이지 「줄을 만들라」가 아니다),
  계정 수정 창의 **아이디 배지를 ● 로 되돌렸다**(읽기 전용은 그대로 — 못 고치는 이유가
  자동 생성이 아니라 변경 이력의 주체라서다).
  v1.9 는 **[QR 지점 등록]을 만들었다 — v1.1 부터 「관리자 화면은 지점을 만들지 않는다」였던
  조항이 뒤집혔다.** 근거였던 「지점이 새로 생기는 일은 안내판을 제작해 붙이는 일이다」는
  지금도 맞지만 결론이 틀렸다: **안내판이 서고 나면 그 코드는 어딘가에 반드시 등록되어야
  한다.** 화면에 자리가 없으면 그 일은 개발 쪽 요청으로 넘어가고 담당자는 안내판을 붙여
  놓고 기다린다. **식별자 자동생성은 그대로 없었다** — 등록 창에서도 코드는 담당자가
  적었다 (화면이 지어낸 코드는 인쇄된 코드와 맞을 이유가 없다). **그 조항은 v1.18 에서
  뒤집혔다** (위). 열면서 등록 창 맨 위에 안내 상자를 세웠다가 **같은 판에서 걷어냈다** —
  거기 적던 것이 전부 화면에 이미 서 있다
  (식별자 칸의 안내 줄 · 설치 상태 고르개와 꺼진 스위치 · 수정 창의 경고 상자).
  같은 판에서 오류신고의 누적 강조를 미처리만 세도록 좁히고 배지를 접수번호 칸으로
  옮겼다 — **그 강조는 v1.15 에서 통째로 없어졌다** (아래).
  v1.8 은 **같은 일이 화면마다 다른 모양이던 것을 맞췄다.** 공공시설의 유형 탭이
  고르개가 되어 검색창 왼쪽으로 내려오고(다른 화면의 고르개가 서는 자리), 체크박스
  일괄 처리가 목록 다섯 전부에 선다(상점가·축제는 노출/숨김, QR 지점은 활성/비활성).
  **일괄 삭제는 어디에도 없다** — 삭제가 영구라서다. 같은 판에서 **QR 지점의 열 항목을
  전부 고치고 지울 수 있게 했다** — 읽기 전용이던 넷(식별자·지점명·주소·좌표)이 폼으로
  내려온다. 「안내판에 인쇄된 값」이라는 근거는 그대로지만 **잠그는 대신 적는다**:
  식별자 칸이 「바꾸면 옛 코드를 찍은 사용자에게 미등록 안내가 뜬다」를, 삭제 확인 창이
  「안내판은 현장에 남는다 · 교체했다면 [철거] + 활성 끄기」를 적는다.
  (v1.8 은 **[QR 지점 등록]을 열지 않았다** — 그것은 v1.9 다.)
  v1.7 은 **삭제를 영구로 바꿨다 — 목록 다섯의 「삭제된 항목 n」 탭이 없어졌다.**
  v1.4~v1.5 가 만든 되돌리기(탭 · 연쇄 복구 · 꺼진 채로 돌아오기)를 통째로 걷어내고,
  확인 창의 **첫 문장**이 「삭제한 항목은 영구적으로 지워지며 되돌릴 수 없습니다」다
  (마지막 줄이 아니라 맨 앞인 것이 요점이다). 10장의 「물리 삭제 없음」 조항도 함께
  뒤집혔다. 잘못 지운 한 건을 되찾는 길은 이제 없다 — 남는 것은 변경 이력의 「삭제」
  한 줄과 검수용 [데모 데이터 초기화](전부 버린다)뿐이라, **지우기 전에 보여주는 것**이
  유일한 안전장치였다. **v1.17 은 그 안전장치를 믿지 않고 지우는 자리 자체를 없앴다** (위).
  같은 판에서 대시보드의 **「탭별 조회 비중」 카드를 없앴다 — 차트가 셋에서 둘이다.**
  담당자가 그 수를 보고 할 수 있는 일이 없다(탭 셋은 시민 화면의 고정 구조다).
  겉모습이 같은 「시설 유형별 조회 비중」은 남는다 — 안내판 문구와 등록 우선순위를
  바꾸므로 보고 나서 할 일이 있다.
  관리자 화면의 기준 문서. 화면 목록 · 입력 항목표 · 검증 규칙(V-01~V-07)이
  전부 여기 있다. 관리자 쪽을 고칠 때는 이 문서가 코드보다 앞선다.
  v1.1 에서 **권한 구분과 API 쿼터 설정이 사라졌다**. v1.2 는 그 위에 **최종 관리자
  (`admin`) 하나**를 얹었다 — 업무 화면은 여전히 모든 계정이 같지만,
  **계정 관리만 `admin` 에게 열리고 그 계정은 삭제·비활성화되지 않는다.**
  등급을 되살린 것이 아니라 아무도 못 들어오는 상태를 막는 자물쇠다.
  v1.3 은 **환경 설정 화면을 없앴다 — 화면이 열다섯이다.** 서비스 운영 설정(안내 범위 ·
  배너 기준 · 코스 반경 · 탭별 지도 확대 단계 등)은 **개발 쪽**이고, 변경 이력은 기록만
  남고 열람 화면이 없다.
  v1.4 는 삭제한 것을 화면에서 되돌리게 했다 (「전체 | 삭제된 항목 n」 탭). **v1.7 이
  그것을 되돌렸다 — 삭제가 영구가 됐다** (그리고 v1.17 에서 삭제 자체가 없어졌다). 같은 판에서 **상점가 삭제 차단을 없앴는데**
  그쪽은 지금도 그대로다: 막는 대신 걸린 것이 함께 지워진다.
  v1.5 는 그 「함께」에 **소속 점포를 넣었다**(상점가만 지우면 점포를 볼 수 있는 자리가
  없어진다). 되돌리기를 그 삭제로 실제로 지워진 것으로 좁힌 조항은 v1.7 과 함께 없어졌다.
  안내판은 현장에 남아 있으므로 **QR 지점의 소속 상점가를 M12 에서 고칠 수 있게** 해
  빠져나갈 길을 두었다 — 되돌릴 수 없게 된 뒤로 이 길이 더 중요해졌다.
  같은 판에서 **번호를 당겼다 —
  계정 관리가 M16 에서 M15 가 되고 화면 번호가 M01~M15 로 이어진다** (v1.4 까지의
  개정 이력에 적힌 「M16」은 지금의 M15, 같은 이력의 「M15」는 없어진 환경 설정이다).
  v1.6 은 **비밀번호 초기화 요청이 도착할 자리를 만들었다** — 명세서는 v1.2 부터
  "로그인 화면에서 요청하면 최종 관리자가 계정 관리에서 넣어 준다"고 적었는데 앞쪽
  절반만 화면에 있었다(요청이 아무 데도 남지 않았다). 계정 관리의 탭은 지금 둘이다:
  「전체 | **비밀번호 초기화 요청 n**」 (`admin/data/passwordResets.js`. v1.6 에서는
  가운데 「삭제된 항목 n」이 있어 셋이었고 v1.7 에서 빠졌다).
  같은 판에서 **세션 시간 제한(8시간)을 없애고**(탭을 닫을 때까지 유지 — 브라우저가
  재는 만료는 지킬 수 있는 규칙이 아니었다), **계정 화면의 안내 문단들과 빈 목록의
  뒷문장을 걷어냈다**(칸과 필터 줄이 이미 하고 있는 말이다).
  문서 맨 앞의 개정 이력 참조.
- 디자인 시스템 가이드: `design-systems/README.md`, `design-systems/SKILL.md`
- 지도 오버레이 규칙(5-3 확정본): `docs/S03_map_overlay_rules.md`
- 시민용 화면 현황과 실행 방법: `screens/README.md`
- 관리자 웹: `admin/README.md`

## 디자인 시스템 사용 원칙
- `design-systems/` 하위 파일(_ds_bundle.js, styles.css, tokens/, components/)은
  이번 프로젝트의 기준 디자인 시스템이다. 화면을 만들 때는 반드시 이 시스템의
  컴포넌트와 토큰을 우선 사용한다.
- **공개 진입점이 둘이다** (2026-08-19). 시민용 화면(`screens/**`)은 `index.js` 에서만,
  관리자 화면(`admin/**`)은 `admin.js` 에서만 가져온다. 나눈 이유는 index.js 에 관리자
  컴포넌트를 합치면 재수출을 타고 **시민용 모바일 번들에 데이터 표와 다이얼로그가 들어가기**
  때문이다. 공용 컴포넌트는 admin.js 가 index.js 를 통해 다시 내주므로 공용의 단일 출처는
  그대로 index.js 다.
- 단, 이 디자인 시스템은 고정불변이 아니다. 화면 작업 중 컴포넌트 변경이나
  추가가 필요하면 임의로 새 스타일을 만들지 말고, 디자인 시스템 파일 자체를
  수정/확장한 뒤 그걸 화면에 반영한다. (즉 "이 화면만 다르게" 처리 금지)
- 컴포넌트 변경 시 tokens/와 components/ 양쪽에 일관되게 반영하고,
  변경 사유를 간단히 커밋 메시지나 CHANGELOG에 남긴다.

## 이번 단계 범위
- 산출물 형태: 시민용 모바일 웹 (앱 설치 및 회원가입 없음) + 관리자 웹 (데스크톱)
- 시민용 모바일 웹 12개 화면 (S01~S11) — **완료**
- 관리자 웹 — 관리자 기능명세서 v1.15 의 **화면 열여섯(M01~M16) 전부 완료**.
  범위는 **개별 건의 조회·수정·신규 등록과 오류신고 기반 보정**이다. 데이터 일괄 적재·매칭·
  갱신·검수와 **서비스 운영 설정값**은 **개발 쪽**이며 별도 문서로 분리되었다 (명세서 범위 문단).
  조건부 `C` 항목(축제 프로그램·부스)은 감추지 않고 화면이 그 사실을 적는다.
  `admin/README.md` 참조
- 더미 데이터 기반, 카카오맵 API는 실제 SDK 연동 (placeholder 아님)
- 고정 높이 금지 — 카드/버튼/목록은 가변 높이 (2차 글자 확대 대응)
- 본문 16px 고정, 언어/음성/글자크기 버튼 자리 비워두지 않음

## 화면 구조 (v1.1)
별도 홈 화면이 없다. 하단 탭 3개가 화면 내용을 교체한다.
**S02(공공시설) · S03(상점가) · S04(둘러보기)는 별도 페이지가 아니라 같은 화면의 탭 상태**이며
`screens/main/` 한 폴더에 있다. 페이지를 나누면 탭 전환 때 지도가 다시 뜨는데,
U-CM-16이 그것을 금지한다.

**지도는 S02·S03에만 있다** (2026-08-18 변경). S04 둘러보기는 지도 없이 정보 화면만 쓴다 —
32개소를 한 화면에 담으려면 시 전역까지 줌아웃해야 하는데 그 축척의 핀은 아무것도 알려주지
못했고, 이 탭의 내용(축제·신규매장·코스)도 지도 위의 점이 아니라 읽을거리다.
**지도 인스턴스는 언마운트하지 않고 `display:none`으로 감춘다** — U-CM-16이 금지하는 것은
"재로딩"이지 "안 보이는 것"이 아니다.

**상세·길찾기 화면도 HTML 페이지를 나누지 않는다** (2026-08-18 결정). `screens/detail/` 에
화면별 파일을 두되, 셸(`screens/main/`) 안에서 해시 라우터(`#/facility/:id`, `#/store/:id`)로
전체화면 오버레이(z-modal)를 띄운다. 페이지를 나누면 뒤로 갈 때 지도가 재생성될 뿐 아니라
S03의 필터 상태(업종 칩·온누리·검색어·정렬·시트 스냅)가 통째로 초기화된다 — 335곳에서 조건을
좁혀놓고 한 곳 눌러본 뒤 돌아오면 처음부터 다시 걸어야 한다. 오버레이는 셸을 언마운트하지
않으므로 뒤로가기가 즉시이고 상태가 그대로 남는다. 브라우저 뒤로가기 버튼도 그대로 동작한다.

## 대표 화면 우선순위
S03(상점가 탭)을 기준 화면으로 먼저 확정했다 (v0.8의 S07에 해당).
시트, 필터, 지도 클러스터가 모두 걸리는 유일한 화면이라 여기서 정한 규칙
(지도 오버레이 레이어 7단계, 바텀시트 3단 스냅, 지도 패딩, 필터 고정 범위,
탭바-시트 공존, 탭 전환 시 지도 동작)이 나머지 지도 화면에 그대로 복제된다.
새 화면을 만들 때 이 값을 다시 정하지 말고 `docs/S03_map_overlay_rules.md`를 따른다.

**진행 상황**: 시민용 12개 화면 **전부 완료** (S01~S11 + S03-E, 전체보기 둘 S12 · S13,
**축제 팜플렛 진입 안내 S14**, 그리고 같은 화면의 다른
상태인 S13-S · **S11-A** · **S11-B**). `screens/README.md` 참조.
**검수 입구는 저장소 루트 `index.html` 이다** — 상태 화면을 새로 만들면 그 목록의
「**예외상황 화면**」에 한 줄을 함께 더한다. 거기 없으면 검수하는 사람에게는 없는 화면이다.
그 입구에 **문은 둘뿐이다**(사용자 모바일 웹 · 관리자 웹) — 상세·길찾기·신고로 바로 뛰는
카드를 다시 늘어놓지 않는다 (2026-08-25, 사용자 요청). 하나의 앱이 일곱 조각으로 나뉘어
보였고, 그 여섯은 전부 메인 화면 **안에서** 열리는 자리다.
관리자 웹도 **완료** — 별도 주소 `/admin/`, 데스크톱 전용. `admin/README.md` 참조.
관리자의 입력 항목은 **`admin/data/fields.js` 한 표가 유일한 출처**다 (명세서 항목표 그대로).
화면이 항목을 더 넣고 싶어도 넣을 자리가 없다 — 그게 이 구조의 요점이다.
**관리자가 데이터를 무리로 다루지 않는다** — 재매칭 · 동기화 · 검수 큐는 개발 쪽이다.
**엑셀 일괄 등록은 파일을 받는 데까지만 화면의 일이다** (2026-09-04, 명세서 v1.28) —
**점포·공공시설 목록**의 [등록]이 그 창을 열고(파일보다 먼저 소속 상점가·시설 유형을
고른다), 읽고 넣고 실패한 행을 돌려주는 일은 서버 쪽이다.
**상점가 목록의 [등록]은 폼이다** — 새 상점가는 한 달에 두세 곳이라 한 건씩 받는다.
**시민용 화면 어디에도 관리자로 가는 링크를 두지 않는다** (저장소 루트 `index.html` 은 검수
입구라 예외). 관리자에서 고친 내용은 서버가 없어 시민용 화면에 반영되지 않으며, 그 사실을
관리자 화면 상단 띠가 늘 적는다.

**진입은 `screens/main/App.jsx` 가 맡는다** (2026-08-18 추가). QR 코드를 조회해 S01 로딩을
보이고, 유효하면 셸(MainApp)로, 미등록·훼손·철거면 S11 로, **설치는 끝났으나 아직
활성화하지 않은 지점이면 S11-A**, **조회 자체가 실패하면 S11-B(네트워크 오류)** 로 간다
(2026-08-24). 번호를 가르는 기준은 **「지금 무엇을 하면 되는가」**다 — 다른 QR 을 찍는다 ·
그 자리에서 나중에 다시 찍는다 · 지금 바로 다시 시도한다. 셸보다 **앞에서** 갈라두는
이유는 셸이 서는 순간 지도 SDK 를 받고 335곳을 그리기 때문이다 — 보여줄 수도 없는 지도를
다 만든 뒤에 안내를 덮을 이유가 없다. 정상 진입에서는 여기서 한 번만 갈라지고 그 뒤로
관여하지 않는다 (셸이 다시 언마운트되면 U-CM-16 이 깨진다).

**QR 지점 표는 `screens/main/data/qr.js` 다.** 셸을 세우는 코드가 둘뿐인 것은 의도다 —
더미 데이터(점포·시설·거리·좌표)가 전부 둔전 시장 입구 한 지점 기준이라, 표에 다른 지점을
더하면 이름만 바뀌고 거리는 그대로인 화면이 된다. S03-E(U-ST-16)는 그래서 없는 지점을
지어내는 대신 `?district=none` 검수 플래그로 상태만 켠다 (`TODAY` 와 같은 성격).
**셸을 세우지 않는 코드는 표에 더해도 된다** — 안내 화면에서 갈라져 나가 거리를 한 번도
쓰지 않기 때문이다. 철거된 `qr-002`(S11)와 설치 후 미활성 `qr-003`(S11-A)이
그렇게 들어와 있다. S11-B 는 코드와 무관하므로 `?net=fail` 플래그로 켠다 — **첫 조회만**
실패시킨다 (매번 실패하면 [다시 시도]가 되살아나는지 검수할 수 없다).
**팜플렛 코드 `qr-004`(S14)만 셸을 세우는데 그래도 규칙을 어기지 않는다** (2026-09-02) —
**소속 상점가가 둔전이고 팜플렛의 앵커는 그 상점가에서 읽으므로**(`districtAnchor`) 더미
거리가 그대로 맞는다. 그 값이 `ANCHOR` 와 같은 것은 우연이 아니라 `districts.js` 가
**둔전만 상권 좌표를 QR 지점으로** 두었기 때문이다 — 다른 상점가의 팜플렛이었다면 달라진다.
**이 코드에는 도로명주소가 없다**: 종이에는 붙어 있는 자리가 없어 관리자 항목표에서도
조건부(◐)다.
축제 상태별 문구 셋은 `?pamphlet=<축제 id>` 로 켠다 — 코드를 셋 더 넣으면 같은 자리에 선
같은 지점이 셋이 된다 (지점의 성격이 아니라 축제의 상태가 다를 뿐이다).

## 도보 길찾기(S07)의 전제
**카카오가 공개한 길찾기 REST API 는 자동차 경로다. 도보는 공개 API 가 없다.** 어느 제공자를
쓰든(카카오 협의 · TMAP 보행자 API) REST 키를 보관할 **서버 프록시**를 반드시 거친다 —
REST 키는 브라우저에 들어갈 수 없다. 그래서 경로는 `screens/main/data/walkRoute.js` 한 곳에서만
가져오고, 화면은 그것이 실 응답인지 예시 경로인지 알지 못한다. `config.js` 의
`WALK_ROUTE_ENDPOINT` 를 채우면 전환되며 **화면 코드는 손대지 않는다.**